% Do not change document class, margins, fonts, etc.
\documentclass[a4paper,oneside,bibliography=totoc]{scrbook}

% some useful packages (add more as needed)
\usepackage[utf8]{inputenc}
\usepackage{graphicx}
\usepackage{latexsym}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{algorithm} % you can modify the algorithm style to your liking
\counterwithin{algorithm}{chapter} % so that algorithms have chapter numbers as well
\usepackage{algorithmic}
\usepackage{csquotes}
\renewcommand{\algorithmiccomment}[1]{\hfill\textit{// #1}}
\usepackage[usenames,dvipsnames]{xcolor}
\usepackage[colorlinks,citecolor=Green]{hyperref} % you may change/remove the colors
% lipsum package removed: not available on this cluster's TeX install and only used by template placeholder text below

% chicago citation style
\usepackage{natbib}
\bibliographystyle{plainnat} % chicagoa.bst is unavailable on this cluster's TeX install; plainnat ships with natbib and works with the authoryear \setcitestyle below; switch back to chicagoa if your submission environment has it
\setcitestyle{authoryear,round,semicolon,aysep={},yysep={,}} \let\cite\citep

% example enviroments (add more as needed)
\newtheorem{definition}{Definition} \newtheorem{proposition}{Proposition}

%remove this: 
% \textcolor{red}{TODO: }
\usepackage{xcolor}
\usepackage[htt]{hyphenat}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, fit}

\begin{document}

\frontmatter \subject{Master Thesis} % change to appropriate type
\title{Design and Evaluation of Efficient Semantic Company Retrieval at Scale}
\author{Marmee Pandya\\
(matriculation number 1963521)} \date{\today}
\publishers{{\small Submitted to}\\
Data and Web Science Group\\


Dr.\ Ralph Peeters\\ University of Mannheim\\}
\maketitle

\chapter{Abstract}

A company that is a perfect match for a search query can still be invisible to a keyword-based search system, simply because its own description never repeats the query's exact wording. This vocabulary mismatch problem sits at the centre of a concrete question for ISTARI.AI's company search product: can embedding-based semantic retrieval, paired with approximate nearest-neighbour search, generate accurate candidate sets efficiently at scale, and does it actually outperform the keyword-driven production system currently in use? Comparing more than ten dense embedding models, a late-interaction architecture, and several hybrid pipelines over a corpus of 98,716 companies, a learned fusion ranker trained directly on this task's own queries reaches the strongest result under the standard protocol, Recall@1,000~$=$~0.823.

That number turns out to rest on shakier ground than it first appears. No human relevance judgements exist for this task, so the only available yardstick is production's own output, a circular standard that can never show a system beats production if production itself defines what counts as correct. This thesis measures exactly how much that circularity was hiding. Retrained against an independently built, multi-judge silver standard, the fusion ranker's own interpretive model reverses one of its most confident conclusions, and production's own results turn out to miss 27.3\% of independently verified relevant companies outright. Tested on a pool of companies production had never even ranked, the same pipeline recovers 88.5\% of them, the strongest evidence yet that this coverage gain is genuine rather than an artefact of how the evaluation was built.

At the largest tested scale, 397,025 companies, roughly 2\% of Istari's full database, approximate nearest-neighbour search matches exact search's retrieval quality within 2.5\% while running up to 78 times faster, a gap confirmed by statistical testing rather than asserted from raw numbers alone, establishing a favourable trend at the scale actually tested rather than a claim about full production scale. Together, these results make a concrete case that semantic retrieval can outperform a keyword-based production system on both accuracy and speed, and that trusting a production system's own output as ground truth, a common shortcut when no human-labelled data exists, deserves far more scrutiny than it typically receives.

% table of contents
\begingroup%
\hypersetup{hidelinks}% disable link color in TOC only
\linespread{0.96}\selectfont%
\tableofcontents%
\endgroup

% none of the things below is needed, but you may add them if you feel that they
% are helpful for your work \listofalgorithms \listoffigures \listoftables
% \listtheorems{definition} \listtheorems{proposition}


% okay, start new numbering ... here is where it really starts
\mainmatter
% here the original content of my thesis starts 

\chapter{Introduction}
\label{ch:introduction}

A company that is a strong match for a search query can still be invisible to a keyword-based search system, simply because its own description uses different words than the query does. A search for a \textit{``renewable energy consultancy''}, for example, will not surface a company that describes itself as a \textit{``sustainability advisory service''}, even though the company may be exactly the kind of business the query is looking for. This is a well-known limitation of classical keyword-based retrieval. Sales teams targeting prospects, investors screening deals, and analysts mapping a market or a supply chain all depend on locating the right handful of relevant organisations out of a much larger pool, often millions of candidates. Providers such as ISTARI.AI build company intelligence platforms specifically to support this need, maintaining datasets containing tens of millions of organisation profiles, covering everything from small local businesses to large multinational enterprises. Existing workflows at these platforms, however, typically rely on exactly the manually configured keyword filters that are vulnerable to the mismatch problem described above.

At the same time, any system intended to operate on a company database of this size must satisfy strict constraints on efficiency and scalability. Retrieval has to remain accurate over a dataset that is high-cardinality in a specific sense. It spans tens of millions of distinct organisation profiles, any one of which could be the correct answer to a given query, rather than a small, fixed set of categories a classifier could simply learn to distinguish. It also has to return results quickly enough to support an interactive search experience, not just a slow, offline batch process.

This thesis is conducted in collaboration with ISTARI.AI (Chapter~\ref{ch:istariAndData} introduces the company and its data), which grounds the thesis in a real operational setting.

\paragraph{Research context and gap.} Dense, embedding-based retrieval and approximate nearest-neighbour search are well established individually: bi-encoder architectures, late-interaction models, and hybrid fusion methods have all been shown to outperform keyword matching on standard information retrieval benchmarks \citep{karpukhin2020dense} (Chapter~\ref{ch:related_literature} reviews this literature in full). What is missing is evidence for how these methods behave on large-scale company profiles specifically, an applied setting closer to entity-level retrieval and company-embedding work \citep{gerling2023company} than to general-purpose passage benchmarks, where records combine heterogeneous structured fields with AI-generated free text. This is the first gap this thesis addresses: applying established retrieval methods to a domain with genuinely different data characteristics.

\paragraph{Evaluation gap.} A second, deeper gap concerns evaluation itself. Existing retrieval research is predominantly validated against benchmarks with human relevance judgements. For Istari's company-search task, no independently constructed relevance judgements were available for the full corpus and query set at the start of this project, since Istari's 20-million-company database has never been exhaustively labelled by anyone. In their absence, a natural fallback is to treat production's own retrieved companies as a proxy for relevance, a practical benchmark when human judgements are unavailable~\citep{voorhees2002philosophy, thakur2021beir}. Such a proxy is circular by construction, however: it can support operational comparisons between systems, but it cannot establish whether a competing system retrieves relevant companies that production itself misses. This thesis treats that circularity as part of the research problem rather than a limitation to note in passing, and constructs an independent relevance standard, built incrementally rather than all at once (Section~\ref{sec:silver_labels}), to test it directly.

\paragraph{Problem statement.} Put directly, this thesis addresses one specific, technical problem: it is not known whether embedding-based semantic retrieval can outperform Istari's existing keyword-based production system, at production-relevant scale and query latency, and it is not known whether any such comparison, as currently measured, is valid at all.

\paragraph{Research questions.} This problem translates into three research questions, addressed in turn across Chapters~\ref{ch:methodology} and~\ref{ch:results}:

\begin{enumerate}
\item[\textbf{RQ1}] Can embedding-based semantic retrieval, combined with approximate nearest-neighbour search, generate accurate top-$k$ candidate company sets from a large-scale dataset while keeping query latency low, and how does its retrieval quality compare to Istari's keyword-based production system?
\item[\textbf{RQ2}] Does combining multiple, complementary retrieval signals, culminating in a model trained directly on this task's own queries, retrieve more relevant companies than any single retrieval method alone?
\item[\textbf{RQ3}] Is the production-derived evaluation protocol used to answer RQ1 and RQ2 itself trustworthy, and do the retrieval gains observed under it still hold once tested against an independently constructed, non-circular relevance standard?
\end{enumerate}

\paragraph{Contributions.} This thesis makes five main contributions:

\begin{enumerate}
\item A controlled, common-protocol comparison of retrieval approaches for company search, covering lexical retrieval, dense bi-encoder retrieval across more than ten models spanning different sizes and training objectives, late-interaction retrieval, and retrieval-specific pretraining, all evaluated on the same corpus, query set, and metrics.
\item A hybrid and learned retrieval framework that combines complementary retrieval signals, culminating in a fusion ranker trained on this task's own queries that outperforms every pipeline built from frozen, pretrained components under the protocol it is trained and evaluated against, together with an interpretable account of which retrieval signals drive relevance.
\item An analysis of the circularity inherent in production-derived pseudo-relevance, together with a production-independent silver standard, built from external LLM judgements and human adjudication, testing whether the production-based evaluation's conclusions, including the fusion ranker's own findings, still hold once that circularity is removed.
\item An analysis of the accuracy-efficiency trade-off of approximate nearest-neighbour search across corpus sizes of up to 397,025 companies, with a latency analysis separating local computation from network overhead for API-based methods.
\item A quantified data-quality issue in which some query topics, most severely ``web hosting companies'' (85.4\% of the companies judged relevant to that query), have AI-generated summaries that describe a website hosting or platform provider rather than the company itself.
\end{enumerate}

\paragraph{Scope.} These contributions come with explicit boundaries. ``At scale'' throughout this thesis means the largest corpus directly evaluated. This corpus reaches nested tiers of up to 397,025 companies (Section~\ref{sec:scaling_corpus}), roughly 2\% of Istari's full 20-million-company database. It does not mean an end-to-end evaluation at full production scale. The experiments establish a scalability trend up to the tested size, and Chapter~\ref{ch:conclusion} discusses what would be needed to extend it further. This thesis evaluates retrieval quality and computational efficiency under the query set and corpus described in Chapter~\ref{ch:methodology}. It does not evaluate the end-user search experience, run a live production A/B test, or build and deploy a production system. Every result reported here is measured offline against the datasets and pipelines described in that chapter.

\paragraph{Thesis outline.} The remainder of this thesis is organised as follows:

\begin{itemize}
\item Chapter~\ref{ch:istariAndData} introduces Istari's Global Organisation Index, the dataset underlying this thesis.
\item Chapter~\ref{ch:related_literature} reviews the relevant retrieval and evaluation literature.
\item Chapter~\ref{ch:methodology} describes the experimental setup and the design of every baseline and experiment.
\item Chapter~\ref{ch:results} reports the results, findings, and interpretation for each of those experiments.
\item Chapter~\ref{ch:conclusion} summarises the main findings and outlines directions for future work.
\end{itemize}

\chapter{ISTARI.AI and its Data}
\label{ch:istariAndData}

\section{About ISTARI.AI and the Global Organisation Index}

This thesis is conducted in collaboration with ISTARI.AI\protect\footnote{\url{https://docs.istari.ai/goi/goi_start_page/}}, a data intelligence company specialising in firmographic data at scale. Istari's core product is a curated, organisation-level dataset covering organisations worldwide, enabling use cases such as market analysis, company search, and lead generation. The central research challenge addressed in this thesis is improving the quality of semantic company retrieval, which arises directly from Istari's operational needs.

The primary dataset used in this thesis is Istari's \textbf{Global Organisation Index} (GOI), a curated collection of approximately 20 million active organisations spanning 232 countries and territories. The GOI serves as the canonical source of firmographic data within Istari's data infrastructure, covering organisation identity, location, industry classification, organisation size, and other firmographic attributes.

\section{Data Pipeline and Methodology}

The GOI is constructed through a multi-stage validation pipeline that deliberately prioritises data quality over volume. The pipeline proceeds as follows:

\begin{enumerate}
\item \textbf{Registry Collection.} National registers are systematically queried worldwide, including commercial registers, company registers, and association registers. These primary sources are enriched with additional open data sources, yielding an initial pool of approximately 400 million organisations.

\item \textbf{Domain Attribution.} Each organisation is assessed for attribution to an identifiable web domain. Approximately 10\% of the initial pool, around 40 million organisations can be matched to a domain. The remainder are excluded as they are either no longer active, were never truly operational (e.g., pure holding structures), or lack a web presence.

\item \textbf{Activity Verification.} The 40 million domain-attributed organisations are further filtered by verifying that their domains are still actively operated. This final step reduces the dataset to approximately 20 million organisations that are demonstrably active.
\end{enumerate}

According to Istari, this pipeline results in a GOI in which every record is both web domain-verified and confirmed active. Istari positions this as a differentiator from traditional firmographic databases that typically retain many inactive or dormant records.

\subsection{Schema and Key Fields}

Each record in the GOI contains a set of core identity fields, geographic fields, and classification fields. The fields most relevant to this thesis are summarised in Table~\ref{tab:goi_schema}.

\begin{table}[h]
\centering
\caption{Key fields of the GOI schema used in this thesis.}
\label{tab:goi_schema}
\begin{tabularx}{\textwidth}{llX}
\toprule
\textbf{Field} & \textbf{Type} & \textbf{Description} \\
\midrule
\texttt{name}              & STRING  & Organisation name \\
\texttt{domain}            & STRING  & Organisation website domain (unique identifier) \\
\texttt{summary}           & STRING  & AI-generated textual summary of the organisation \\
\texttt{keywords}          & LIST    & Keywords \\
\texttt{employee\_class}            & STRING  & Employee count bracket \\
\texttt{country}            & STRING  & Country \\
\texttt{country\_code}            & STRING  &  ISO Country Code \\
\texttt{state / state\_code}            & STRING  & State or province \\
\texttt{region / region\_code}            & STRING  & Region \\
\texttt{district / district\_code}            & STRING  & District \\
\texttt{municipality / municipality\_code}            & STRING  & Municipality \\
\texttt{address}            & STRING  & Full address \\
\texttt{latitude}            & FLOAT64  & Latitude coordinate \\
\texttt{longitude}            & FLOAT64  & Longitude coordinate \\
\texttt{nace\_code}        & STRING  & NACE industry classification code \\
\texttt{organization\_type}& STRING  & Company, Startup, Academic, Public, or Other \\
\texttt{organization\_size}& STRING  & Micro, Small, Medium-sized, or Large enterprise \\
\bottomrule
\end{tabularx}
\end{table}

Of particular importance for this thesis is the \texttt{summary} field. This field contains an AI-generated natural language description of each organisation, synthesised from its web presence and available structured data. It is the most semantically rich and consistently populated field in the dataset, with effectively no missing values in the experimental corpus, and therefore anchors the text representation fed to every retrieval model evaluated in this thesis, alongside the other GOI fields. This field is not without its own data-quality issues, however. For a subset of query topics, the summary instead describes a website hosting or platform provider rather than the company's own business, an issue quantified directly in Section~\ref{sec:trust_filter_result}.

\subsection{Industry Classification: NACE Codes}

Industries in the GOI are classified using a taxonomy Istari refers to as \textbf{NACE} \protect\footnote{\url{https://ec.europa.eu/eurostat/web/nace}} (\textit{Nomenclature statistique des activités économiques dans la Communauté européenne}), the European Union's standard statistical classification of economic activities. An inspection of the \texttt{nace\_code} values in the corpus finds 22 distinct sections, one more than the official 21-section NACE Rev.\,2 standard. The section letters also do not consistently correspond to the official EU lettering (for example, the data's section K covers telecommunications and computing, whereas official NACE Rev.\,2 assigns K to financial and insurance activities). This suggests the section labels reflect Istari's own \texttt{nace\_code} categorisation rather than a literal reproduction of the EU standard. Table~\ref{tab:nace_top} lists the ten most represented sections in the dataset.

\begin{table}[h]
\centering
\caption{Top 10 industries in the GOI by organisation count.}
\label{tab:nace_top}
\begin{tabular}{llrr}
\toprule
\textbf{NACE} & \textbf{Industry} & \textbf{Count} & \textbf{Share (\%)} \\
\midrule
N & Professional, scientific \& technical activities & 3{,}093{,}879 & 18.03 \\
G & Wholesale \& retail trade                        & 2{,}240{,}191 & 13.05 \\
C & Manufacturing                                    & 2{,}006{,}435 & 11.69 \\
R & Human health \& social work                      & 1{,}440{,}340 &  8.39 \\
K & Telecom, IT \& computing                         & 1{,}269{,}131 &  7.39 \\
F & Construction                                     & 1{,}173{,}226 &  6.84 \\
I & Accommodation \& food service                    & 1{,}126{,}594 &  6.56 \\
S & Arts, sports \& recreation                       & 1{,}101{,}592 &  6.42 \\
Q & Education                                        &   584{,}430 &  3.41 \\
J & Publishing, broadcasting \& content              &   554{,}985 &  3.23 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Organisation Size Classification}

Organisation size in the GOI is derived from employee and revenue signals and bucketed into four categories following the European Union's definitions of small and medium-sized enterprises (SME), as shown in Table~\ref{tab:size_dist}.

\begin{table}[h]
\centering
\caption{Organisation size distribution in the GOI.}
\label{tab:size_dist}
\begin{tabular}{llrr}
\toprule
\textbf{Size} & \textbf{Employee Range} & \textbf{Count} & \textbf{Share (\%)} \\
\midrule
Micro           & 0--9   & 7{,}341{,}838 & 42.78 \\
Small           & 10--49 & 7{,}494{,}409 & 43.67 \\
Medium-sized    & 50--249 & 1{,}501{,}655 &  8.75 \\
Large enterprise & 250+  &   824{,}126 &  4.80 \\
\bottomrule
\end{tabular}
\end{table}

The distribution is heavily skewed towards smaller organisations: over 86\% of the GOI consists of micro and small enterprises.

\subsection{Geographic Coverage}

The GOI covers 232 countries and territories. Table~\ref{tab:geo_top} lists the top five countries by volume.

\begin{table}[h]
\centering
\caption{Top 5 countries by organisation count in the GOI.}
\label{tab:geo_top}
\begin{tabular}{lr}
\toprule
\textbf{Country} & \textbf{Organisations} \\
\midrule
United States   & 3{,}626{,}794 \\
Germany         & 1{,}828{,}181 \\
United Kingdom  & 1{,}013{,}026 \\
Netherlands     &   778{,}416 \\
Italy           &   625{,}754 \\
\bottomrule
\end{tabular}
\end{table}

Approximately 2.7 million organisations in the GOI do not have a standardised administrative region. These records are intentionally retained as they remain valuable for non-geographic analyses such as industry or domain-level retrieval, the primary focus of this thesis.

\subsection{Evaluation Dataset}

The full 20-million-company GOI corpus is not directly used for retrieval in this thesis. Instead, Istari provided an evaluation dataset consisting of 101 real-world search queries along with each query's top-1\,000 production results, yielding 101\,000 (query, company) pairs. The same company can legitimately appear in more than one query's top-1\,000 results, so this thesis deduplicates the 101\,000 pairs by \texttt{domain}, keeping a single record per company; after this deduplication, the evaluation corpus covers \textbf{98\,716 unique companies}. All retrieval experiments described in Chapter~\ref{ch:methodology} are conducted over this corpus. Consequently, the 98\,716-company corpus should not be interpreted as a random sample of the full GOI; it is derived from the companies production itself returned for the 101 queries. The exact pseudo-relevance threshold used to define ``relevant'' companies for evaluation (and how it changes across experiments) is described in Sections~\ref{subsec:eval_protocol} and~\ref{sec:protocol_note}.

Testing retrieval quality at the full 20-million-company scale was not pursued in the current phase of this thesis, since doing so would require a substantially larger data export from Istari than the query-level top-1\,000 results used here; this is discussed as an open question in Chapter~\ref{ch:conclusion}.

\chapter{Literature Review}
\label{ch:related_literature}

This chapter reviews the retrieval paradigms, applied literature, and evaluation practices this thesis builds on and compares against, organised as follows:
\begin{itemize}
\item Section~\ref{sec:lit_sparse_dense} covers sparse retrieval and Section~\ref{sec:lit_dense} covers dense retrieval, the two foundational paradigms for the baselines described in Chapter~\ref{ch:methodology}.
\item Section~\ref{sec:lit_ann} covers approximate nearest neighbour search, the technique that makes dense retrieval practical at scale.
\item Section~\ref{sec:lit_late_interaction} covers late-interaction retrieval and retrieval-specific pretraining, the two alternative paradigms evaluated in Sections~\ref{sec:simlm}--\ref{sec:colbert}.
\item Section~\ref{sec:lit_hybrid} covers hybrid retrieval, cross-encoder reranking, and learning to rank, the techniques underlying the hybrid pipelines in Sections~\ref{sec:hybrid_fullpool}--\ref{sec:learned_fusion}.
\item Section~\ref{sec:lit_entity} reviews the applied literature closest to this thesis's own task, entity matching, entity resolution, and company-level embeddings, and identifies what that literature does not yet address.
\item Section~\ref{sec:lit_evaluation} covers evaluation practice in information retrieval when human relevance judgements are unavailable, directly motivating the pseudo-relevance protocol used throughout this thesis (Section~\ref{subsec:eval_protocol}).
\end{itemize}

% ────────────────────────────────────────────────────────────
\section{Sparse Retrieval}
\label{sec:lit_sparse_dense}
% ────────────────────────────────────────────────────────────

Text retrieval systems fall into two broad families. \textbf{Sparse} retrieval represents queries and documents as high-dimensional vectors of term weights and scores matches by term overlap. A widely used sparse retrieval method is BM25 \citep{robertson2009probabilistic}, a probabilistic scoring function that weighs each query term by its frequency in a document and its rarity across the whole collection. BM25 is fast, requires no training, and remains a strong baseline decades after its introduction. However, it cannot reliably match queries and documents that describe the same concept using different words, a limitation known as the vocabulary mismatch problem and directly observed for this thesis's task in Section~\ref{subsec:bm25_qual}.

% ────────────────────────────────────────────────────────────
\section{Dense Retrieval}
\label{sec:lit_dense}
% ────────────────────────────────────────────────────────────

\textbf{Dense} retrieval instead represents queries and documents as fixed-size vectors learned by a neural encoder, and scores matches by vector similarity rather than literal term overlap. Dense Passage Retrieval (DPR) \citep{karpukhin2020dense} popularised the bi-encoder architecture for open-domain retrieval, using separate encoders for queries and documents (or, in this thesis, companies). The two encoders are trained so that relevant query-document pairs are close in vector space, while each side is encoded independently. This allows documents to be embedded once, offline, and their representations reused across future queries. Sentence-BERT \citep{reimers2019sentence} showed that the same bi-encoder idea, applied to general-purpose sentence embeddings rather than a retrieval-specific training objective, produces embeddings useful for semantic similarity and retrieval tasks broadly, and underlies the general-purpose sentence-transformer models (including MiniLM, Section~\ref{subsec:minilm}) used throughout this thesis.

More recent embedding models, such as the BGE family \citep{xiao2024cpack,chen2024bge}, combine this bi-encoder architecture with large-scale contrastive pretraining and hard-negative mining, and have become strong general-purpose dense retrieval models, forming the basis of several baselines evaluated in Sections~\ref{subsec:bge} and~\ref{sec:extended_baselines}.

Two distinct further directions in embedding model development are also represented among this thesis's extended baselines (Section~\ref{sec:extended_baselines}): GTE \citep{li2023towardsgte} and Nomic Embed \citep{nussbaum2024nomic} show that a comparatively small (100--300M-parameter) encoder, trained with careful multi-stage contrastive learning over diverse data, can match or exceed much larger models. E5-Mistral \citep{wang2024improving}, SFR-Embedding-Mistral \citep{meng2024sfrembedding}, and Linq-Embed-Mistral \citep{linq2024technical} instead take the opposite approach, adapting an already-large 7-billion-parameter instruction-tuned language model (Mistral-7B) into an embedding model via further contrastive fine-tuning. This split between ``small and carefully trained'' and ``large and repurposed'' is directly tested for this thesis's own task in Section~\ref{sec:extended_baselines_result}, where it does not resolve in the direction model scale alone would predict.

Public leaderboards such as the Massive Text Embedding Benchmark (MTEB) \citep{muennighoff2023mteb} rank models like these against one another across many tasks, but its results, like all such leaderboards, reflect performance averaged over its constituent benchmark datasets, not any particular downstream domain. The extent to which a strong MTEB ranking transfers to company-profile retrieval specifically is itself part of what Section~\ref{sec:extended_baselines_result} investigates.

% ────────────────────────────────────────────────────────────
\section{Approximate Nearest Neighbour Search}
\label{sec:lit_ann}
% ────────────────────────────────────────────────────────────

Dense retrieval requires finding the nearest vectors to a query vector among the full document collection. Exact search costs time linear in the number of documents, which becomes impractical at the scale of millions of documents relevant to this thesis's target deployment (Section~\ref{ch:istariAndData}). Approximate nearest neighbour (ANN) search instead searches only a carefully chosen subset of the collection, avoiding a full linear scan. The central trade-off is between recall, how often the true nearest neighbours are actually found, and query latency, and unlike exact search this trade-off is tunable. It depends on the index structure and its search-time parameters, not on a single fixed accuracy cost paid by approximating at all. FAISS \citep{johnson2017billion} provides GPU-accelerated implementations of both exact and approximate search, including inverted-file indexes (IVF), which cluster the embedding space in advance and, at query time, search only the clusters nearest the query rather than every vector, reducing candidate comparisons at the cost of possibly missing a true neighbour that fell into a cluster the query never visits. Hierarchical Navigable Small World graphs (HNSW) \citep{malkov2020efficient} take a different approach. They build a multi-layer proximity graph over the document vectors at index-construction time, then greedily traverse it at query time, so the accuracy-latency trade-off is governed by how deep and wide that traversal is allowed to go rather than by how many clusters are searched. Both approaches also carry a memory and index-build cost that a linear scan does not, IVF's cluster centroids and HNSW's proximity graph must be built once, in advance, and held in memory alongside the vectors themselves, a cost this thesis's own scaling experiment tracks directly rather than assumes away. Both IVF and HNSW are evaluated directly in this thesis's ANN scaling experiment (Section~\ref{sec:ann_scaling}), which tests whether their recall-latency trade-off remains favourable as corpus size grows toward simulated production scale.

% ────────────────────────────────────────────────────────────
\section{Late Interaction and Retrieval-Specific Pretraining}
\label{sec:lit_late_interaction}
% ────────────────────────────────────────────────────────────

A bi-encoder represents an entire document with a single fixed-size vector, which can discard fine-grained information relevant to a specific query. ColBERT \citep{khattab2020colbert} addresses this with \emph{late interaction}. Queries and documents are each represented as sets of per-token vectors, and relevance is scored by summing, for each query token, the maximum similarity to any document token (MaxSim). This avoids requiring a single vector to summarise the whole document in advance.

This adds compute and storage cost relative to a single-vector bi-encoder, since retrieval now requires comparing token sets rather than single vectors. ColBERTv2 \citep{santhanam2022colbertv2} introduces residual compression of the per-token vectors, substantially reducing this cost, and the subsequent PLAID engine \citep{santhanam2022plaid} builds on that compressed representation to make late-interaction search efficient at scale. This thesis instead implements MaxSim scoring directly (Section~\ref{sec:colbert}), trading PLAID's efficiency for an exact, dependency-light implementation suited to the scale of the evaluation corpus used here.

A separate line of work improves dense retrieval not through architecture but through pretraining. SimLM \citep{wang2022simlm} pretrains a bi-encoder with a representation-bottleneck objective before any retrieval-specific fine-tuning, explicitly designed to produce embeddings well suited to downstream passage retrieval. This thesis evaluates whether that retrieval-specific pretraining transfers to a domain (company profiles) quite different from the passage-retrieval benchmarks it was designed for (Section~\ref{sec:simlm}).

% ────────────────────────────────────────────────────────────
\section{Hybrid Retrieval, Reranking, and Learning to Rank}
\label{sec:lit_hybrid}
% ────────────────────────────────────────────────────────────

Sparse and dense retrieval, and different dense models, make different errors. A hybrid system that combines multiple retrieval signals can recover from a single method's individual weaknesses. Reciprocal Rank Fusion (RRF) \citep{cormack2009reciprocal} is a simple, training-free method for combining several ranked lists. Each document's fused score is computed as the sum of $1/(k_{\mathrm{RRF}} + \mathrm{rank})$ across the lists in which it appears, requiring only that documents be ranked rather than that their raw scores be comparable across methods. RRF underlies the hybrid baseline in Section~\ref{sec:rrf}.

A complementary technique is \emph{reranking}. A first-stage retriever returns a candidate set cheaply, and a more expensive model reorders it. Cross-encoder rerankers, such as the \texttt{bge-reranker-v2-m3} \citep{baaireranker2024} and \texttt{mxbai-rerank-large-v1} \citep{mixedbread2024mxbai} models used in Sections~\ref{sec:hybrid_fullpool} and~\ref{sec:reranker_ablation}, feed the query and each candidate into the same transformer jointly, rather than encoding them independently as a bi-encoder does. This lets the model attend directly between query and candidate tokens, typically producing more accurate relevance scores than a bi-encoder \citep{nogueira2019passage}, at the cost of requiring a full forward pass per candidate rather than a single fast vector comparison, so cross-encoders are used to rerank a small candidate set rather than to search a full collection.

More generally, combining multiple retrieval signals into a single relevance score can be framed as \emph{learning to rank} \citep{liu2009learning}. Instead of a fixed, hand-designed combination rule such as RRF, a model is trained directly on labelled query-document pairs to learn how to weight and combine whatever features are available (similarity scores, ranks, or other signals) into a final ranking. This is the approach taken by the learned fusion ranker in Section~\ref{sec:learned_fusion}, implemented with the logistic regression and gradient-boosted tree models provided by scikit-learn \citep{pedregosa2011scikit}.

% ────────────────────────────────────────────────────────────
\section{Company and Entity-Level Retrieval}
\label{sec:lit_entity}
% ────────────────────────────────────────────────────────────

Much of the retrieval literature reviewed above is developed and evaluated on general-purpose passage and document retrieval benchmarks, questions answered from Wikipedia or news text. Comparatively little of it addresses retrieval over company-profile data specifically. The applied literature closest to this thesis's own task instead comes from \emph{entity matching} and \emph{entity resolution}. Both address the problem of deciding whether two records, often company or product records drawn from different sources, refer to the same real-world entity. DeepMatcher \citep{mudgal2018deep} was among the first to systematically apply deep learning to this problem, and Ditto \citep{li2020deep} later showed that fine-tuning a pretrained language model on the same task, framed as a sequence-pair classification problem, achieves very high accuracy, over 96\% F1 on company-matching benchmarks specifically. More recently, \citet{peeters2025entity} show that large language models, prompted directly rather than fine-tuned for the task, can match or exceed these earlier approaches on the same entity matching benchmarks. Entity matching and the retrieval task addressed in this thesis are related but distinct. Matching decides whether two already-identified records are the same entity, a pairwise classification problem, while retrieval must first find, from a natural-language query, which of millions of candidate companies are even worth considering, a search problem over an entire corpus rather than a single pair.

Closer still to this thesis's own setting is Company2Vec \citep{gerling2023company}, which builds company embeddings directly from the unstructured text of corporate websites, the same kind of web-derived company description this thesis's own AI-generated summaries are built from, and uses those embeddings to identify similar companies for peer-firm and industry-classification tasks. Company2Vec demonstrates that web-derived company text carries a usable semantic signal for retrieval-adjacent tasks, but it does not address ranked retrieval against a natural-language query, evaluate against a validated relevance standard, or test at the scale addressed in this thesis. This is precisely the gap this thesis fills. General-purpose semantic retrieval is well studied, and company-level embeddings have been shown to work for adjacent tasks like matching and peer-firm identification. The literature reviewed for this thesis did not identify prior work evaluating full retrieval, from a natural-language query to a ranked set of company candidates, on data and at a scale comparable to this thesis's own, under an evaluation protocol whose own validity has been separately established.

% ────────────────────────────────────────────────────────────
\section{Evaluation Without Human Relevance Judgements}
\label{sec:lit_evaluation}
% ────────────────────────────────────────────────────────────

Even once a retrieval task and domain are defined, evaluating a system against it requires a way to decide which results are actually relevant. Standard information retrieval evaluation compares each system's ranking against a set of human relevance judgements for a fixed query set, historically constructed through pooling. In pooling, the top results from several systems are merged and judged by human assessors, as established by the methodology of the Text REtrieval Conference (TREC) \citep{voorhees2002philosophy}. No such human-annotated relevance judgements exist for the company search task addressed in this thesis. In their absence, this thesis instead adopts a \emph{pseudo-relevance} protocol (Section~\ref{subsec:eval_protocol}). Under this protocol, a company is treated as relevant if it appears in the existing production system's own results for that query. This provides a practical way to compare systems when human judgements are unavailable, although the resulting scores measure agreement with the reference system rather than absolute relevance, a limitation this thesis states explicitly wherever pseudo-relevance labels are used. Separately, the BEIR benchmark \citep{thakur2021beir} demonstrates the value of evaluating retrieval methods across diverse tasks and domains using established relevance assessments, rather than relying on a single retrieval task or domain.

A more recent alternative to human pooling, and the one this thesis relies on for its independent silver standard (Section~\ref{subsec:pooled_gold_standard}), is to use large language models themselves as relevance judges. \citet{thomas2024llmjudge} show that LLM judgments can be useful for large-scale system comparison, but agreement with human judgement, prediction of user preference, and agreement between LLM judges are related, distinct claims, and reliability on any of them depends on the task, the prompting procedure, and the judge model used. Published findings on one task or judge pairing therefore cannot simply be assumed to transfer to this thesis's own. This is exactly why this thesis's own blind calibration check (Section~\ref{subsec:pooled_gold_standard_result}) tests agreement directly against its own judges and task, rather than relying on the literature's findings alone. This approach is not without open problems. A broader survey of LLM-as-a-judge methods \citep{li2024fromgeneration} documents recurring failure modes across the wider literature, including inconsistency across repeated runs, sensitivity to the order candidates are presented in, and systematic scoring biases that do not necessarily show up in simple percent-agreement figures, part of the motivation for this thesis's use of Cohen's kappa rather than raw agreement alone (Section~\ref{subsec:pooled_gold_standard_result}) and for cross-checking judges from different model families rather than relying on a single model's judgments throughout. Because these labels are produced through a model-assisted, human-adjudicated process rather than exhaustive expert annotation, this thesis refers to them throughout as a \textbf{silver standard} rather than a gold standard, a distinction discussed further in Section~\ref{subsec:pooled_gold_standard}.


% ============================================================
\chapter{Methodology}
\label{ch:methodology}

This chapter describes the experimental setup, evaluation protocol, computational environment, and the design of every retrieval baseline and experiment carried out for the company search task over Istari's Global Organisation Index~(GOI). It answers \emph{what was built and why}. The companion chapter, Chapter~\ref{ch:results}, reports \emph{what happened when it was run}: the resulting numbers, tables, findings, and interpretation for every experiment described here.

Both this chapter and Chapter~\ref{ch:results} are organised into the same five conceptual phases, so that a method described here under a given phase has its results reported under the correspondingly numbered phase in Chapter~\ref{ch:results}, even though the two chapters' own section numbers do not line up one-to-one.
\begin{itemize}
\item \emph{Phase~I, baselines} (Section~\ref{sec:phase1_method}, results in Section~\ref{sec:phase1_result}): lexical and dense retrieval compared under the production-derived pseudo-relevance protocol.
\item \emph{Phase~II, improving retrieval} (Section~\ref{sec:phase2_method}, results in Section~\ref{sec:phase2_result}): query expansion, hybrid fusion, reranking, and additional retrieval paradigms, all still evaluated against production-derived labels, culminating in the learned fusion ranker.
\item \emph{Phase~III, evaluation validity} (Section~\ref{sec:phase3_method}, results in Section~\ref{sec:phase3_result}): the discovery that Phase~II's labels are circular, and the construction, validation, and active-learning expansion of an independent silver standard for subsequent evaluation.
\item \emph{Phase~IV, learned retrieval revisited} (Section~\ref{sec:phase4_method}, results in Section~\ref{sec:phase4_result}): tuning and fine-tuning experiments re-run against the new, non-circular standard, with paired significance testing added for the comparisons that matter most.
\item \emph{Phase~V, scalability} (Section~\ref{sec:phase5_method}, results in Section~\ref{sec:phase5_result}): the pipeline tested on a corpus roughly four times larger, with IVF and HNSW approximate search re-evaluated at that scale to check whether their recall-latency trade-off still holds.
\end{itemize}
Each phase's central question is listed at the start of Chapter~\ref{ch:results}; each phase's evaluation protocol is given in Table~\ref{tab:protocol_summary} below.

\section{Notions of Relevance Used in This Thesis}
\label{sec:protocol_note}

The pseudo-relevance protocol defined in Section~\ref{subsec:eval_protocol} (production's top-1\,000 results per query) is used throughout this chapter's baseline, reranking, fusion, and oracle-ceiling experiments (Sections~\ref{sec:exp_results}--\ref{sec:oracle_union}). This thesis later introduces two further, independent notions of relevance, once the circularity of trusting production's own output is identified and addressed (Section~\ref{subsec:pooled_gold_standard} onward). Table~\ref{tab:protocol_summary} previews all three so that later references to ``the silver standard'' or ``the production-independent set'' can be looked up in one place, even though the latter two are not defined in full methodological detail until the sections indicated.

\begin{table}[htbp]
\centering
\small
\caption{The three relevance protocols used across this thesis, in order of introduction.}
\label{tab:protocol_summary}
\setlength{\tabcolsep}{3pt}
\begin{tabular}{>{\raggedright\arraybackslash}p{2.5cm}>{\raggedright\arraybackslash}p{5.5cm}>{\raggedright\arraybackslash}p{5.5cm}}
\toprule
\textbf{Protocol} & \textbf{A company counts as relevant if\ldots} & \textbf{Used for} \\
\midrule
Pseudo-relevance & it appears in production's top-1{,}000 for that query & Introduced in Section~\ref{subsec:eval_protocol}.\newline Used for baseline comparison, reranking, hybrid fusion, and oracle-ceiling experiments (Phases~I--II). \\
Silver standard & two independent LLM judges agree (or a human resolves their disagreement) that it is relevant, drawn from a pooled, multi-source candidate set & Introduced in Section~\ref{subsec:pooled_gold_standard}.\newline Used for fusion-ranker retraining, tuning, fine-tuning, and all comparisons in Phase~III--IV. \\
Production-independent & the same silver-standard judging process is applied to a candidate pool drawn exclusively from companies production never ranked for that query & Introduced in Section~\ref{subsec:production_independent_eval}.\newline The one test in this thesis not subject to production-pooling bias by construction. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The silver standard should be read with caution, not treated as ground truth.} The silver standard is not a gold standard, and every result evaluated against it in this thesis should be read accordingly. It rests on LLM judgments with only minimal, and not fully independent, human checking. The author resolves judge disagreements (roughly 16\% of candidates pooled across every judging round, Figure~\ref{fig:judge_disagreement_rate}), and those same resolved labels then score the author's own retrieval methods, a conflict of interest that is disclosed and partially mitigated but not eliminated (Section~\ref{subsec:honest_synthesis} discusses this in full). The candidates it covers were also not drawn randomly. Active-learning sampling deliberately targets ambiguous, boundary-case candidates for labelling (Section~\ref{subsec:active_learning}), so the standard is systematically skewed toward harder cases rather than a representative sample of all relevant companies. For these reasons, the silver standard is best understood as a considerably more rigorous alternative to trusting production's own circular output, not as an infallible reference. Every Recall and NDCG figure computed against it in this thesis should be read as an estimate with these specific, disclosed sources of uncertainty, not as an exact measurement.

\begin{figure}[ht]
\centering
\includegraphics[width=0.85\textwidth]{figures/fig_judge_disagreement_rate.png}
\caption{Judge disagreement rate by labelling round. The dashed line marks the pooled rate across all 2{,}539 doubly-judged candidates.}
\label{fig:judge_disagreement_rate}
\end{figure}

Only the last two protocols are independent of production's own output; pseudo-relevance, used throughout Phases~I--II of the roadmap above, measures agreement with production rather than an externally verified notion of relevance, and results reported under it should be read with that limitation in mind (Section~\ref{subsec:eval_protocol} states this explicitly).

\section{Phase I: Baselines}
\label{sec:phase1_method}

\subsection{Dataset}
\label{subsec:dataset}

The evaluation corpus is derived from Istari's production search logs. A set of 101 representative queries was provided by Istari, covering a wide range of company search intents, including industry-specific queries (e.g.\ \textit{``pharmaceutical companies in the US''}), geographic queries (e.g.\ \textit{``tech startups in Berlin''}), and functional queries (e.g.\ \textit{``B2B SaaS companies''}). For each query, the production system returned its top-1\,000 ranked results, yielding 101\,000 query--company pairs in total. After de-duplication across queries, the corpus consists of \textbf{98\,716 unique companies}, each represented by a domain name, a company name, and an AI-generated natural-language summary of the company's business activity.

The summary field was identified as the richest retrieval signal after an exploratory data analysis of all available GOI attributes. It was found to be fully populated across the corpus (0\% missing values) and to contain the richest semantic content for matching against free-text queries, compared to structured fields such as industry codes or founding year, so it anchors the all-fields text representation used throughout this thesis. This field is not without its own data-quality issues, however. For a subset of query topics, the summary instead describes a website hosting or platform provider rather than the company's own business, an issue quantified directly in Section~\ref{sec:trust_filter_result}.

\subsection{Evaluation Protocol}
\label{subsec:eval_protocol}

\paragraph{Pseudo-relevance labels.} No human-annotated relevance judgements are available for the Istari company search task. Following standard practice in information retrieval research when ground-truth labels are absent~\citep{voorhees2002philosophy}, this thesis adopts a \textit{pseudo-relevance} assumption. A company is considered \textbf{relevant} for a given query if and only if it appears in the \textbf{top-1\,000 results} of Istari's production search system for that query. This yields a relevance set of 1\,000 companies per query, representing ${\sim}1\%$ of the total corpus. This thesis deliberately chooses this threshold over a stricter one so that reranking, fusion, and oracle-ceiling experiments later in this chapter have enough relevant companies per query to produce meaningful comparisons over large candidate pools.

This assumption has an important implication for the evaluation. The metrics measure the degree to which each retrieval system agrees with the production system, not whether it recovers objectively relevant companies. Consequently, a retrieval system that is strictly better than the production system may appear to underperform because it surfaces companies the production system missed. This limitation is acknowledged throughout the analysis.

\paragraph{Metrics.} Three standard information retrieval metrics are reported at cut-offs $k \in \{10, 50, 100, 1000\}$:

Writing $R_q$ for the set of relevant companies for query $q$ and $T_k(q)$ for the top-$k$ companies a system retrieves for $q$:

\begin{itemize}
\item \textbf{Precision@$k$}: the fraction of the top-$k$ retrieved companies that are relevant, $\text{Precision@}k = \frac{|T_k(q) \cap R_q|}{k}$. This measures user-facing quality, what a user sees on the first page of results.

\item \textbf{Recall@$k$}: the fraction of all relevant companies that appear in the top-$k$ results, $\text{Recall@}k = \frac{|T_k(q) \cap R_q|}{|R_q|}$. This measures coverage, important for downstream re-ranking pipelines that operate on a retrieved candidate set. \textbf{Recall is the primary metric in this thesis}, following the priority set by Istari for this task. Whether the system finds the relevant companies matters more than the exact order they are ranked in.

\item \textbf{NDCG@$k$} (Normalised Discounted Cumulative Gain): a ranking-aware metric that rewards systems for placing relevant companies higher in the list, using a logarithmic discount. It is reported as a secondary, supporting metric, reflecting the experience of a user who interacts primarily with the top results.
\end{itemize}

All metrics are averaged across all 101 queries. Precision and Recall are always reported at the same cut-off values so that the precision--recall trade-off at each depth is visible.

\paragraph{Reading Precision and Recall together.} Every one of the 101 queries has exactly 1\,000 relevant companies under the pseudo-relevance protocol, so at any cut-off $k \le 1\,000$ the two metrics are exactly proportional to one another, \[ \text{Recall@}k = \text{Precision@}k \times \frac{k}{1\,000}, \] and only coincide once $k = 1\,000$, where the denominators become equal. A practical consequence is that Recall@$k$ for small $k$ can look misleadingly low in isolation. A system with a strong Precision@10 of 0.97 still shows a Recall@10 of only $0.97 \times 10/1\,000 \approx 0.0097$, not because the top-10 results are poor, but because 10 items can never cover more than 1\% of a 1\,000-company relevant set. Precision@$k$ is therefore the more directly interpretable number at shallow cut-offs, while Recall@1\,000 is the headline metric throughout this thesis, the one cut-off at which Recall stops being a rescaled copy of Precision and becomes a genuinely independent measure of total coverage.

\label{subsec:quality}
\paragraph{Computational environment.}
\label{subsec:compute}

All experiments are conducted on bwUniCluster 3.0, a shared high-performance computing (HPC) cluster accessed through the University of Mannheim.\footnote{\url{https://wiki.bwhpc.de/e/BwUniCluster3.0}} Dense model encoding is performed on an NVIDIA A100 graphics processing unit (GPU) with 80\,GB HBM2 memory, on a node equipped with two Intel Xeon Platinum 8358 CPUs (64 physical cores total). BM25 retrieval runs on the central processing unit (CPU) instead, since it does not involve any neural network computation. Query latency measurements are performed on the same hardware and reported as the mean over 20 benchmark queries to obtain stable estimates. Latency is decomposed into three components (local query encoding, FAISS index search, and external network time) so that self-hosted and API-based methods are compared on equal terms.

\subsection{Sparse Retrieval Baseline: BM25}
\label{sec:baseline_sparse}

\textbf{BM25}, introduced in Section~\ref{sec:lit_sparse_dense}, is included here as the sparse-retrieval baseline against which every \emph{dense} (embedding-based, semantic) method in this chapter is compared.
\label{subsec:bm25_impl}

The BM25 baseline is implemented with the \texttt{rank\_bm25} Python library~\citep{robertson2009probabilistic}, chosen for this thesis specifically because it provides the Okapi BM25 variant with well-established default hyperparameters ($k_1 = 1.5$, $b = 0.75$), avoiding the need for a separate hyperparameter search for what is only the lexical baseline. All 98\,716 company summaries are tokenised using the punkt tokeniser from the Natural Language Toolkit (NLTK)~\citep{nltk} with lower-casing applied, and the resulting token lists are used to construct the BM25 inverted index. At query time, each query is tokenised identically and scored against the index. The top-1\,000 companies by BM25 score are retrieved for each query and stored for evaluation. Index construction takes 28.7 seconds and requires no GPU.

\subsection{Dense Retrieval Baselines}
\label{sec:baseline_dense}

\textbf{Dense retrieval} represents both the query and every candidate document as a fixed-length numerical vector (an \emph{embedding}) produced by a neural network, such that texts with similar meaning end up as nearby vectors in that space, regardless of whether they share any exact words. This directly targets BM25's weakness. Two texts can be semantically related while sharing almost no vocabulary. Three dense retrieval baselines are evaluated first, covering a range of model sizes, computational requirements, and deployment constraints. All three follow the same two-stage pipeline introduced by \citet{karpukhin2020dense}. First, all company summaries are encoded offline into a dense vector index using FAISS~\citep{johnson2017billion}. Second, at query time, the query is encoded into a vector and the top-1\,000 nearest neighbours are retrieved from the index. MiniLM, BGE, and OpenAI's model were chosen because they represent three deployment profiles a practitioner actually has to choose between. MiniLM is small and self-hosted, BGE is large and self-hosted, and OpenAI's model is a paid commercial API. Section~\ref{sec:extended_baselines} later broadens this comparison to eight further models to check whether a newer or larger model changes the conclusion.

\subsubsection{MiniLM (all-MiniLM-L6-v2)}
\label{subsec:minilm}

\texttt{all-MiniLM-L6-v2} is a lightweight sentence-transformer model~\citep{reimers2019sentence} with 22 million parameters, producing 384-dimensional embeddings. It is distilled from a larger BERT model and fine-tuned on sentence similarity tasks. All 98\,716 company summaries are encoded in a single batch pass, taking 0.6 minutes on the A100 GPU. A \texttt{faiss.IndexFlatL2} index (exact Euclidean search) is constructed over the resulting embeddings in 0.2 seconds. MiniLM does not require embedding normalisation.

MiniLM is included as the smallest and most computationally efficient dense baseline, providing a lower bound on dense retrieval quality and establishing whether a minimal dense model is sufficient to outperform BM25.

\subsubsection{BGE (bge-large-en-v1.5)}
\label{subsec:bge}

\begin{sloppypar}
\texttt{BAAI/bge-large-en-v1.5} is a 335-million parameter BERT-large model developed by the Beijing Academy of Artificial Intelligence~\citep{xiao2024cpack}, producing 1\,024-dimensional embeddings. It is specifically optimised for dense retrieval through hard negative mining during fine-tuning, a training strategy in which the model is exposed to query--document pairs that are superficially similar but semantically distinct, forcing it to learn fine-grained semantic distinctions.
\end{sloppypar}

Encoding all 98\,716 company summaries takes 7.0 minutes on the A100 GPU at a batch size of 256. BGE requires L2 normalisation of all embeddings (\texttt{normalize\_embeddings=True}), as it is trained under the cosine similarity objective. Accordingly, a \texttt{faiss.IndexFlatIP} index (inner product, equivalent to cosine similarity for unit-normalised vectors) is used rather than \texttt{IndexFlatL2}.

At query time, each query is encoded independently and normalised before being passed to the FAISS index; the resulting query latency is reported alongside every other method's in Section~\ref{sec:exp_runtime_result}.

\subsubsection{OpenAI (text-embedding-3-small)}
\label{subsec:openai}

\texttt{text-embedding-3-small} is a commercial embedding model provided by OpenAI via their REST API. It produces 1\,536-dimensional embeddings. Unlike the open-source models above, the architecture and training procedure are proprietary. Encoding all 98\,716 company summaries is performed via batched API calls (batch size 500) and takes 9.7 minutes, of which the majority is network latency rather than model computation. The resulting embeddings are unit-normalised by the API, and a \texttt{faiss.IndexFlatIP} index is constructed locally. The total cost of encoding all 98\,716 companies in this evaluation corpus is approximately \$0.15 at the published pricing of \$0.02 per million tokens.

This model is included as the commercial upper-bound baseline, testing whether a high-quality API-based embedding model outperforms the open-source alternatives evaluated above.

\section{Phase II: Improving Retrieval}
\label{sec:phase2_method}

\subsection{Extended Baseline Comparison: Additional Embedding Models}
\label{sec:extended_baselines}

Section~\ref{sec:baseline_dense}'s three dense baselines already span a size and cost spectrum, from MiniLM's 22 million parameters to OpenAI's paid commercial API, and found the much smaller MiniLM ahead of both BGE and OpenAI, which were themselves effectively tied with one another. This raises a direct follow-up question. Does retrieval quality keep improving as model size and specialisation increase, and if so, is that improvement worth the added compute cost? Five further dense embedding models were evaluated to test this, on the same 98\,716-company corpus, all-fields text representation, and 101-query evaluation set described in Section~\ref{subsec:dataset}, deliberately split into two tiers. The first tier stays close to BGE's scale (1\,024-dimensional models, comparable parameter count). It comprises BGE-M3 and GTE-large. The second tier jumps to 7-billion-parameter models, an order of magnitude larger, but holds the base architecture and parameter count fixed across all three (E5-Mistral-7B-Instruct, Linq-Embed-Mistral, SFR-Embedding-Mistral all build on the same Mistral-7B backbone), varying only their fine-tuning data and objective, so any quality difference among these three specifically isolates the effect of fine-tuning from the effect of raw scale, rather than confounding the two:

\begin{itemize}
\item \textbf{BGE-M3} (\texttt{BAAI/bge-m3}): a multi-lingual, multi-functionality embedding model, 1\,024-dimensional embeddings.
\item \textbf{GTE-large} (\texttt{thenlper/gte-large}): a general-purpose text embedding model, 1\,024 dimensions.
\item \textbf{E5-Mistral-7B-Instruct} (\texttt{intfloat/e5-mistral-7b-instruct}): an instruction-tuned 7-billion-parameter Mistral-based embedding model, 4\,096 dimensions.
\item \textbf{Linq-Embed-Mistral} (\texttt{Linq-AI-Research/Linq-Embed-Mistral}): a 7B-parameter Mistral-based embedding model fine-tuned for retrieval, 4\,096 dimensions.
\item \textbf{SFR-Embedding-Mistral} (\texttt{Salesforce/SFR-Embedding-Mistral}): Salesforce's 7B-parameter Mistral-based retrieval embedding model, 4\,096 dimensions.
\end{itemize}

\subsection{Query Expansion: HyDE Pipeline}
\label{sec:hyde}

\textbf{HyDE} (Hypothetical Document Embeddings) is a query-expansion technique. Instead of embedding the user's short query directly, an LLM first generates several hypothetical, ideal-answer documents for that query, and their embeddings are averaged into a single, richer query vector. This thesis implements HyDE by asking a local LLM (\texttt{llama3.1:8b} via Ollama) to generate $N=10$ stochastic (temperature~$=0.8$) hypothetical company descriptions that would ideally answer the query. The embeddings of these 10 descriptions are averaged into a single query vector, which is then searched against the MiniLM company index. A second stage reranks the top-50 HyDE results with a cross-encoder (\texttt{BAAI/bge-reranker-v2-m3}).

\subsection{Hybrid Retrieval: Reciprocal Rank Fusion}
\label{sec:rrf}

\textbf{Reciprocal Rank Fusion (RRF)} combines two or more separately produced rankings into one, by giving every document a score based only on its \emph{rank position} in each input ranking (not its raw similarity score, which is not directly comparable across different retrieval methods), then summing those rank-based scores across methods:
\[
\mathrm{RRF}(d) = \sum_{\text{method}} \frac{1}{k_{\mathrm{RRF}} + \mathrm{rank}(d)}
\]
As an alternative to query expansion, BM25 and MiniLM rankings are fused directly using RRF with $k_{\mathrm{RRF}}=60$ \citep{cormack2009reciprocal}. A second stage reranks the top-50 fused results with the same cross-encoder used for HyDE.

\subsection{ANN Index Scaling to 2 Million Vectors}
\label{sec:ann_scaling}

\textbf{Approximate Nearest Neighbour (ANN) search} trades a small amount of retrieval accuracy for a large speed-up, by searching an index structure that does not guarantee finding the exact closest vectors, unlike the exact brute-force search used everywhere else in this chapter so far. This section runs a first, synthetic-scale test of that trade-off. To test whether exact search (\texttt{IndexFlatL2}/\texttt{IndexFlatIP}) remains practical at production scale, the 98\,716 real MiniLM company embeddings were padded with 1\,901\,284 synthetic filler vectors (real embeddings perturbed with Gaussian noise at 15\% of the average vector norm), giving a 2-million-vector corpus. Exact search (\texttt{FlatL2}) was compared against two approximate indexes. IVF (\texttt{nlist}=4096, with \texttt{nprobe} $\in \{1, 8, 32, 128, 4096\}$) and HNSW ($M=32$, with \texttt{efSearch} $\in \{16, 64, 256\}$) were both tested.

\paragraph{Recall@1000 error analysis.}
\label{sec:error_analysis}

To understand why exact MiniLM search does not achieve perfect Recall@1000 (Section~\ref{sec:exp_results}), every production-relevant company that MiniLM failed to retrieve within the top-1\,000 was extracted and searched for at much greater depth ($k=10\,000$). The resulting misses are classified and quantified in Section~\ref{sec:error_analysis_result}.

\subsection{Corpus Quality: Platform Boilerplate in AI-Generated Summaries}
\label{sec:trust_filter}

The duplicated ``Bluehost.com'' description found in Section~\ref{sec:error_analysis_result} is one instance of a broader, previously unquantified data-quality issue affecting the \texttt{summary} field that every retrieval method in this thesis depends on. For a meaningful share of companies in the corpus, the AI-generated summary describes the website hosting provider or website-building platform that the company's domain happens to run on, rather than the company's own business. This happens because many small businesses have a thin, template-driven web presence, so the process that generates each company's summary from its web content sometimes picks up generic platform language (for example, marketing copy for a web hosting provider or an e-commerce storefront builder) instead of any information specific to the company itself. A company using such a platform is then, in effect, indistinguishable in its summary text from every other company using the same platform, the mechanism behind the description duplication already observed in Section~\ref{sec:error_analysis_result}.

\paragraph{Detection approach.} A classifier was built to flag boilerplate summaries automatically, using embedding similarity between each company's summary and a small, fixed reference set of around 300 known boilerplate templates, an approach chosen so that classifying any single company costs one embedding comparison against a fixed-size reference set, regardless of whether the underlying corpus is the 98\,716-company evaluation set used in this thesis or the full multi-million-company GOI.

\paragraph{Oracle union recall ceiling.}
\label{sec:oracle_union}

Before investing in a trained reranker or a fine-tuning pipeline, it is useful to know the best-case recall achievable by simply combining the candidate pools already produced by every method run so far, at zero additional encoding cost, since every channel's top-1\,000 results per query are already cached on disk. This mirrors how pool coverage grows as more systems contribute to a pooled test collection \citep{zobel1998reliable}, applied here to the retrieval channels themselves rather than to human relevance judging. This experiment computes, for every combination of the six available channels (MiniLM, BM25, BGE-all-fields, BGE-summary, OpenAI-large, Nomic), the recall of the \emph{union} of their top-1\,000 candidate sets against the top-1\,000 ground truth.

\subsection{SimLM: A Purpose-Built Retrieval Pre-training Baseline}
\label{sec:simlm}

All dense baselines evaluated so far are general-purpose sentence-embedding models. \textbf{SimLM} \citep{wang2022simlm} takes a different approach. It is a BERT-base (110\,M parameter) encoder pre-trained specifically for the retrieval task itself, matching a short query to a longer passage via a representation-bottleneck objective, before any task-specific fine-tuning. It is included to test whether a model purpose-built for retrieval, but far smaller than the 7B-parameter models in Section~\ref{sec:extended_baselines}, can match or exceed them.

\paragraph{Implementation.} The public checkpoint \texttt{intfloat/simlm-base-msmarco-finetuned} is used, following its documented convention rather than the all-fields concatenation used elsewhere in this thesis. The company name is encoded as a \emph{title}, and the remaining fields (country, state, city, type, size, industry, summary, keywords) as the paired \emph{body} text, using CLS-token pooling (not mean pooling) with L2 normalisation. Queries are encoded without any prefix, following the same model card.

\subsection{ColBERT: Late-Interaction Retrieval via Brute-Force MaxSim}
\label{sec:colbert}

Every method evaluated so far represents each company and each query as a single dense vector. \textbf{ColBERT} \citep{khattab2020colbert, santhanam2022colbertv2} instead represents both as \emph{sets of per-token} embeddings and scores a (query, company) pair using \emph{MaxSim}. For every query token, the maximum similarity to any company token is taken, and these per-token maxima are summed. This \emph{late-interaction} design is architecturally richer than single-vector dense retrieval, since no single fixed-size vector has to compress the entire meaning of a company description, and it is included to test whether that added expressiveness translates into higher recall on this task.

\paragraph{Implementation.} The public checkpoint \texttt{colbert-ir/colbertv2.0} is used, encoding each company summary into a sequence of 128-dimensional per-token embeddings via the model's linear projection layer. Due to indexing and library constraints on the cluster used for this thesis, the official PLAID-indexed ColBERT pipeline was not used; instead, MaxSim scoring is implemented from scratch as a brute-force computation over all 98\,716 companies for every query. This is computationally more expensive than an indexed implementation would be, but is exact rather than approximate, and is tractable at this corpus size.

\subsection{Hybrid Full-Pool Reranking}
\label{sec:hybrid_fullpool}

Section~\ref{sec:rrf} showed that fusing BM25 and MiniLM with Reciprocal Rank Fusion, followed by reranking only the top-50 fused candidates, essentially reproduces MiniLM's own quality (NDCG@10~=~0.975) without improving Recall@1000. Section~\ref{sec:oracle_union} then showed that the \emph{union} of several channels' top-1\,000 candidate pools reaches a much higher oracle recall ceiling than any single channel. Together, these two findings motivate a different hybrid design. Rerank the \emph{entire} union pool of several dense channels with a cross-encoder, rather than fusing rankings and reranking only a shallow, fixed-size slice of the result.

\paragraph{Implementation.} For each query, the top-1\,000 results from a set of dense channels are combined by simple set union (no rank fusion, no truncation), giving an unranked candidate pool that is typically 1\,300--1\,800 companies. A cross-encoder reranker, \texttt{BAAI/bge-reranker-v2-m3} \citep{baaireranker2024}, then scores every (query, candidate) pair in the full pool directly, and the top-1\,000 by reranker score are kept as the final result. Four variants are compared, adding one channel at a time:

\begin{itemize}
\item \textbf{2-way}: MiniLM~+~Linq-Embed-Mistral.
\item \textbf{3-way}: 2-way~+~GTE-large.
\item \textbf{4-way}: 3-way~+~OpenAI-large.
\end{itemize}

\paragraph{Reranker ablation: isolating the bottleneck.}
\label{sec:reranker_ablation}

Given the channel-saturation finding in Section~\ref{sec:hybrid_fullpool}, the natural next question is whether the specific reranker model used throughout this chapter, \texttt{BAAI/bge-reranker-v2-m3}, is itself limiting recall, or whether reranker choice matters little once the candidate pool is fixed.

\paragraph{Implementation.} \texttt{BAAI/bge-reranker-v2-m3} (built on an XLM-RoBERTa backbone) is replaced with \texttt{mixedbread-ai/mxbai-rerank-large-v1} \citep{mixedbread2024mxbai} (built on a DeBERTa-v2 backbone, an architecturally distinct, comparably-sized cross-encoder), applied to the \emph{exact same} 3-way union pool from Section~\ref{sec:hybrid_fullpool}. Only the reranker model changes; the candidate pool, corpus, and queries are identical.

\subsection{Learned Fusion Ranker}
\label{sec:learned_fusion}

Every hybrid pipeline so far combines \emph{frozen}, independently pretrained components. Candidate lists are pooled by simple union, and a pretrained cross-encoder, never fit to this task's own queries or labels, reranks the result. This section instead treats fusion as a supervised \textbf{learning-to-rank} problem. A model is trained directly on this thesis's own 101 queries and their pseudo-relevance labels to learn how to combine multiple retrieval signals, rather than relying on a fixed, hand-designed combination rule.

\paragraph{Formal setup.} For each (query, candidate) pair, the model receives a ten-dimensional input feature vector $x \in \mathbb{R}^{10}$ (the similarity scores, inverse ranks, reranker score, and channel count described below) and predicts a single output $\hat{y} \in [0, 1]$, the estimated probability that the candidate is relevant to the query. The training target $y \in \{0, 1\}$ is the pseudo-relevance label from Section~\ref{subsec:eval_protocol} (1 if the candidate is in production's top-1\,000 for that query, 0 otherwise). Both model variants are trained to minimise binary cross-entropy (log loss), $\mathcal{L}(y, \hat{y}) = -\left[y \log \hat{y} + (1 - y) \log (1 - \hat{y})\right]$, averaged over all training (query, candidate) pairs, the standard objective for a probabilistic binary classifier and the default loss for both \texttt{scikit-learn} estimators used here.

\paragraph{Implementation.} For every (query, candidate) pair in the same 3-way union pool used in Section~\ref{sec:hybrid_fullpool} (with BM25 added as a fourth, lexical, signal), ten features are computed: the raw similarity score and inverse rank ($1/\text{rank}$) from each of MiniLM, Linq-Embed-Mistral, GTE-large, and BM25; the \texttt{BAAI/bge-reranker-v2-m3} score from Section~\ref{sec:hybrid_fullpool} (included as one input feature among ten, not as the final decision rule); and a count of how many channels retrieved the candidate at all. Two models are trained on these features using \texttt{scikit-learn} \citep{pedregosa2011scikit}, a logistic regression and a histogram-based gradient-boosted decision tree ensemble (GBDT). Crucially, both are trained and evaluated using 5-fold cross-validation grouped by \texttt{query\_id}. For each fold, the model is fit on candidates from 80\% of queries and scores held-out candidates from the remaining 20\%, so that no query is ever scored by a model that has seen its own label during training. Final metrics are computed by concatenating the held-out predictions from all five folds, giving a complete, leakage-free evaluation over all 101 queries.

\section{Phase III: Evaluation Validity}
\label{sec:phase3_method}

The pseudo-relevance protocol used throughout this thesis (Section~\ref{subsec:eval_protocol}) treats a company as relevant if and only if it appears in production's own results for a query. This is a reasonable practice when no human-annotated relevance judgements exist (Section~\ref{sec:lit_evaluation}), but it carries a specific risk for the learned fusion ranker (Section~\ref{sec:learned_fusion}): a model trained directly on labels derived from production's own output may partly be learning to reproduce whatever idiosyncrasies that particular system has, rather than genuine relevance. This section reports a small-scale attempt to check whether the learned fusion ranker's key interpretive finding, that MiniLM's inverse rank is the most informative feature, survives replacing that circular ground truth with an independently constructed one.

\subsection{Building a Pooled, Independently Judged Silver Standard}
\label{subsec:pooled_gold_standard}

This section builds this thesis's \emph{silver standard}\footnote{This thesis calls the pooled, multi-judge, human-tie-broken labels built in this section a \emph{silver standard} rather than a \emph{gold standard}, since LLM judges with only minimal human checking do not meet the bar traditional IR evaluation reserves for that term. The larger, classifier-distilled set built from it in Section~\ref{sec:silver_labels} is correspondingly renamed from \emph{silver} to \emph{bronze}, one trust tier below.}, a pooled, multi-judge, human-adjudicated relevance standard used throughout the remainder of this thesis.

\paragraph{Pooling.} Following the pooling methodology established for the Text REtrieval Conference \citep{voorhees2002philosophy, zobel1998reliable}, the top-20 candidates per query were taken from six sources. These were the frozen production ground truth, a freshly fetched sample from Istari's live v2 search API, BM25, GTE-large, ColBERT, and the learned fusion ranker's own output, then unioned and deduplicated by domain. Applied across all 101 queries, this yields 8\,309 pooled candidates, an average of 82.3 unique candidates per query, a substantially smaller and more tractable set than the original 1\,000-per-query pool.

\paragraph{Trust filtering.} Before any relevance judging, every pooled candidate's summary was checked against the boilerplate detector from Section~\ref{sec:trust_filter}, reusing its already-cached reference templates. This flagged 487 of 8\,309 candidates (5.9\%) as untrustworthy, which were excluded from judging entirely, so that a summary describing the wrong entity could not poison a relevance label built on top of it.

\paragraph{Multi-judge relevance labelling.} Each remaining, trustworthy candidate was independently judged by two large language models (OpenAI's \texttt{gpt-4o-mini} and Anthropic's Claude Haiku), on a three-level scale (0~=~not relevant, 1~=~partially relevant, 2~=~highly relevant), following the LLM-as-judge approach validated at scale by \citet{thomas2024llmjudge}. These smaller, cheaper models were chosen deliberately for this stage. The initial pilot and active-learning expansion (Section~\ref{subsec:active_learning}) required judging over a thousand candidates, and a lower per-candidate cost made judging at that volume affordable at all. This choice is validated rather than assumed. The blind calibration check below and a direct kappa comparison against a stronger judge pair (Section~\ref{subsec:pooled_gold_standard_result}) confirm the cheaper pair reaches ``substantial'' rather than ``almost perfect'' agreement, a real but bounded gap that motivated upgrading to stronger judges (\texttt{claude-sonnet-5}, \texttt{gpt-5.4}, and Google's Gemini) for the later spot-checks and the production-independent evaluation (Sections~\ref{sec:silver_labels_result}, \ref{subsec:production_independent_eval}). Where the two judges agreed, that label was accepted directly. Where they disagreed, the case was resolved by manual review, performed by the author. The resulting standard is therefore independent of the production system, but not fully independent of the researcher, a limitation discussed in full in Section~\ref{subsec:honest_synthesis}.

\paragraph{Blind calibration of the judges.} Resolving disagreements by reading both judges' stated reasoning risks anchoring the human reviewer toward whichever judge's framing is read first. To check this, a separate blind sample of 20 candidates that neither judge had yet seen was labelled by the author with no access to either judge's output, then scored independently by both judges. OpenAI matched the blind label 75\% of the time and Claude 80\%, close enough that neither judge should be treated as categorically more reliable than the other on this task.

\paragraph{Scope of this pilot.} Labelling all 101 queries with two paid LLM judges directly was not attempted, given the per-candidate cost of LLM judging (quantified in full in Section~\ref{sec:silver_labels}). A 5-query pilot was completed first instead, to validate the labelling approach at a small, affordable scale before committing to a larger expansion. Of 430 candidates judged, 360 (83.7\%) received unanimous agreement between the two judges. Of the remaining 70 disagreements, 59 were resolved by manual review and 11 were excluded outright for insufficient evidence (for example, unreachable domains or placeholder websites with no verifiable content), rather than being forced into a label the available information could not support. This yields \textbf{419 silver-labelled candidates} across 5 queries, distributed as 8 not relevant, 119 partially relevant, and 292 highly relevant.

\subsection{Retraining Against the New Silver Standard}
\label{subsec:refit_fusion_ranker}

The exact architecture from Section~\ref{sec:learned_fusion} (the same ten features, the same 5-fold cross-validation grouped by \texttt{query\_id}, here equivalent to leave-one-query-out since exactly five queries are covered) was retrained using the 417 of 419 silver-labelled candidates that matched an existing feature row from the original candidate pool. The binary classification target used the strict threshold \texttt{gold\_label} = 2 (highly relevant only, 291 positive vs.\ 126 negative examples); the more permissive threshold \texttt{gold\_label} $\geq$ 1 leaves only 8 negative examples across the whole pilot, too few for cross-validated coefficients to be estimated reliably. Ranking quality is reported as NDCG@5/10/20 rather than the deeper cutoffs used elsewhere in this thesis, since this pilot's candidate pool is only around 80 silver-labelled candidates per query, an order of magnitude smaller than the top-1{,}000 pools evaluated in earlier sections, so a cutoff of 1{,}000 would simply score the entire pool rather than isolate top-of-list quality.

\label{sec:silver_labels}

No gold-standard relevance data exists for this task at any scale to build on. Istari's full 20-million-company database has never been exhaustively human-labelled by anyone, so this thesis treats labelling as an incremental process rather than a one-time effort. A small, independently judged set is verified first (Section~\ref{subsec:pooled_gold_standard}), and how much further it is affordable and worthwhile to extend then becomes a concrete decision rather than an assumption that the answer is either ``all of it'' or ``none of it''. Extending independent LLM judging to every candidate the retrieval systems in this thesis actually produce is not affordable. This estimate is not a guess. It scales the per-candidate API cost actually incurred while judging the 419-candidate pilot (Section~\ref{subsec:pooled_gold_standard}) up to larger volumes at different pooling depths. The original fusion ranker's own candidate pool (Section~\ref{sec:learned_fusion}) covers 173\,262 (query, candidate) pairs across all 101 queries. Judging every one of those at the pilot's observed per-candidate cost would cost approximately \$22\,000, rising toward \$28\,000 at a deeper pooling depth approaching exhaustive judging of every corpus company against every query, and the majority of those pairs are companies no retrieval channel ever considered plausible, judging them would mean paying to confirm the obvious.

\paragraph{Approach.} Instead, the 419 silver-labelled candidates from Section~\ref{subsec:pooled_gold_standard} are used to train a final classifier (the same architecture and feature set as Section~\ref{sec:learned_fusion}, fit on all 419 labels rather than held out via cross-validation, since this model is intended for deployment rather than evaluation). This classifier is then applied to all 173\,262 existing candidate rows, which already have the required ten features computed, at no additional retrieval or labelling cost. This produces two explicitly distinct label tiers, following standard practice in weak and distant supervision. These are a small \textbf{silver} tier (419 candidates, LLM-judged and human-adjudicated) and a much larger \textbf{bronze} tier (the remaining candidates, model-predicted rather than independently judged). The two tiers are not merged or treated as equally reliable anywhere in this pipeline.

\paragraph{Validating the bronze tier.} To estimate how trustworthy the bronze labels actually are, a random sample of 150 bronze-labelled candidates is independently judged by two more capable models, \texttt{claude-sonnet-5} and \texttt{gpt-5.4} (an upgrade from the \texttt{claude-haiku-4-5}/\texttt{gpt-4o-mini} pair used for the original silver standard, chosen after confirming via the blind calibration check in Section~\ref{subsec:pooled_gold_standard} that the cheaper pair was not itself the limiting factor). Only candidates where both stronger judges unanimously agree are counted, disagreements are excluded from the spot-check rather than guessed at, the same principle used throughout Section~\ref{subsec:pooled_gold_standard}. This validation costs under \$1, compared to the \$22\,000+ a full re-judging pass would require.

\subsection{Expanding the Silver Standard via Active Learning (AL)}
\label{subsec:active_learning}

Rather than judging a random or exhaustive sample of the remaining 96 uncovered queries, the silver standard is expanded using \emph{uncertainty sampling}. The distillation classifier's own predicted probability is used to find, for every one of the 101 queries, the ten candidates it is least confident about (predicted probability closest to 0.5), and only those are sent for judging. This targets exactly the candidates most likely to teach the classifier something it does not already know, rather than spending judging budget on cases it already calls correctly. The judging prompt is enriched at the same time with company metadata that was previously collected but never shown to the judges. This includes \texttt{organization\_type}, \texttt{organization\_size}, \texttt{state}, \texttt{district}, \texttt{municipality}, and \texttt{nace\_code}, alongside the original name, country, and summary fields, giving the LLM judges more grounding for cases a summary alone leaves ambiguous. This active-learning approach is well established in the machine learning literature \citep{settles2009active}, and combining LLM-based and human-provided relevance judgments to build a test collection under a limited labelling budget mirrors the approach of \citet{takehi2024llmassisted}, who propose a similar strategy for balancing manual and LLM annotation effort in IR test collection construction. Uncertainty sampling specifically for building IR test collections, rather than for training a downstream classifier, has direct precedent in \citet{rahman2020efficient}, who show that selecting documents for human judgement by a system's own uncertainty reduces the labelling budget needed to reach a reliable test collection, the same logic applied here to selecting candidates for LLM judgement instead. This is not a universally settled question, however. \citet{althammer2023annotating} find that, for the specific task of selecting training data to fine-tune a neural ranker, active learning strategies did not outperform random selection once labelling cost was accounted for. This thesis's use case differs in an important respect that plausibly explains why uncertainty sampling still pays off here. The goal in Section~\ref{subsec:active_learning} is not to train the best possible ranker directly, but to close a measured, diagnosed coverage gap in the underlying silver standard (Section~\ref{sec:silver_labels_result}), where deliberately targeting the classifier's most uncertain, least-covered queries is exactly the failure mode identified. \citet{xia2025fromselection}'s recent survey of LLM-based active learning situates this thesis's approach within a fast-moving area of the literature, where using an LLM as the labeller within an uncertainty-sampling loop, rather than only as the model being trained, is an increasingly common pattern that this thesis's own active-learning pass follows.

\begin{figure}[!htb]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  every node/.style={font=\footnotesize},
  box/.style={rectangle, rounded corners, draw, align=center, text width=2.4cm, minimum height=1.3cm, inner sep=3pt},
  data/.style={box, draw=blue!55!black, fill=blue!10},
  judge/.style={box, draw=green!45!black, fill=green!10},
  human/.style={box, draw=orange!70!black, fill=orange!12},
  gold/.style={box, draw=yellow!55!black, fill=yellow!20},
  silver/.style={box, draw=gray!55!black, fill=gray!12},
  diag/.style={box, draw=red!60!black, fill=red!10},
  decision/.style={diamond, draw=orange!70!black, fill=yellow!15, aspect=2.2, align=center, inner sep=1pt, font=\footnotesize},
  arr/.style={-{Stealth[length=2mm]}, thick},
]

% row 1: pooling and silver-standard construction
\node[data]     (pool)    at (0,0)      {Pooling\\(6 sources)\\ $\sim$8{,}309 candidates\\101 queries};
\node[data]     (filter)  at (2.9,0)    {Trust filter\\(boilerplate check)};
\node[judge]    (judge1)  at (5.8,0)    {Multi-judge LLM ensemble\\\texttt{\footnotesize gpt-4o-mini}\\+ Claude Haiku\\(5-query pilot)};
\node[decision] (agree)   at (8.9,0)    {Agree?};
\node[gold]     (gold1)   at (12.0,0)   {Silver standard\\5 queries\\419 candidates};
\node[human]    (tiebreak) at (8.9,-2.3) {Human tie-break\\(blind calibration verified)};

\draw[arr] (pool.east) -- (filter.west);
\draw[arr] (filter.east) -- (judge1.west);
\draw[arr] (judge1.east) -- (agree.west);
\draw[arr] (agree.east) -- node[above]{agree} (gold1.west);
\draw[arr] (agree.south) -- node[left]{disagree} (tiebreak.north);
\draw[arr] (tiebreak.east) -| (gold1.south);

% row 2: distillation and spot-check
\node[data]   (classifier) at (0,-4.9)    {Distillation classifier\\LogReg + GBDT\\10 retrieval features};
\node[silver] (bronze1)    at (3.2,-4.9)  {Bronze labels\\full corpus\\101 queries\\(no extra cost)};
\node[judge]  (spot1)      at (6.4,-4.9)  {Spot-check vs.\ stronger judges\\\texttt{\footnotesize gpt-5.4} + Sonnet 5\\(150 candidates)};
\node[diag]   (diag1)      at (9.6,-4.9)  {Diagnostic\\spot-check};

\draw[arr] (gold1.south) -- (12.0,-3.5) -- (0,-3.5) -- (classifier.north);
\draw[arr] (classifier.east) -- (bronze1.west);
\draw[arr] (bronze1.east) -- (spot1.west);
\draw[arr] (spot1.east) -- (diag1.west);

% row 3: active learning and refresh
\node[human]  (al)        at (0,-9.3)     {Active learning:\\uncertainty sampling\\(top-10/query, 101 queries, enriched prompt)};
\node[gold]   (gold2)     at (3.2,-9.3)   {Expanded silver\\standard\\101 queries};
\node[data]   (retrain)   at (6.4,-9.3)   {Retrain distillation classifier};
\node[silver] (bronze2)   at (9.6,-9.3)   {Bronze labels v2\\full corpus};
\node[diag]   (spot2)     at (12.4,-9.3)  {Fresh\\spot-check};

\draw[arr] (diag1.south) -- (9.6,-7.3) -- node[above]{diagnosed cause} (0,-7.3) -- (al.north);
\draw[arr] (al.east) -- (gold2.west);
\draw[arr] (gold2.east) -- (retrain.west);
\draw[arr] (retrain.east) -- (bronze2.west);
\draw[arr] (bronze2.east) -- (spot2.west);

\end{tikzpicture}%
}
\caption{The silver and bronze standard construction pipeline, from pooling through active-learning expansion.}
\label{fig:gold_silver_pipeline}
\end{figure}

Figure~\ref{fig:gold_silver_pipeline} summarises the full pipeline described across this section.

\subsection{Deepening a Headline Query Subset via Round-Based AL}
\label{subsec:headline_deepening}

The shallow, roughly uniform depth left by Section~\ref{subsec:active_learning}'s expansion (10-14 silver-labelled candidates per query) fixes out-of-distribution generalisation but is too thin to report reliable per-query Recall@$k$/NDCG@$k$ as an evaluation benchmark, deeper judged pools reduce the variance of such metrics (Section~\ref{sec:scaling_corpus} discusses this further). Rather than deepening all 101 queries, this thesis selects a set of 14 queries for methodological diversity beyond simple topic variety, covering geographic coverage not tested by the original pilot, the BM25 vocabulary-mismatch failure mode (``venture capital firms''), adjacent-category disambiguation (e.g.\ pharmaceutical/biotech, investment/wealth management), acronym-heavy queries, and niche or emerging industries.

Unlike the one-shot batch in Section~\ref{subsec:active_learning}, this used genuinely iterative active learning. Each round labelled a batch, retrained the classifier on everything labelled so far, re-ranked the \emph{remaining} candidates with the \emph{updated} classifier, and repeated, since uncertainty estimates go stale as soon as new labels are added. A small fixed validation set (100 candidates, judged once with the stronger judge pair and reused for free across every round) tracked whether each additional round was still worth its cost.

\paragraph{Refreshing the bronze tier at no additional cost.} The combined 2{,}269-candidate silver standard (1{,}429 from Section~\ref{subsec:active_learning_result} plus 840 from this section) was used to retrain the distillation classifier from Section~\ref{sec:silver_labels} once more (2{,}267 of the 2{,}269 matched an existing feature row, the same small gap noted in Section~\ref{subsec:refit_fusion_ranker}), and applied to the existing candidate pool to produce up to 1{,}000 bronze-labelled candidates per query, capped rather than kept at the full pool size, and with every silver-verified candidate guaranteed a place regardless of its predicted probability rank. This required no new retrieval, encoding, or judging. Every one of the 101 queries already has at least 1{,}022 candidates with retrieval features computed (median 1{,}661), so the only new work is classifier inference. All 101 queries now have exactly 1{,}000 candidates each (101{,}000 rows total, 2{,}267 of them silver-verified), the deepest per-query coverage produced so far in this thesis, though still bronze-tier and bounded by the classifier's demonstrated accuracy (Section~\ref{subsec:headline_deepening_result}) rather than independently verified.

\paragraph{Quantifying how far production's own ranking diverges from the independent silver/bronze set.}
\label{subsec:production_divergence}

\texttt{dataset/production\_results.xlsx}, production's own top-1{,}000-per-query ranking, has exactly the same shape as the refreshed bronze set above (101{,}000 rows, 1{,}000 per query, identical \texttt{query\_id}/\texttt{domain} keys), making a direct, row-level comparison possible with no new judging or encoding cost. Four checks quantify what has, until this point, mostly been argued qualitatively in this thesis. They measure how much production's own output actually diverges from an independently verified standard.

\paragraph{A direct ranking comparison: production vs. an independently cross-validated ranker.}
\label{subsec:goi_vs_ours_ranking}

Section~\ref{subsec:production_divergence} shows production's ranking \emph{differs} from the independent silver/bronze set; this section asks whether it is actually \emph{worse} at finding truly relevant companies, a stronger and more specific claim. Production's own top-1{,}000-per-query ranking is compared against a ranking produced by the fusion-ranker classifier trained with leave-one-query-out cross-validation (the same principle as Section~\ref{subsec:refit_fusion_ranker}, extended from 5 to all 101 queries: for every query, the classifier is trained on every \emph{other} query's silver labels only, so no ranking is ever produced by a model that saw that query's own labels). All 101 folds train in under 15 seconds, so this adds no meaningful cost. Both rankings are scored against the same silver-verified relevant set (\texttt{gold\_label} $=2$) using Recall@$k$/Precision@$k$/NDCG@$k$ at $k \in \{10, 50, 100, 300, 500, 1000\}$.

\subsection[Effect of Judgment Coverage on the Production Comparison]{Does Judgment Coverage, Not Just Ranking Quality, Explain the Production Comparison?}
\label{subsec:label_coverage_bpref}

The explanation offered above, that production's advantage in Section~\ref{subsec:goi_vs_ours_ranking} is a candidate-pool artifact rather than a genuine quality gap, was argued qualitatively. This section makes it concrete and measurable instead, following a specific critique of the evaluation methodology. Every precision, recall, and NDCG figure reported in this thesis implicitly treats an unjudged candidate as irrelevant, which punishes a system exactly when it retrieves something good the pooling process happened to miss. If different systems' top-$k$ results carry very different fractions of already-judged candidates, their scores are not measuring the same thing, and are not directly comparable without knowing that fraction.

\paragraph{Method.} For six systems, production's own ranking; the leave-one-query-out cross-validated fusion ranker from Section~\ref{subsec:goi_vs_ours_ranking}; and each of the four individual retrieval channels' own top-1{,}000 ranking within the same candidate pool (BM25, MiniLM, GTE-large, Linq-Embed-Mistral), label coverage is computed as the fraction of a query's top-$k$ results that carry any silver label at all, relevant or not, independently of whether those labels indicate relevance. \texttt{bpref} \citep{buckley2004retrieval}, a metric introduced at TREC specifically for evaluation under incomplete judgments, is computed alongside the usual Recall@1{,}000. Rather than treating an unjudged document as confirmed non-relevant, bpref only counts judged non-relevant documents when penalising a relevant document's rank position, so a system is never docked for retrieving something genuinely unjudged rather than genuinely irrelevant.

\subsection[A Production-Independent Test: Companies Production Never Saw]{A Production-Independent Test: Can This Pipeline Find Companies Production Never Saw?}
\label{subsec:production_independent_eval}

Section~\ref{subsec:goi_vs_ours_ranking}'s comparison could not yet support the claim that this thesis's ranking outperforms production, because the silver standard it was scored against was partly pooled from production's own output. This section closes that gap directly, using the random, production-independent 300{,}000-company sample introduced in Section~\ref{sec:scaling_corpus} (\texttt{source == ``new''} in the combined pool, as opposed to \texttt{``existing''} for the original, potentially production-entangled corpus).

\paragraph{Method.} For the same 19 deep-coverage queries used throughout this thesis's rigorous comparisons (a leakage-free held-out set never used for hyperparameter or model selection, Section~\ref{subsec:hyperparameter_tuning}), candidates are pooled from four independent retrieval channels (BM25, MiniLM, GTE-large, Linq-Embed-Mistral), searched exclusively over \texttt{source == ``new''} companies so the pool is never diluted by the already-curated existing corpus, then trust-filtered the same way as the original silver standard (Section~\ref{subsec:pooled_gold_standard}). Every candidate in this pool is, by construction, a company production was never asked to rank for any of these queries, so production's own output cannot have influenced which candidates end up here, this removes the candidate-pool bias identified in Section~\ref{subsec:goi_vs_ours_ranking_result} by design, not by argument. Trustworthy candidates are then judged by two strong LLM judges from two different labs, Claude Sonnet 5 and Google's Gemini 3.6 Flash, chosen instead of the originally planned GPT-5.4 pairing specifically because this thesis's own OpenAI baseline (\texttt{text-embedding-3-small}, Section~\ref{subsec:openai}) is itself under evaluation, so an OpenAI judge risks the same-family bias this thesis otherwise avoids by construction. Disagreements are resolved by hand, the same tie-breaking principle used for every other silver-standard pass in this thesis.

Because production has no ranking data at all for \texttt{source == ``new''} companies (\texttt{production\_results.xlsx} only ever covers the original corpus), a symmetric production-vs-ours ranking comparison is not even defined here. This section runs a different, meaningful test instead. Do the resulting silver-verified relevant companies, independently and blindly confirmed, get found and ranked well by this thesis's own retrieval pipeline, searching the full, realistic corpus across all four scaling tiers (Section~\ref{sec:scaling_corpus})? This directly tests the coverage-gain claim from Section~\ref{subsec:production_divergence} on a candidate pool that is clean of circularity by construction.

The resulting judgments, and how they were used to build the confirmed-relevant set this test is scored against, are reported in Section~\ref{subsec:production_independent_eval_result}.

\paragraph{Is the classifier's confidence honest? A calibration check.}
\label{subsec:calibration_check}

The accuracy figures reported throughout Sections~\ref{sec:silver_labels_result}--\ref{subsec:headline_deepening_result} measure whether the classifier's predicted label (relevant/not, at the 0.5 cutoff) is correct. A separate question is whether its predicted \emph{probability} is honest, when it reports 90\% confidence, is it right about 90\% of the time. This matters because the overwhelming majority of the bronze tier was never individually spot-checked; trusting the classifier's confidence at face value depends on it being well calibrated, not just accurate at one threshold. Using the same leave-one-query-out cross-validated predictions as Section~\ref{subsec:goi_vs_ours_ranking}, predicted probabilities for all 2{,}267 silver-labelled candidates were binned into deciles and compared against the actual relevant rate within each bin.

\subsection{Silver-Standard Coverage Across the Ranking}
\label{subsec:external_review_prep}

Before sending ranked candidate lists to an external reviewer at ISTARI.AI for a relevance-label review, it is worth checking where the silver labels already collected sit within each query's own ranked output, since that determines whether the top of the ranking can already be called trustworthy or whether it is still, in effect, unverified.

\paragraph{Two representative queries.} Query 1 (``software companies'') and query 92 (``wealth management firms'') were chosen to send out, the former for its comparatively large existing label count, the latter for having the largest coverage gap identified in Section~\ref{subsec:production_divergence_result}.

\paragraph{Method.} For each query, the classifier-ranked top-1{,}000 candidates from Section~\ref{subsec:refit_fusion_ranker}'s bronze-label pipeline were split into five rank bands (1--100, 101--300, 301--500, 501--700, 701--1{,}000), and every candidate already carrying a verified silver label was located within its band.

\paragraph{Implication for the external review.} A curated 100-candidate export per query was prepared for the external review, drawing 20 candidates from each of the same five rank bands, prioritising candidates absent from production's own top-1{,}000 for that query, to test directly whether the pipeline surfaces genuinely relevant companies production misses. Already-labelled candidates remain at their natural position, unflagged, so agreement with this thesis's own silver labels on those rows was intended as a free, blind inter-rater consistency check. The reviewer was given written guidelines matching the standard already used for LLM judging (Section~\ref{subsec:pooled_gold_standard}), also folded directly into the judge prompt from that point forward so that human and model judgments apply the same criteria.

\section{Phase IV: Learned Retrieval Revisited}
\label{sec:phase4_method}

\subsection{Hyperparameter Tuning the Fusion Ranker}
\label{subsec:hyperparameter_tuning}

Every fusion ranker result reported so far, the original model (Section~\ref{sec:learned_fusion}), the pooled-silver-standard retraining (Section~\ref{subsec:refit_fusion_ranker}), and the cross-validated ranking comparison against production (Section~\ref{subsec:goi_vs_ours_ranking}), uses the same hand-picked \texttt{HistGradientBoostingClassifier} configuration (\texttt{max\_iter=150}, \texttt{max\_depth=4}, \texttt{class\_weight="balanced"}), chosen once early on and never systematically tuned against the silver standard. This section addresses that gap with a randomized hyperparameter search.

\paragraph{Method.} The search space covers \texttt{max\_depth} (2 to 8, or unlimited), \texttt{max\_iter} (50 to 300), \texttt{learning\_rate} (0.03 to 0.3), \texttt{min\_samples\_leaf} (5 to 30), and \texttt{l2\_regularization} (0.0 to 2.0); \texttt{class\_weight="balanced"} is kept fixed throughout, since it is an established, justified design choice rather than a model-capacity hyperparameter. 40 combinations are sampled at random (fixed seed for reproducibility).

\paragraph{Avoiding leakage in model selection.} Selecting the best combination using the same cross-validated metric later reported as the final result would bias that result upward (the ``winner's curse'' in model selection). A combination is not a fair, unbiased estimate of unseen-data performance if it was chosen \emph{because} it scored best on exactly the data used to report it. To avoid this, the search itself is restricted to the 82 queries without deep silver coverage, evaluated with \texttt{LeaveOneGroupOut} cross-validation grouped by \texttt{query\_id} within that 82-query pool only, scored by mean Recall@1{,}000 and NDCG@1{,}000. The 19 deep-coverage queries (5-query pilot plus 14 headline queries) are never touched during the search. Once the best combination is fixed, it is evaluated fresh with a full \texttt{LeaveOneGroupOut} pass over all 101 queries, the same protocol used for the untuned baseline (Section~\ref{subsec:goi_vs_ours_ranking}). The 19-deep-query subset of that final evaluation is a genuinely leakage-free comparison against the untuned baseline, since those queries never influenced which hyperparameters were chosen; the all-101-query subset is reported alongside it for comparability with earlier sections, but should be read as directional rather than strictly unbiased, since the other 82 queries did inform the search.

\subsection{A Neural Network Fusion Ranker}
\label{subsec:neural_fusion_ranker}

Every fusion-ranker result so far, tuned or not, uses \texttt{HistGradientBoostingClassifier}, a tree ensemble. This section tries a genuinely different model class on the same ten features, a small multi-layer perceptron (MLP), with its own hyperparameter search over architecture (hidden layer sizes and depth), optimization (learning rate), and regularization (dropout, weight decay), asking whether a neural network does any better than the tuned tree ensemble from Section~\ref{subsec:hyperparameter_tuning}.

\paragraph{Formal setup.} As with the GBDT and logistic-regression models (Section~\ref{sec:learned_fusion}), the MLP takes the same ten-dimensional feature vector $x \in \mathbb{R}^{10}$ per (query, candidate) pair as input and is trained against the same pseudo-relevance target $y \in \{0, 1\}$. Each hidden layer applies a linear transformation followed by a ReLU activation, $\mathrm{ReLU}(z) = \max(0, z)$, and dropout at the searched rate; the final layer is a single linear unit producing a raw logit $z$, with no output activation applied directly. The network is trained to minimise a class-weighted binary cross-entropy loss applied to that logit, $\mathcal{L}(y, z) = -\left[w_y \log \sigma(z) \cdot y + \log(1 - \sigma(z)) \cdot (1 - y)\right]$ where $\sigma$ is the sigmoid function and $w_y$ up-weights the minority (relevant) class inversely to its frequency in the training fold, implemented as \texttt{BCEWithLogitsLoss} with a \texttt{pos\_weight} argument in PyTorch, optimised with AdamW.

\paragraph{Method.} Unlike the GBDT search, full \texttt{LeaveOneGroupOut} cross-validation per hyperparameter combination is too expensive here, each fold trains a network from scratch rather than fitting a near-instant tree, so the search phase uses a single, fixed 80/20 split of the 82 tuning-only queries (66 train, 16 validation), never touching the 19 held-out deep queries; this is a standard, well-justified simplification for neural hyperparameter search. 20 combinations are sampled at random over hidden layer sizes ($\{16\}$ to $\{128,64,32\}$), dropout (0 to 0.5), learning rate ($10^{-4}$ to $5\times10^{-3}$), and weight decay (0 to $10^{-2}$), each scored by NDCG@1{,}000 on the 16 validation queries. Two details are specific to neural networks that a tree ensemble does not need. Features are standardized (zero mean, unit variance, scaler fit on the training split only) before being fed to the network, since gradient descent, unlike a tree split, is sensitive to feature scale, and the loss function is a class-weighted binary cross-entropy (weight inversely proportional to class frequency), mirroring the \texttt{class\_weight="balanced"} convention used by every other classifier in this thesis. Once the best combination is fixed, it is evaluated with the same full \texttt{LeaveOneGroupOut} protocol over all 101 queries used everywhere else, so the comparison against the tuned GBDT on the 19 leakage-free deep queries is direct.

\subsection{Fine-Tuning the Reranker}
\label{subsec:reranker_finetuning}

Every use of \texttt{BAAI/bge-reranker-v2-m3} throughout this thesis, in the hybrid reranking pipeline (Section~\ref{sec:hybrid_fullpool}) and as one of the ten fusion-ranker features, treats it as a frozen, pretrained component, never fit to this thesis's own queries or labels. This section fine-tunes it directly on the silver-labelled (query, candidate) pairs and checks whether that improves ranking quality, reusing the same 82-tuning/19-held-out query split as Section~\ref{subsec:hyperparameter_tuning} rather than introducing a new one, so the two experiments stay directly comparable and the held-out queries remain untouched by either.

\paragraph{Method.} The reranker is fine-tuned as a regression problem. For each silver-labelled (query, candidate-summary) pair from the 82 tuning-only queries ($\sim$1{,}000 examples), the target score is \texttt{gold\_label}/2 (mapping 0/1/2 to 0.0/0.5/1.0, matching the reranker's own continuous score output), trained with MSE loss, AdamW, learning rate $2\times10^{-5}$, batch size 32, for 4 epochs. This is a small dataset for a $\sim$568M-parameter model, a real overfitting risk flagged explicitly rather than glossed over. Both the original pretrained reranker and the fine-tuned version are then evaluated on the same task the reranker actually performs in this pipeline. This means reranking each held-out query's full candidate pool from the union-pool feature set (Section~\ref{sec:learned_fusion}, $\sim$1{,}000-2{,}900 candidates per query) and scoring the result by Recall@$k$/NDCG@$k$ against the silver-verified relevant set, restricted to the 19 deep-coverage queries so the comparison is leakage-free by the same logic as Section~\ref{subsec:hyperparameter_tuning}.

\paragraph{A second attempt: hard negatives and a pairwise ranking loss.} Rather than abandoning fine-tuning, the diagnosed cause was addressed directly. Each training query's negative pool was supplemented with candidates sampled from its own retrieval pool that were never silver-labelled at all (a standard, well-justified assumption given how sparse true relevance is in a raw candidate pool), giving 1{,}782 (positive, negative) training pairs across 79 tuning queries, versus only 19 explicit negatives out of 820 individual examples before. Training switched from MSE regression on an absolute score to a pairwise margin ranking loss, directly optimizing for whether a relevant candidate outscores a non-relevant one for the same query, the thing Recall@$k$/NDCG@$k$ actually measure, rather than an absolute score value.

\section{Phase V: Scalability}
\label{sec:phase5_method}

\subsection{Scaling to a Larger Corpus: A Random Sample and Nested Corpus Tiers}
\label{sec:scaling_corpus}

The 98\,716-company corpus used throughout this thesis is not just query-gatekept (Chapter~\ref{ch:introduction}), it is also small relative to Istari's real production dataset. The thesis title's ``at scale'' claim needs a genuine scalability test, does the retrieval pipeline's quality and latency hold up as the pool of companies to search grows substantially larger, not just a claim resting on the existing corpus size.

\paragraph{A fully random sample.} A collaborator at Istari provided a fully random, query-independent sample of 300\,000 companies drawn from the underlying database, deliberately not filtered or weighted toward any of the 101 queries used elsewhere in this thesis. Randomness here matters methodologically. Using this sample to check whether a retrieval system's index or corpus omits genuinely relevant material, rather than to build more query-specific training data, follows the same logic as random-sampling approaches used to estimate search engine index coverage \citep{baryossef2008random}, applied here to check whether the retrieval pipeline still finds known-relevant companies once far more distractor companies are added, rather than to estimate absolute index coverage as in that work.

\paragraph{Data quality.} The sample was profiled and lightly cleaned. Notably, only 0.56\% (1\,691 companies) overlapped with the existing 98\,716-company corpus, confirming the sample is genuinely independent rather than a re-draw of existing data. No duplicate domains, no empty summaries, and no literal-string placeholder values were found. State/district/municipality fields were missing 15.7\%/60.4\%/75.4\% of the time respectively, expected sparsity given inconsistent administrative-division reporting across countries, not a data quality defect. A small fraction of rows (1.03\%) shared an exact-duplicate summary with another row, flagged rather than dropped, since a shared summary does not necessarily indicate an invalid company record.

\paragraph{Nested corpus tiers.} Rather than four independently drawn samples of increasing size, the scaling corpus is built as four \emph{nested} tiers. These are 100\,000, 200\,000, 300\,000, and $\sim$397\,000 (all available) companies, each strictly containing the previous tier plus additional randomly drawn companies from one fixed shuffle. The existing 98\,716-company corpus, which holds all silver-labelled data used for evaluation, is included in every tier without exception, so that a change in a retrieval metric between tiers can be attributed to the added distractor companies, not to silver-labelled companies being dropped from the search pool. This nested design keeps the four tiers directly comparable to one another, following the same logic that motivates careful control of pooling depth in retrieval evaluation more broadly \citep{lu2016effect}.

\paragraph{Encoding.} Every company in the combined pool ($\sim$397\,025 companies) is encoded exactly once with each embedding model, then subset per tier, avoiding redundant computation. Two exceptions apply. BM25's index is rebuilt separately for each tier, since its term weights depend on the whole indexed collection rather than a fixed per-company encoding. The pretrained reranker (\texttt{bge-reranker-v2-m3}) is not part of this step at all, it scores candidates at retrieval time only, after first-stage retrieval, regardless of corpus size.

\paragraph{Scope: beyond the three fusion-ranker features.} The scaled encoding was not limited to the three models already inside the trained fusion ranker (MiniLM, GTE-large, Linq-Embed-Mistral). Every embedding model evaluated as a baseline earlier in this thesis (BGE-large, BGE-M3, Nomic, E5-Mistral, SFR-Mistral, SimLM) and ColBERTv2 was also encoded at the new scale.

The motivation is the same circularity concern raised in Section~\ref{subsec:refit_fusion_ranker}. The original baseline comparison was evaluated entirely against the circular \texttt{goi\_search\_results.json} ground truth, before the independent, pooled silver standard existed. It has never been rechecked against that standard. The fusion ranker's own feature-importance finding was rechecked this way, and it partially reversed (Section~\ref{subsec:refit_fusion_ranker_result}).

Re-scoring every baseline model's already-computed top-1,000 rankings against the current, non-circular, 1{,}429-candidate silver standard is planned as a follow-up, at zero additional API or GPU cost. It would check whether the reported model ranking, MiniLM best, BGE and BGE-M3 below it, survives independent verification the same way the fusion ranker's finding was checked. Pooling depth and evaluation-set independence, not just raw retrieval scores, determine how trustworthy a reported ranking of methods actually is \citep{baryossef2008random}.

ColBERT is architecturally distinct from every other model here. It stores one embedding per token rather than one vector per company (`doc\_embs.pt`, shape $[N, 192, 128]$, roughly 19.5\,GB at this scale versus 4.85\,GB for the original 98,716-company run), so it cannot simply be subset into corpus-size tiers the same way a single dense vector per company can. It is included nonetheless, for completeness against every method previously evaluated.

\paragraph{Status.} The combined pool and nested tier membership have been constructed, and encoding is complete. All 10 models (MiniLM, GTE-large, BGE-large, BGE-M3, Nomic, Linq-Embed-Mistral, E5-Mistral, SFR-Mistral, SimLM, ColBERTv2) have produced a full embedding file covering all $\sim$397\,025 companies.

The scaling evaluation asks whether Recall@$k$/NDCG@$k$ and query latency hold up as the corpus grows from roughly 100K to 400K companies. It is restricted to the 19 queries with the deepest, most reliable silver-standard coverage (the 5-query pilot plus the 14 headline queries, Section~\ref{subsec:active_learning}), so the resulting metrics are not read from an unreliably shallow label set. BM25 and the three embedding models already inside the trained fusion ranker (MiniLM, GTE-large, Linq-Embed-Mistral) are evaluated at every tier using exact brute-force search. ANN is treated separately in Section~\ref{subsec:ann_tuning}. Every silver-relevant candidate for the 19 deep queries is present in all four tiers by construction, confirmed directly (906 of 906 relevant candidates found in even the smallest, 100K, tier), so any change in these metrics across tiers is attributable to the growing pool of distractor companies rather than relevant companies dropping out of the search pool.

\subsection{Approximate Nearest Neighbor (ANN) Index Tuning}
\label{subsec:ann_tuning}

The thesis proposal commits explicitly to approximate nearest neighbor search (``supports scalable top-$k$ retrieval using approximate nearest neighbor search''; ``trade-off between retrieval accuracy and latency under different ANN index configurations''). Section~\ref{sec:ann_scaling} took a first pass at this question early in the thesis, using a synthetic 2-million-vector corpus (real embeddings padded with Gaussian-noise-perturbed filler vectors) and found a 6.5--6.6$\times$ speedup with no meaningful quality loss. Every other result in this thesis, including the scaling evaluation above, still uses exact brute-force search on real data, a full dot product against every company in the tier. This section repeats the ANN question on the real, nested 100K--400K corpus built for the scaling evaluation, rather than a synthetic one, to check whether the earlier finding holds at genuine (if still sub-20-million) scale.

\paragraph{Method.} A FAISS \citep{johnson2017billion} HNSW index is built over GTE-large's embeddings (the strongest all-around performer from the scaling evaluation above) at each of the four corpus tiers. HNSW's hyperparameters are index-structure settings rather than model-training settings. \texttt{M} (graph connections per node) and \texttt{ef\_construction} (build-time search width) are fixed when an index is built; \texttt{ef\_search} (query-time search width) can be swept cheaply against an already-built index without rebuilding. Two build configurations are tried per tier (\texttt{M}=16/\texttt{ef\_construction}=40, a faster/lighter build, and \texttt{M}=32/\texttt{ef\_construction}=200, a slower/higher-quality build), and \texttt{ef\_search} is swept across \{10, 50, 100, 200, 500, 1{,}000\} for each. Unlike Section~\ref{subsec:hyperparameter_tuning}'s single best-combination search, there is no one ``best'' ANN setting, recall and latency trade off against each other by construction, so the natural output is a curve across settings, compared directly against the exact-search numbers in Table~\ref{tab:scaling_eval} for the same model, tiers, and queries.


\chapter{Results and Discussion}
\label{ch:results}

This chapter reports the results, findings, and interpretation for every experiment described in Chapter~\ref{ch:methodology}, following the same five-phase structure. The central question addressed in each phase is listed below; the evaluation protocol used in each phase is given in Table~\ref{tab:protocol_summary} in Chapter~\ref{ch:methodology}.
\begin{itemize}
\item \emph{Phase~I, Baselines}: Does semantic retrieval beat lexical retrieval?
\item \emph{Phase~II, Improving retrieval}: Can hybrid or learned fusion close the gap to an oracle ceiling?
\item \emph{Phase~III, Evaluation validity}: Is the production-derived label itself trustworthy?
\item \emph{Phase~IV, Learned retrieval revisited}: Do the tuning and fine-tuning improvements remain significant under independent evaluation?
\item \emph{Phase~V, Scalability}: Does the retrieval pipeline remain effective and efficient at four times the corpus scale?
\end{itemize}

\section{Phase I: Baselines}
\label{sec:phase1_result}

\subsection{Phase I: Sparse Retrieval Baseline (BM25)}
\label{sec:baseline_sparse_result}
\label{subsec:bm25_qual}

Manual inspection of BM25 results reveals a consistent failure mode arising from the \textit{vocabulary mismatch problem}. BM25 retrieves companies that contain query keywords in their description, regardless of whether those companies operate in the queried domain.

A representative example is the query \textit{``software companies''}. The top BM25 results are predominantly investment banks and venture capital firms whose summaries frequently mention the word \textit{software} in the context of describing their investment portfolio (e.g.\ \textit{``we invest in software, fintech, and SaaS businesses''}). These companies are not software companies. They fund them. BM25 cannot distinguish between a company \textit{operating} in a domain and a company \textit{discussing} that domain. This limitation provides a clear and concrete motivation for semantic retrieval methods that operate on the meaning of text rather than on token overlap.


\subsection{Phase I: Baseline Results}
\label{sec:exp_results}
\label{sec:exp_runtime_result}

Recall is the primary metric reported in this thesis, following the priority set by Istari for this task. Coverage of relevant companies matters more than precise ranking of the first page of results. NDCG and Precision are used as complementary measures of ranking quality, and query latency is reported alongside them so that quality and cost can be weighed together. Table~\ref{tab:main_results} reports Recall@$k$ for all four baselines, and Table~\ref{tab:latency} breaks query latency down by component.

\begin{table}[ht]
\centering
\caption{Recall@$k$ of all four baseline systems averaged over 101 queries.}
\label{tab:main_results}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Recall@10} & \textbf{Recall@50} & \textbf{Recall@100} & \textbf{Recall@1000} \\
\midrule
BM25   & 0.008 & 0.037 & 0.072 & 0.635 \\
MiniLM & \textbf{0.010} & \textbf{0.047} & \textbf{0.094} & \textbf{0.741} \\
BGE    & 0.009 & 0.046 & 0.091 & 0.726 \\
OpenAI & 0.009 & 0.045 & 0.090 & 0.728 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{BM25 vs dense retrieval.} All three dense methods outperform BM25 at every cut-off tested, most visibly at low cut-offs where semantic matching addresses the vocabulary-mismatch problem that limits BM25 (Section~\ref{subsec:bm25_qual}). At $k=10$, MiniLM achieves the highest NDCG (0.975) and Precision (0.972), ahead of BGE (0.942 / 0.940) and OpenAI (0.934 / 0.927), with all three comfortably above BM25 (0.854 / 0.838). The advantage persists at the largest cut-off rather than narrowing. BM25 reaches a Recall@1000 of 0.635, compared to MiniLM's 0.741, BGE's 0.726, and OpenAI's 0.728, a somewhat different pattern from the BEIR benchmark~\citep{thakur2021beir}, where keyword-based methods often close the gap with dense models at large retrieval depths. MiniLM is therefore the strongest baseline on retrieval quality alone, ahead of both BGE and OpenAI despite having roughly 15 times fewer parameters than BGE and no proprietary training data behind it.

\paragraph{Why does the smallest model win?} This result is worth pausing on, since more parameters and more specialised training usually help, not hurt. BGE and OpenAI's embedding model are both tuned with hard-negative mining and large-scale passage-retrieval objectives, which pay off most on long, heterogeneous documents where distinguishing a genuine match from a superficially similar distractor is hard. Company summaries in this corpus are short (roughly 60 words on average, Section~\ref{subsec:production_independent_eval_result}) and fairly uniform in structure, closer in shape to the short sentence pairs MiniLM was originally fine-tuned on for general sentence similarity. A model's specialised training only helps if that specialisation matches the text it is applied to, and here MiniLM's simpler training objective is the closer match. The same domain-mismatch pattern explains, at a smaller scale, why SimLM underperforms later in this chapter despite being purpose-pretrained for retrieval (Section~\ref{sec:simlm_result}).

\paragraph{BGE vs OpenAI.} BGE and OpenAI remain close to one another across all metrics and cut-offs, though both trail MiniLM. The largest observed difference between them is 0.012 NDCG points (at $k=100$), within the range of query-level variance and not a meaningful quality advantage either way. The practical difference is latency. BGE's 44.4\,ms total query latency is a fraction of OpenAI's 254.6\,ms, so a free, self-hosted model matches a commercial API's quality while running roughly six times faster.

\begin{table}[ht]
\centering
\caption{Query latency by component (encoding, search, network), averaged over 20 queries on an A100 GPU. Bold marks the fastest total.}
\label{tab:latency}
\setlength{\tabcolsep}{8pt}
\begin{tabular}{lrrrr}
\toprule
\textbf{Method}
& \textbf{Encode (ms)}
& \textbf{Search (ms)}
& \textbf{Network (ms)}
& \textbf{Total (ms)} \\
\midrule
BM25   &   0.1 & 104.0 &   0.0 & 104.1 \\
MiniLM &  68.9 &  12.0 &   0.0 &  80.9 \\
BGE    &  16.6 &  27.8 &   0.0 &  \textbf{44.4} \\
OpenAI & \textit{server} &  42.9 & 211.7 & 254.6 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{What drives the latency differences.} Latency differences trace to hardware and network, not model size. BGE is the only method that encodes on GPU (16.6\,ms), against MiniLM's CPU encoding (68.9\,ms, despite 15 times fewer parameters than BGE) and BM25's CPU-bound inverted-index lookup (104.0\,ms). OpenAI's latency is dominated instead by its 211.7\,ms network round-trip, an unavoidable cost of API-based encoding that a self-hosted model like BGE avoids.

\begin{table}[ht]
\centering
\caption{Summary comparison of all baseline methods (pseudo-relevance protocol, Section~\ref{subsec:eval_protocol}). Bold marks the best value per column. ``GPU (query)'' refers to query-time encoding only.}
\label{tab:summary}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lllllll}
\toprule
\textbf{Method}
& \textbf{NDCG@10}
& \textbf{Prec@10}
& \textbf{Recall@1k}
& \textbf{Latency}
& \textbf{Cost}
& \textbf{GPU (query)} \\
\midrule
BM25   & 0.854 & 0.838 & 0.635 & 104\,ms & Free    & No \\
MiniLM & \textbf{0.975} & \textbf{0.972} & \textbf{0.741} & 81\,ms  & Free    & No \\
BGE    & 0.942 & 0.940 & 0.726 & \textbf{44\,ms} & Free & Yes \\
OpenAI & 0.934 & 0.927 & 0.728 & 255\,ms & \$0.02/M tok. & No \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:summary} collects these trade-offs. MiniLM leads on quality and remains free and self-hostable. BGE is the fastest of the four and doubles as the cross-encoder reranker used in later hybrid experiments (Section~\ref{sec:rrf}). These baselines motivate the next experimental phase, where a pre-filtering stage and hybrid retrieval system are built on top of them to improve precision at low cut-offs and overall efficiency.

\section{Phase II: Improving Retrieval}
\label{sec:phase2_result}

\subsection{Extended Baseline Comparison: Results}
\label{sec:extended_baselines_result}

\begin{table}[ht]
\centering
\caption{Extended baseline Recall@$k$ under the (top-1\,000)
         pseudo-relevance protocol (Section~\ref{sec:protocol_note}).
         Bold indicates the best value in each column.}
\label{tab:extended_baselines}
\begin{tabular}{lccccc}
\toprule
& \multicolumn{4}{c}{\textbf{Precision@$k$}} & \textbf{Recall@1000} \\
\cmidrule(lr){2-5}
\textbf{Method} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{1000} & \\
\midrule
BGE-M3        & 0.927 & 0.916 & 0.905 & 0.704          & 0.704 \\
GTE-large     & 0.946 & 0.936 & 0.925 & \textbf{0.768} & \textbf{0.768} \\
E5-Mistral-7B & 0.629 & 0.572 & 0.523 & 0.274          & 0.274 \\
Linq-Mistral  & 0.934 & 0.921 & 0.915 & 0.749          & 0.749 \\
SFR-Mistral   & 0.933 & 0.922 & 0.915 & 0.746          & 0.746 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Findings.} Quality does not keep climbing with size, and the added compute cost of the 7B-parameter tier is not consistently repaid. GTE-large is the strongest of the five additional models (Recall@1000~=~0.768), ahead of Linq-Mistral and SFR-Mistral (0.749 and 0.746) and BGE-M3 (0.704). These four sit well clear of E5-Mistral-7B-Instruct (0.274), despite it sharing the same base architecture and parameter count as Linq-Mistral and SFR-Mistral, so this gap cannot be a scale effect. It indicates that fine-tuning data and strategy, not model size, are the dominant factor for this retrieval task, and that the two much smaller BGE-M3 and GTE-large are competitive with or better than every 7B-parameter model tested, at a fraction of the cost. 

Precision here is measured against production's own pseudo-relevance labels (Section~\ref{subsec:eval_protocol}). This thesis stops tracking Precision once that label set's limitations are addressed and relies on Recall as the metric that matters for this task from that point onward (Section~\ref{subsec:refit_fusion_ranker_result}).

\paragraph{Encoding cost.} The three 7B-parameter models also took substantially longer to encode, roughly 34 minutes each on an A100 GPU, about five times the 7-minute encoding time of the original BGE-large baseline (Section~\ref{subsec:bge}), for retrieval quality that is not better and, in E5-Mistral's case, substantially worse.

\subsection{Query Expansion: HyDE Results}
\label{sec:hyde_result}

\begin{table}[ht]
\centering
\caption{HyDE pipeline results vs.\ the plain MiniLM baseline (top-1\,000 protocol).}
\label{tab:hyde}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccc}
\toprule
\textbf{Method} & \textbf{NDCG@10} & \textbf{Recall@1000} & \textbf{Latency (ms)} \\
\midrule
MiniLM (no expansion)        & 0.975 & 0.741 & 16.9 \\
Stage 1: HyDE + MiniLM       & 0.829 & 0.596 & 9\,246.9 \\
Stage 2: HyDE + MiniLM + Reranker & 0.954 & 0.596 & 9\,488.3 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Query expansion via HyDE \emph{hurts} retrieval quality relative to directly encoding the original query. Stage 1 NDCG@10 drops from 0.975 to 0.829, and Recall@1000 drops from 0.741 to 0.596. Adding a cross-encoder reranker (Stage 2) recovers most of the NDCG@10 loss (0.829~$\to$~0.954) by re-ordering the top-50 candidates, but cannot recover the recall loss, since reranking only reorders an already-fixed candidate set. Latency is dominated overwhelmingly by the LLM call itself (9.2 seconds), three orders of magnitude slower than directly encoding the query with MiniLM (16.9~ms). For this dataset, HyDE is not a worthwhile query-expansion strategy. It is far slower and retrieves a worse candidate set than simply embedding the query directly.

\subsection{Hybrid Retrieval: RRF Results}
\label{sec:rrf_result}

\begin{table}[ht]
\centering
\caption{Hybrid RRF results (top-1\,000 protocol).}
\label{tab:rrf}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{NDCG@10} & \textbf{Recall@1000} & \textbf{Latency (ms)} \\
\midrule
MiniLM (no fusion)              & 0.975 & 0.741 & 16.9 \\
Stage 1: RRF (BM25 + MiniLM)    & 0.955 & 0.724 & 104.9 \\
Stage 2: RRF + Reranker          & 0.975 & 0.724 & 352.0 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Unlike HyDE, RRF fusion of BM25 and MiniLM adds negligible latency (the fusion step itself takes 2.2\,ms) and, after reranking, matches the plain MiniLM baseline's NDCG@10 (0.975), while Recall@1000 remains slightly below MiniLM alone (0.724 vs 0.741). BM25's keyword matches do not add companies beyond what MiniLM already retrieves for this query set, so fusion mostly re-confirms rather than extends MiniLM's candidate pool. RRF is a much cheaper and more effective way to incorporate a second retrieval signal than HyDE-style query expansion.

\subsection{ANN Index Scaling: Results}
\label{sec:ann_scaling_result}

\begin{table}[ht]
\centering
\caption{Exact vs.\ approximate search at 2M-vector scale (top-1\,000 protocol, representative configurations only).}
\label{tab:ann_scaling}
\begin{tabular}{lccccc}
\toprule
\textbf{Index} & \textbf{NDCG@10} & \textbf{Recall@1000} & \textbf{ms/query} & \textbf{Speedup} & \textbf{Index size} \\
\midrule
Exact (FlatL2)        & 0.975 & 0.741 & 175.5 & 1.0$\times$ & 3\,072\,MB \\
IVF (nprobe=8)         & 0.974 & 0.743 & 26.4  & 6.6$\times$ & 3\,094\,MB \\
HNSW (efSearch=256)    & 0.975 & 0.727 & 26.9  & 6.5$\times$ & 3\,616\,MB \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Both IVF (\texttt{nprobe}=8) and HNSW (\texttt{efSearch}=256) recover essentially all of exact search's NDCG@10, and IVF slightly exceeds exact search's Recall@1000 (0.743 vs 0.741, within noise), while running 6.5--6.6$\times$ faster at 2-million-vector scale, at the cost of modest additional index memory (0.7\% and 17.7\% larger, respectively). This confirms that approximate search is a safe and substantial efficiency gain for scaling the retrieval system toward the full 20-million-company GOI, without a meaningful quality trade-off at this scale.

\subsection{Recall@1000 Error Analysis: Results}
\label{sec:error_analysis_result}

Every MiniLM miss was searched for at much greater depth ($k=10\,000$) to see how close it actually was. Table~\ref{tab:error_analysis} classifies the resulting 26\,185 misses by rank depth beyond 1\,000.

\begin{table}[ht]
\centering
\caption{Classification of the 26\,185 Recall@1000 misses by rank depth beyond 1\,000.}
\label{tab:error_analysis}
\begin{tabular}{lrr}
\toprule
\textbf{Bucket} & \textbf{Count} & \textbf{Share (\%)} \\
\midrule
Near miss (rank 1\,000--2\,000)   & 16\,157 & 61.7 \\
Mid miss (rank 2\,000--5\,000)    &  8\,061 & 30.8 \\
Far miss (rank 5\,000--10\,000)   &  1\,375 &  5.3 \\
Not found (beyond rank 10\,000)   &    592 &  2.3 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} The large majority of misses (61.7\%) are near misses, just outside the top-1\,000 cutoff, suggesting most of the recall gap is a matter of ranking granularity rather than a hard semantic failure. Only 2.3\% of misses are not found at all within $k=10\,000$, indicating genuine embedding failures are rare. Separately, 3.8\% of missed companies (996 of 26\,185) were flagged as boilerplate content, and one extreme case of description duplication was found. The placeholder text ``Bluehost.com'' appears as the company description for 709 distinct domains, meaning these domains are indistinguishable to any text-embedding model and can never be individually retrieved correctly. This is a concrete data-quality artifact rather than a retrieval-model limitation, and sets a hard ceiling on achievable recall for any embedding-based method built on this corpus's text representation.

\subsection{Corpus Quality: Findings}
\label{sec:trust_filter_result}

\paragraph{How much of the corpus is affected.} This contamination is not spread evenly across query topics. An audit of the pseudo-relevance labels for all 101 queries found that, for the query \textit{``web hosting companies''}, 85.4\% of the 1\,000 ground-truth relevant companies have a boilerplate rather than a company-specific summary. For \textit{``cloud computing providers''} the figure is 73.1\%, and for \textit{``e-commerce platforms''} it is 53.1\%. By contrast, most other query topics in the evaluation set (among them \textit{``software companies''}, \textit{``healthcare providers''}, and \textit{``construction companies''}) show no measurable contamination at all. This pattern is intuitive once the underlying mechanism is understood. Boilerplate summaries describe hosting, domain, and e-commerce infrastructure, so they are only likely to be retrieved, and only likely to be judged relevant by the production system's own ranking, for queries whose intent is itself adjacent to that infrastructure. A query about software companies is unlikely to surface a domain registrar's boilerplate text, but a query about web hosting companies is exactly the case in which that boilerplate text looks, on the surface, like a plausible match.

This classifier was validated against 300 companies independently judged by an LLM as having a trustworthy or untrustworthy summary. At its best-performing similarity threshold, the classifier reaches an F1 score of 0.59, with a precision of 0.52 and a recall of 0.69 for detecting untrustworthy summaries.

\paragraph{Why this limitation was not corrected in the reported results.} A precision of 0.52 means that of every two summaries the classifier flags as boilerplate, roughly one is, in fact, a genuine, company-specific summary. Removing or down-weighting every flagged company from the retrieval pipeline would therefore discard a large number of companies that are not actually contaminated, trading one source of error for another rather than removing it. For this reason, no correction for boilerplate contamination was applied to any of the results reported in this thesis, and all recall and precision figures should be read with this limitation in mind, particularly for the small set of query topics identified above as heavily affected. This is reported here as an explicitly known and quantified, but unaddressed, limitation of the underlying corpus rather than as a solved problem (Section~\ref{sec:future_work} returns to this as a direction for future work).

\paragraph{Does this get worse at scale?} Applying the same classifier to the nested scaling corpus (Section~\ref{sec:scaling_corpus}) answers this directly rather than leaving it to speculation. The original 98\,716-company corpus is contaminated at a stable 2.84\% at every tier, expected, since it is the same fixed set of companies present in all four. The randomly sampled companies added to reach each larger tier are, however, markedly cleaner, contaminated at only 0.39--0.41\%, roughly a seventh of the original corpus's rate. Because the clean, randomly sampled companies make up a growing share of the total pool at each successive tier, the pool-wide contamination rate actually falls as the corpus scales. It falls from 2.81\% at 100K to 1.61\% at 200K, 1.21\% at 300K, and 1.02\% at the full 397K tier. This is a reassuring, if unexpected, result. The boilerplate contamination problem does not compound at scale, it dilutes, most likely because the original 98\,716-company corpus was itself drawn from production's own top-1\,000 results per query (Section~\ref{subsec:dataset}), which concentrates exactly the infrastructure-adjacent queries (Section~\ref{sec:trust_filter}) where boilerplate is most likely to be judged relevant, a bias a uniformly random sample of the broader GOI does not share.

\subsection{Oracle Union Recall Ceiling: Results}
\label{sec:oracle_union_result}

\paragraph{Findings.} The best single channel (MiniLM alone) reaches Recall@1000~=~0.741, while the full six-channel union reaches 0.936 (average pool size 1\,949 candidates/query), a gain of +0.196. Separately, the best-performing \emph{pair}, MiniLM~+~OpenAI-large, reaches only 0.856 (average pool size 1\,300), and a pool-size sweep on this pair shows the ceiling is highly sensitive to per-channel depth, from 0.150 recall at 100 candidates/channel to 0.581 at 500 and 0.856 at the full 1\,000.

\paragraph{Interpretation.} Because the six-channel union clears 0.936, well above any individual channel, the different retrieval methods are largely making \emph{complementary}, not correlated, errors, so a reranker over a modestly sized union pool is likely sufficient to approach this ceiling without a full fine-tuning pipeline. The residual 0.064 recall lost even under the full union matches the hard data-quality ceiling from duplicated or boilerplate descriptions identified in Section~\ref{sec:error_analysis_result}.

\subsection{SimLM: Results}
\label{sec:simlm_result}

SimLM is evaluated under the same top-1\,000 protocol as every other baseline in this chapter. Table~\ref{tab:simlm} reports its Recall@1000 alongside the strongest baselines seen so far, for reference.

\begin{table}[ht]
\centering
\caption{SimLM Recall@1000 (top-1\,000 protocol) alongside the
         strongest baselines evaluated so far, for reference.}
\label{tab:simlm}
\begin{tabular}{lccccc}
\toprule
& \multicolumn{4}{c}{\textbf{Precision@$k$}} & \textbf{Recall@1000} \\
\cmidrule(lr){2-5}
\textbf{Method} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{1000} & \\
\midrule
BM25 (Section~\ref{sec:baseline_sparse})         & 0.838 & 0.748 & 0.723 & 0.635 & 0.635 \\
SimLM                                            & 0.920 & 0.881 & 0.861 & 0.609 & 0.609 \\
MiniLM (Section~\ref{subsec:minilm})             & 0.972 & 0.950 & 0.938 & 0.741 & 0.741 \\
GTE-large (Section~\ref{sec:extended_baselines}) & 0.946 & 0.936 & 0.925 & \textbf{0.768} & \textbf{0.768} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} SimLM underperforms every dense baseline evaluated in this thesis, and its Recall@1000 (0.609) falls even below the lexical BM25 baseline (0.635), despite SimLM being purpose-pretrained for passage retrieval. (NDCG@10 tells a less clear-cut story here. SimLM's 0.923 sits above BM25's 0.854, since NDCG rewards ranking quality among whichever companies are retrieved, not coverage of the full relevant set.) A likely explanation is a domain mismatch between SimLM's pre-training convention and this task. SimLM was pre-trained and fine-tuned on MS~MARCO, a corpus of short, single-topic passages, using a title/body sentence-pair format that assumes the title is a concise topical label. Company names are not topical labels in the same sense. ``Sweet Software'' or ``Springcoast Capital Partners'' convey little about what the paired body text describes, so the sentence-pair convention that helps SimLM on MS~MARCO-style data may provide a weaker signal here than simply concatenating all fields into one string, as the other baselines do. This result is a useful negative finding. Retrieval-specific pre-training does not automatically transfer across domains, and general-purpose sentence embeddings (MiniLM, GTE-large) remain the stronger choice for this task.

\subsection{ColBERT: Results}
\label{sec:colbert_result}

\begin{table}[ht]
\centering
\caption{ColBERT (brute-force MaxSim) Recall@1000 (top-1\,000
         protocol) alongside the strongest single-vector baselines.}
\label{tab:colbert}
\begin{tabular}{lccccc}
\toprule
& \multicolumn{4}{c}{\textbf{Precision@$k$}} & \textbf{Recall@1000} \\
\cmidrule(lr){2-5}
\textbf{Method} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{1000} & \\
\midrule
MiniLM (Section~\ref{subsec:minilm})             & 0.972 & 0.950 & 0.938 & 0.741 & 0.741 \\
GTE-large (Section~\ref{sec:extended_baselines}) & 0.946 & 0.936 & 0.925 & 0.768 & 0.768 \\
ColBERT (MaxSim)                                 & 0.936 & 0.931 & 0.925 & \textbf{0.773} & \textbf{0.773} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} ColBERT's Recall@1000 (0.773) is the highest of any single retrieval channel evaluated in this thesis, narrowly ahead of GTE-large (0.768) and clearly ahead of MiniLM (0.741). Its NDCG@10 (0.937), however, is lower than both MiniLM (0.975) and GTE-large (0.946), meaning ColBERT retrieves a broader set of relevant companies overall but ranks the very top results somewhat less precisely. Token-level interaction therefore does deliver a genuine, if modest, recall improvement over the best single-vector models, though not one large enough on its own to close the gap to the hybrid rerank pipelines described in Section~\ref{sec:hybrid_fullpool}.

\subsection{Hybrid Full-Pool Reranking: Results}
\label{sec:hybrid_fullpool_result}

\begin{table}[ht]
\centering
\caption{Full-pool reranking vs.\ the shallow RRF~+~top-50-rerank baseline (Section~\ref{sec:rrf}). Bold marks the best value.}
\label{tab:hybrid_fullpool}
\small
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{lccccc}
\toprule
\textbf{Method} & \textbf{P@10} & \textbf{P@100} & \textbf{P@1000} & \textbf{R@1000} & \textbf{Pool} \\
\midrule
RRF + top-50 (\S\ref{sec:rrf})   & 0.980 & 0.940 & 0.782          & 0.782          & 50 \\
2-way (MiniLM+Linq)              & 0.955 & 0.950 & 0.802          & 0.802          & 1\,300 \\
3-way (+GTE-large)               & 0.955 & 0.950 & \textbf{0.804} & \textbf{0.804} & 1\,424 \\
4-way (+OpenAI-large)            & 0.955 & 0.950 & 0.802          & 0.802          & 1\,600 \\
\bottomrule
\end{tabular}
\normalsize
\end{table}

\paragraph{Finding.} Reranking the full union pool instead of a fixed top-50 slice raises Recall@1000 substantially (0.782~$\to$~0.802, 2-way), at the cost of a small drop in NDCG@10 (0.979~$\to$~0.955). The reranker now has many more genuinely difficult candidates to rank, so its ordering of the very top results is slightly less precise, even though it recovers more of the true relevant set overall. Adding a third channel (GTE-large) improves Recall@1000 further, to 0.804, the best result obtained by any hybrid pipeline built from off-the-shelf pretrained components in this thesis. Adding a fourth channel (OpenAI-large) does \emph{not} improve on this. Recall@1000 is unchanged at 0.802, within noise of the 3-way result.

\paragraph{Interpretation: channel saturation.} This flat 3-way~$\to$~4-way result is an important turning point in this thesis's experimental narrative. Section~\ref{sec:oracle_union_result} showed that combining \emph{more} candidate-generating channels reliably raises the oracle recall ceiling. Here, adding a fourth channel to an already capable 3-way pool does not translate into a better final result. This suggests that candidate coverage is no longer the limiting factor for this pipeline. The union pool already contains the relevant companies needed to reach a substantially higher recall than 0.804 (recall Section~\ref{sec:oracle_union_result}'s six-channel oracle ceiling of 0.936), and the reranker itself is now the bottleneck preventing the pipeline from closing that gap. This motivates two follow-up questions, addressed in the next two sections. Is the specific reranker \emph{model} the limiting factor (Section~\ref{sec:reranker_ablation}), or is a fundamentally different fusion approach needed (Section~\ref{sec:learned_fusion})?

\subsection{Reranker Ablation: Results}
\label{sec:reranker_ablation_result}

This ablation isolates the reranker model itself, holding the candidate pool fixed and swapping only the cross-encoder. Table~\ref{tab:reranker_ablation} compares the two.

\begin{table}[ht]
\centering
\caption{Reranker ablation Recall@1000, two reranker models on an identical candidate pool.}
\label{tab:reranker_ablation}
\small
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{lcccc}
\toprule
\textbf{Reranker} & \textbf{P@10} & \textbf{P@100} & \textbf{P@1000} & \textbf{R@1000} \\
\midrule
BAAI/bge-reranker-v2-m3 (\S\ref{sec:hybrid_fullpool}) & 0.955 & 0.950 & \textbf{0.8042} & \textbf{0.8042} \\
mxbai-rerank-large-v1                                 & 0.968 & 0.940 & 0.8035          & 0.8035 \\
\bottomrule
\end{tabular}
\normalsize
\end{table}

\paragraph{Finding.} Recall@1000 is essentially unchanged between the two rerankers (0.8042 vs.\ 0.8035, a difference of 0.0007, well within query-level noise). NDCG@10 does improve modestly with the alternative reranker (0.955~$\to$~0.971), indicating that \texttt{mxbai-rerank-large-v1} orders the very top results somewhat better, without changing which companies land in the top-1\,000 overall. The conclusion is that reranker \emph{model choice} is not the bottleneck identified in Section~\ref{sec:hybrid_fullpool_result}. The remaining gap to the oracle ceiling is not a matter of picking a better off-the-shelf cross-encoder. This points towards a more fundamental limitation. Either genuinely hard, vocabulary-mismatched cases exist that no cross-encoder scoring this corpus's text representation can resolve, or the fusion mechanism itself (simple set union plus independent reranking) is insufficiently expressive, motivating the learned approach in Section~\ref{sec:learned_fusion}.

\subsection{Learned Fusion Ranker: Results}
\label{sec:learned_fusion_result}

Rather than combining channels by a fixed rule, a model is trained to weigh them. Table~\ref{tab:learned_fusion} reports its held-out, cross-validated Recall@1000 against the best pretrained-component pipeline.

\begin{table}[ht]
\centering
\caption{Learned fusion ranker Recall@1000 vs.\ the best pretrained-component pipeline (Section~\ref{sec:hybrid_fullpool}). Bold marks the best value.}
\label{tab:learned_fusion}
\small
\setlength{\tabcolsep}{2.5pt}
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{P@10} & \textbf{P@100} & \textbf{P@1000} & \textbf{R@1000} \\
\midrule
3-way + BGE reranker (\S\ref{sec:hybrid_fullpool}) & 0.955 & 0.950 & 0.804          & 0.804          \\
Learned fusion, logistic regression                  & 0.968 & 0.953 & 0.821          & 0.821          \\
Learned fusion, gradient-boosted trees                & \textbf{0.977} & \textbf{0.955} & \textbf{0.823} & \textbf{0.823} \\
\bottomrule
\end{tabular}
\normalsize
\end{table}

\paragraph{Finding.} The gradient-boosted learned fusion ranker reaches Recall@1000~=~0.823, the best result obtained anywhere in this thesis, improving on the best pretrained-component pipeline (0.804) by $+0.019$. For scale, this gain is roughly 27 times larger than the 0.0007 movement observed when merely swapping reranker models in Section~\ref{sec:reranker_ablation_result}. This is a substantive improvement attributable to training on the task's own data, not noise. NDCG@10 also improves (0.955~$\to$~0.979).

\paragraph{Interpretation: what the model learned.} The trained logistic regression's coefficients offer a direct, interpretable view into which signals matter most for this task. MiniLM's \emph{inverse rank} has by far the largest coefficient magnitude (+5.01), exceeding every raw similarity score \emph{and} the pretrained reranker's own score (+0.16). In other words, where a candidate is ranked by MiniLM is a more informative signal for final relevance than the pretrained cross-encoder's score. This is a concrete, non-obvious finding that would not be visible from simply combining components by hand. This supports the conclusion drawn in Section~\ref{sec:reranker_ablation}. The pretrained reranker was not extracting the maximum available signal from the candidate pool, and a lightweight model trained on this task's own data closes a meaningful part of the remaining gap to the oracle ceiling from Section~\ref{sec:oracle_union}.

\section{Phase III: Evaluation Validity}
\label{sec:phase3_result}

\subsection{Building the Silver Standard: Results}
\label{subsec:pooled_gold_standard_result}

\paragraph{Inter-rater reliability.} Percent agreement, reported throughout this thesis so far, overstates reliability on a skewed label distribution. Two judges that both default to the majority label (highly relevant) will agree often without adding any real information. Cohen's kappa \citep{cohen1960coefficient} corrects for this by subtracting out the agreement expected by chance, and is standard practice for reporting annotator reliability in computational linguistics and IR test-collection construction \citep{artstein2008inter}. Computed across every judging round run for this thesis (2\,539 doubly-judged candidates in total), the original judge pair (\texttt{gpt-4o-mini}/Claude Haiku) reaches $\kappa = 0.626$ (82.5\% agreement, $n=2\,280$), and the upgraded pair (\texttt{gpt-5.4}/Claude Sonnet 5) reaches $\kappa = 0.882$ (93.8\% agreement, $n=259$). Using the conventional interpretation scale of \citet{landis1977measurement}, this places the original pair at ``substantial'' agreement (0.61--0.80) and the upgraded pair at ``almost perfect'' agreement (0.81--1.00), giving the earlier decision to upgrade judges for spot-checks and the production-independent evaluation (Section~\ref{subsec:production_independent_eval}) a concrete quantitative justification rather than only the qualitative one offered at the time.

\paragraph{Blind calibration against the silver standard.} The external review from the annotator at Istari (Section~\ref{subsec:external_review_prep}) has since returned. The kappa figures above measure whether the LLM judges agree with each other, and the small 20-candidate pilot in Section~\ref{subsec:pooled_gold_standard} measures whether the author's blind judgment matches each judge individually. Neither measures whether an independent human agrees with the silver standard as a whole, at a scale large enough to support a proper kappa. To close that gap, the author blind-labelled a much larger, 300-candidate sample spanning industry, geographic, and functional queries, with no rank or prior label visible, and compared the result against the existing silver labels.

Agreement was substantial, 78.7\% raw agreement and a kappa of 0.603, and disagreements were balanced rather than one-sided: the author judged roughly as many candidates more strictly as more loosely than the silver label. The one real weak spot was geographic queries. The lowest-agreement queries were disproportionately ones asking for companies in a specific city or region, a pattern that tracks a genuinely harder judgement call, a company's registered location versus where it actually operates, rather than a general problem with the silver standard.

\subsection{Retraining Against the New Silver Standard: Results}
\label{subsec:refit_fusion_ranker_result}

From this point onward, this thesis reports Recall@$k$ and NDCG@$k$ but not Precision@$k$. The silver standard is pooled rather than exhaustive, so an unlabelled candidate cannot be scored as a false positive even when it is genuinely relevant. Recall@$k$ and NDCG@$k$ are computed only over candidates that already carry a label, so this gap does not bias them. Precision@$k$ would be biased, since some of a method's unlabelled retrieved candidates are almost certainly relevant and would inflate the apparent false-positive count if simply treated as irrelevant. Section~\ref{subsec:honest_synthesis} discusses this limitation, and its remaining effect on Recall and NDCG, in full.

\begin{table}[ht]
\centering
\caption{NDCG@$k$ against the new silver standard: retrained vs.\ original fusion ranker, same 417 candidates.}
\label{tab:pooled_ndcg}
\begin{tabular}{lccc}
\toprule
\textbf{Model} & \textbf{NDCG@5} & \textbf{NDCG@10} & \textbf{NDCG@20} \\
\midrule
Original model's scores (\S\ref{sec:learned_fusion_result}) & 0.915 & 0.930 & 0.940 \\
Retrained on new silver standard        & \textbf{0.934} & \textbf{0.952} & \textbf{0.947} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{Logistic regression feature weights: original ground truth vs.\ the 5-query and 101-query silver standards (Section~\ref{subsec:active_learning}).}
\label{tab:pooled_weights}
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{lrrr}
\toprule
\textbf{Feature} & \textbf{Original} & \textbf{Pilot (5 queries)} & \textbf{Expanded (101 queries)} \\
\midrule
invrank\_minilm  & $+5.014$ & $-0.909$ & $-1.095$ \\
score\_linq      & $+1.097$ & $+1.965$ & $+1.381$ \\
invrank\_gte     & $+0.958$ & $+0.323$ & $+0.072$ \\
score\_gte       & $+0.921$ & $+0.791$ & $-0.701$ \\
invrank\_linq    & $+0.867$ & $+0.416$ & $+0.172$ \\
score\_minilm    & $+0.749$ & $-0.146$ & $-0.703$ \\
n\_channels      & $+0.459$ & $-0.289$ & $+0.394$ \\
invrank\_bm25    & $-0.258$ & $-1.252$ & $-2.246$ \\
reranker\_score  & $+0.164$ & $+0.319$ & $+0.146$ \\
score\_bm25      & $+0.054$ & $-0.134$ & $-0.069$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Retraining on the pooled, independently judged silver standard modestly improves ranking quality over simply reusing the original model's scores (NDCG@5: 0.915~$\to$~0.934; NDCG@10: 0.930~$\to$~0.952), confirming that the new labels carry a genuine, learnable signal rather than just noise. More importantly, the original interpretive finding from Section~\ref{sec:learned_fusion_result} does not replicate. \texttt{invrank\_minilm}, previously the dominant feature by a wide margin ($+5.014$, more than double the next-largest coefficient), collapses not merely toward zero but to a small \emph{negative} coefficient ($-0.909$). The most plausible explanation is that MiniLM's ranking correlated strongly with production's own circular output specifically, rather than with relevance itself, a correlation that a model trained and evaluated against that same circular output could not distinguish from a genuine relevance signal.

\texttt{score\_linq} (Linq-Embed-Mistral's raw similarity score) is the new dominant feature ($+1.965$), and \texttt{invrank\_bm25} is now strongly negative ($-1.252$). Being ranked highly by BM25 is associated with \emph{lower} true relevance in this sample. This corroborates, independently and statistically, the qualitative BM25 failure mode already documented in Section~\ref{subsec:bm25_qual}, where investment firms and venture capital companies are ranked highly by BM25 for the query ``software companies'' because they mention the word in passing while describing their investment portfolio, without themselves being software companies.

\paragraph{Confirmation at full scale.} The Limitations paragraph below originally flagged this as a 5-query pilot whose specific coefficients should be read cautiously. Section~\ref{subsec:active_learning}'s expanded silver standard (1{,}429 candidates across all 101 queries) makes it possible to check this directly. Retraining the same architecture on the full 101-query silver standard (rightmost column of Table~\ref{tab:pooled_weights}) does not weaken the finding, if anything it sharpens it. \texttt{invrank\_minilm} remains negative ($-1.095$, was $-0.909$ at 5 queries), and \texttt{invrank\_bm25} becomes even more strongly negative ($-2.246$, was $-1.252$), now the single largest-magnitude coefficient in the model. \texttt{score\_linq} remains the dominant positive feature throughout all three columns. The BM25 vocabulary-mismatch failure mode is therefore not an artefact of the ``software companies'' pilot query specifically, it holds consistently once every one of the 101 query topics contributes labelled examples.

\paragraph{Limitations.} This result covers 5 of 101 queries and 417 candidates, a genuine pilot rather than a full replication, and the specific numerical coefficients should be read with that scale in mind. However, the magnitude of the reversal on what was previously the single dominant feature is unlikely to be an artefact of sample size alone, and is treated here as a preliminary but suggestive finding. The interpretive claims drawn from a model trained on circular pseudo-relevance labels should not be taken as descriptions of genuine relevance without independent validation. Scaling this pooled, multi-judge silver standard to the full 101-query set is a direction for future work.

\subsection{Bronze Labels: Results}
\label{sec:silver_labels_result}

\paragraph{Bronze-label accuracy.} The bronze tier (Section~\ref{sec:silver_labels}) reproduces the silver standard's relevance judgments with a trained classifier rather than independent LLM judging, and has now been applied to all 173\,262 candidates. Of the 150 spot-checked bronze candidates, 132 (88\%) received unanimous agreement between \texttt{gpt-5.4} and \texttt{claude-sonnet-5}. Against that unanimous subset, the bronze labels achieved 65.9\% accuracy, against a 56.8\% majority-class baseline on the same 132 candidates, so the bronze labels carry a real, learnable signal, but a modest one. The classifier over-predicts relevance, consistent with the 75.4\% overall relevant-rate it assigns across the full bronze tier.

The most likely explanation for the gap between this figure and the substantially higher NDCG improvement reported in Section~\ref{subsec:refit_fusion_ranker_result} is a training-distribution mismatch rather than a flaw in the distillation approach itself. Only 8 of the 132 unanimously-judged spot-check candidates (6\%) fall under one of the 5 queries the underlying silver standard was built from, the remaining 124 (94\%) come from query topics the classifier never saw a single labelled example of during training. Generalising from 5 narrow query topics to the other 96 is a substantially harder test than the leave-one-query-out cross-validation in Section~\ref{subsec:refit_fusion_ranker}, which only ever held out one of those same 5 topics at a time. This points toward a concrete, affordable fix rather than abandoning the approach: expanding the silver standard to a larger, more topically diverse subset of the 101 queries (at the roughly \$22\,000 cost estimated above for full-depth pooling) before the resulting bronze labels are relied on broadly, rather than distilling from 5 queries alone.

\subsection{Active Learning: Results}
\label{subsec:active_learning_result}

\paragraph{Expanded silver standard.} This active-learning pass covers all 101 queries (versus the original 5) and adds 1{,}010 newly judged candidates, 852 (84.4\%) agreed unanimously between the two judges, 158 needed human tie-breaking, a disagreement rate close to the original pilot's 16\% once an initial data-merging bug that had corrupted roughly 40\% of the first labelling pass with a missing query field was found and fixed. Combined with the original 419, the expanded silver standard now contains \textbf{1{,}429 candidates spanning all 101 queries} (label distribution: 63 not relevant, 381 partially relevant, 985 highly relevant), a nearly 3.5$\times$ increase in size and full query coverage instead of a 5\% sample of it.

\paragraph{Bronze accuracy after retraining.} The distillation classifier was retrained on 1{,}427 of the 1{,}429 silver-labelled candidates spanning all 101 queries (two lacked a matching feature row, the same small gap noted for the pilot in Section~\ref{subsec:refit_fusion_ranker}) and re-applied to all 173\,262 candidates. A fresh 150-candidate spot-check, judged with the same stronger judge pair, found bronze-label accuracy rose from \textbf{65.9\% to 80.7\%}. Since the majority-class baseline also shifted (56.8\%~$\to$~64.3\%), the more informative figure is the margin over it, which nearly doubled (9.1 to 16.4 percentage points). This most directly confirms the original diagnosis. 100\% of the unanimously-judged spot-check candidates this time fell under a query the classifier had been trained on, compared to only 6\% previously. The out-of-distribution generalisation gap identified in the first spot-check was therefore a direct consequence of training on 5 of 101 query topics, not a flaw in the distillation approach itself, and expanding query coverage resolved it.

\subsection{Round-Based AL: Results}
\label{subsec:headline_deepening_result}

Each round labels 280 new candidates across the 14 headline queries and retrains the classifier, tracked against the fixed 100-candidate validation set. Table~\ref{tab:headline_rounds} shows the trajectory across three rounds.

\begin{table}[ht]
\centering
\caption{Round-based active learning trajectory: labels and validation accuracy per round.}
\label{tab:headline_rounds}
\begin{tabular}{lccc}
\toprule
\textbf{Round} & \textbf{New labels this round} & \textbf{Cumulative labels} & \textbf{Validation accuracy} \\
\midrule
1 & 280 & 280 & 79\% \\
2 & 280 & 560 & 83\% ($+4$pp) \\
3 & 280 & 840 & 83\% ($+0$pp) \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Stopping.} Round 3 produced no further improvement over round 2, a clean plateau after two rounds of genuine gain, which is exactly the stopping signal the method was designed to detect (Section~\ref{subsec:headline_deepening}'s round-based design follows the general active-learning stopping-criterion principle of monitoring diminishing returns \citep{settles2009active}). Further rounds were not run. One caveat. The validation set is only 100 candidates, so its standard error on a proportion near 80\% is itself roughly 4 percentage points, meaning the round-2 gain sits close to the edge of what is statistically distinguishable from noise on its own. The plateau at round 3 (an exact repeat, not a smaller but still-positive gain) is the more convincing part of the signal.

\paragraph{Result.} The 14 headline queries now hold 840 newly silver-labelled candidates on top of the 140 already present (10 per query from Section~\ref{subsec:active_learning}), roughly \textbf{70 silver-labelled candidates per headline query}, short of the 100-300 per query considered ideal for a fully reliable per-query evaluation benchmark (Section~\ref{sec:scaling_corpus}), but a deliberate, cost-justified depth rather than an arbitrarily chosen one.

\subsection{Production Divergence: Results}
\label{subsec:production_divergence_result}

\begin{table}[ht]
\centering
\caption{Four checks comparing production's own top-1,000-per-query ranking against the independent silver/bronze set.}
\label{tab:production_divergence}
\begin{tabular}{p{3cm}p{11cm}}
\toprule
\textbf{Check} & \textbf{Result} \\
\midrule
1. Overlap & Mean 77.2\% overlap between production's top-1{,}000 and the independent top-1{,}000 per query (median 81.0\%), but with real spread. Query 1 (``software companies'', the query used throughout Section~\ref{subsec:bm25_qual}'s qualitative BM25 discussion) overlaps only 34.9\%. \\
2. Pick accuracy & Of production's top-1{,}000 picks that happen to already have an independent silver label (1{,}434 candidates), 76.0\% are confirmed highly relevant, 22.2\% only partially relevant, and 1.8\% not relevant at all. \\
3. Coverage gap & Of 1{,}499 silver-verified highly relevant candidates, \textbf{27.3\% (409) are entirely absent from production's own top-1{,}000 ranking}, not ranked lower, simply never surfaced. Worst-affected queries: ``wealth management firms'' (45 missed), ``investment management firms'' (40), ``software companies'' (27). \\
4. Rank agreement & For the 77{,}993 candidates present in both rankings (spanning all 101 queries), the mean per-query Spearman correlation between production's rank and the independently-trained classifier's predicted probability is \textbf{$-0.056$}, essentially no relationship, far from the strongly negative correlation (rank and predicted relevance moving in opposite, i.e. agreeing, directions) that would indicate the two rankings agree. \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} These four checks quantify, rather than merely argue, the circularity concern raised at the start of this thesis (Chapter~\ref{ch:introduction}). Production's own ranking is right more often than not when judged against the independent silver standard (Check 2), but it misses over a quarter of genuinely relevant companies outright (Check 3), and even among candidates both systems agree are worth considering, production's internal ranking carries almost no relationship to independently-verified relevance (Check 4, $-0.056$). Benchmarking this thesis's retrieval pipeline against production as a strong baseline (as recommended by Istari's own engineering team, Section~\ref{ch:introduction}) remains the right framing, but these results make explicit exactly how much headroom that baseline leaves, both in what it fails to surface at all and in how loosely its internal ranking tracks true relevance among what it does surface.

\subsection{Production vs. Independent Ranker: Results}
\label{subsec:goi_vs_ours_ranking_result}

\begin{table}[ht]
\centering
\caption{Recall@$k$: production's own ranking vs. the independently cross-validated ranking, against the same silver-verified relevant set.}
\label{tab:goi_vs_ours}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{6}{c}{\textbf{Recall@$k$}} \\
\textbf{System} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
\multicolumn{7}{l}{\emph{All 101 queries}} \\
Production (GOI)               & 0.011 & 0.048 & 0.088 & 0.225 & 0.366 & 0.720 \\
Ours (cross-validated)         & 0.002 & 0.012 & 0.018 & 0.066 & 0.180 & 0.659 \\
\midrule
\multicolumn{7}{l}{\emph{19 deeply-labelled queries only (5 pilot + 14 headline, Sections~\ref{subsec:pooled_gold_standard}, \ref{subsec:headline_deepening})}} \\
Production (GOI)               & 0.050 & 0.118 & 0.165 & 0.332 & 0.459 & 0.746 \\
Ours (cross-validated)         & 0.012 & 0.042 & 0.073 & 0.183 & 0.325 & 0.654 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Production's own ranking outperforms the independently cross-validated ranking at \emph{every} $k$ value tested, on both the full 101-query set and the 19 deeply-labelled queries. This is not the result this thesis's other findings would predict, and it is reported here without qualification rather than omitted. The most plausible explanation lies in the composition of the silver-standard candidate pool itself, not in the correctness of the labels. The pooling methodology (Section~\ref{subsec:pooled_gold_standard}) drew candidates from six sources, one of which was production's own frozen output, and Section~\ref{subsec:production_divergence_result}'s Check 2 already showed that 72.7\% of silver-verified relevant companies were already present in production's own top-1{,}000.

Production therefore receives a structural advantage on this specific comparison. A large share of what later became a confirmed-relevant label was, by construction, a company production itself had already surfaced. This does not undermine the label-correctness evidence elsewhere in this thesis (the labels themselves are independently judged, Section~\ref{subsec:pooled_gold_standard}), but it does mean this particular head-to-head ranking comparison cannot yet support a claim that this thesis's ranking algorithm retrieves relevant companies more effectively than production does. Testing that claim fairly requires a silver-verified relevant set drawn from a candidate pool production had no part in constructing, which is exactly what the random 300{,}000-company sample (Section~\ref{sec:scaling_corpus}) is for.

\subsection{Judgment Coverage: Results}
\label{subsec:label_coverage_bpref_result}

\paragraph{Finding.} Coverage at $k=10$ varies by more than an order of magnitude across systems. BM25 (6.5\%), production (5.6\%), and GTE-large (5.8\%) carry far more already-judged candidates among their very top results than the cross-validated fusion ranker (0.8\%) or MiniLM (0.4\%) do, tracking closely with which systems were and were not among the six original pooling sources (production, BM25, and GTE-large were among them, Section~\ref{subsec:pooled_gold_standard}, while MiniLM was not. The leave-one-query-out cross-validated ranker is retrained per held-out query, so it is never identical to the pooling-time fusion ranker either).

Under standard Recall@1{,}000, the six systems rank MiniLM (0.751) $>$ GTE-large (0.741) $>$ Linq (0.732) $>$ production (0.720) $>$ BM25 (0.664) $>$ the cross-validated fusion ranker (0.659), placing this thesis's own ranking algorithm last of the six. Under bpref, correcting for exactly the coverage disparity just measured, the ordering changes to Linq (0.609) $>$ GTE-large (0.583) $>$ production (0.568) $>$ the cross-validated fusion ranker (0.541) $>$ MiniLM (0.506) $>$ BM25 (0.495). The cross-validated ranker moves from last to fourth, ahead of both MiniLM and BM25 and closer to production, while MiniLM, the apparent Recall@1{,}000 leader, drops to fifth.

This does not reverse Section~\ref{subsec:goi_vs_ours_ranking_result}'s finding into an outright win. Production still edges out the cross-validated ranker under bpref too, but it substantially narrows the gap and confirms quantitatively, not only qualitatively, that part of the original comparison's apparent production advantage was a coverage artifact of the pooling process rather than a genuine difference in ranking quality.

\subsection{Production-Independent Test: Results}
\label{subsec:production_independent_eval_result}

\paragraph{Result.} Of the 1{,}229 trustworthy pooled candidates, both judges produced a usable label for every one. 1{,}130 (91.9\%) agreed unanimously, comparable to the 93.8\% unanimous rate this same upgraded judge pairing reached in Section~\ref{subsec:pooled_gold_standard_result}. The remaining 99 (8.1\%) were resolved by manual tie-break, none of which turned out highly relevant (label distribution across the full 1{,}229: 271 not relevant, 263 partially relevant, 695 highly relevant). The confirmed-relevant count of 695 across the 19 queries is therefore final rather than a lower bound.

\begin{table}[ht]
\centering
\caption{Production-independent evaluation: Recall@1{,}000 and NDCG@1{,}000 against the 695 blindly-verified relevant companies, across all four corpus tiers.}
\label{tab:production_independent_eval}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrrrrrrrr}
\toprule
& \multicolumn{4}{c}{\textbf{Recall@1{,}000}} & \multicolumn{4}{c}{\textbf{NDCG@1{,}000}} \\
\textbf{Method} & \textbf{100K} & \textbf{200K} & \textbf{300K} & \textbf{400K} & \textbf{100K} & \textbf{200K} & \textbf{300K} & \textbf{400K} \\
\midrule
MiniLM             & 0.004 & 0.210 & 0.423 & 0.592          & 0.002 & 0.099 & 0.200 & 0.283          \\
GTE-large          & 0.004 & 0.255 & 0.543 & 0.751          & 0.002 & 0.116 & 0.256 & 0.366          \\
Linq-Embed-Mistral & 0.002 & 0.305 & 0.639 & \textbf{0.885} & 0.001 & 0.160 & 0.327 & \textbf{0.462} \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{figures/fig_production_independent_scaling.pdf}
\caption{Recall@1{,}000 against the production-independent relevant set, as a function of corpus size, for all three retrieval methods evaluated.}
\label{fig:production_independent_scaling}
\end{figure}

\paragraph{Finding.} At the 100K tier, every method scores at or near zero (Recall@1{,}000~$\leq$~0.004), the same artefact already anticipated for this experiment. Only a small fraction of the random 300{,}000-company sample lands in the smallest tier, so a specific set of 695 relevant companies has very little chance of overlapping with it by chance, this is a property of the tier's size, not a retrieval failure.

From the 200K tier onward, a real, steadily climbing signal appears for every method. At the full 400K tier, \textbf{Linq-Embed-Mistral reaches Recall@1{,}000~=~0.885}, ahead of GTE-large (0.751) and MiniLM (0.592), recovering the large majority of companies that were independently and blindly verified as relevant and that production had no opportunity to already know about. This figure is, notably, higher than this thesis's own headline Recall@1{,}000 result under the original protocol (0.823, Section~\ref{sec:learned_fusion_result}), on a test built specifically to remove the pooling bias that left the closest earlier comparison (Section~\ref{subsec:goi_vs_ours_ranking_result}) unable to support a coverage claim.

One reversal is worth flagging rather than glossing over. Linq-Embed-Mistral, not GTE-large, is the strongest method here, whereas Section~\ref{sec:scaling_corpus}'s main scaling evaluation found GTE-large consistently ahead on NDCG at every tier. These are not the same test, this one scores against a fixed, independently-judged relevant set restricted to companies production never ranked, while Section~\ref{sec:scaling_corpus} scores the same 19 queries' full silver-standard coverage, so the difference is a genuine finding about the two evaluations' distinct candidate composition rather than a contradiction. It does mean, though, that ``GTE-large is the strongest method overall'' (Section~\ref{sec:scaling_corpus}'s conclusion) should be read as scoped to that section's specific test, not as a claim that holds across every possible evaluation.

\paragraph{Qualitative error analysis: what does the pipeline still miss at 400K?} Section~\ref{sec:error_analysis} examined MiniLM's early misses against the original pseudo-relevance protocol. The same question is worth asking of this thesis's best available result too, not only its strongest single-model baseline. Re-encoding the 19 queries with MiniLM and GTE-large and re-ranking the full 400K tier identifies 77 companies (out of 695) that both methods miss from their own top-1{,}000, despite being independently confirmed highly relevant.

A recurring pattern in these misses, read across queries, is signal dilution rather than outright irrelevance. The company genuinely matches the query, but the matching capability is a minor part of a longer, differently-themed description rather than its dominant theme. For the query ``software companies'', several missed companies are unambiguously software vendors, Dataflex Software GmbH (glass-industry software), Waterloo Manufacturing Software (production scheduling software), Dolphin Software (microfinance software), but each summary foregrounds its specific industry vertical rather than the word ``software'' or a generic software-company framing, so the dominant semantic content of the description is closer to ``glass industry'' or ``microfinance'' than to ``software company'' as a category, even though it is one.

A related pattern appears for ``companies providing cloud migration services.'' PROMAIN offers cloud migration among six or seven other listed services (business process analysis, IT consulting, data analytics, AI/ML, low-code development), so ``cloud migration'' is present but not the description's dominant theme. This is not primarily a description-length effect, missed companies' summaries average 60 words, in line with the corpus generally, it is specifically about which theme a company's own description foregrounds. This complements rather than duplicates the vocabulary-mismatch failure mode already documented for BM25 (Section~\ref{subsec:bm25_qual}, where a company mentions a query term without being a match). Here, dense embedding models can under-rank a company that genuinely is a match, when its own description emphasises a narrower or different framing than the query's own vocabulary.

\paragraph{What this answers, and what it still does not.} This is the strongest evidence in this thesis that the retrieval pipeline finds and ranks genuinely production-blind relevant companies well, and at meaningfully large scale. It is not, strictly, the head-to-head ``does this pipeline outperform production'' comparison Section~\ref{subsec:goi_vs_ours_ranking} set out to make. Production has no ranking data at all for these companies, so a symmetric comparison remains undefined by construction, not just difficult, as already stated in the Method above.

Read together, the two results are complementary rather than contradictory. Section~\ref{subsec:goi_vs_ours_ranking} showed the one existing head-to-head test was compromised by pooling bias in production's favour. This section shows that once that bias is removed by using a genuinely blind candidate pool, the pipeline finds the large majority of what is actually there. That is the central open question this thesis has been explicit about not having closed (Section~\ref{subsec:honest_synthesis}), substantially, if not completely, addressed.


\subsection{Calibration Check: Results}
\label{subsec:calibration_check_result}

\begin{table}[ht]
\centering
\caption{Calibration: mean predicted probability vs. actual relevant rate, per decile (cross-validated).}
\label{tab:calibration}
\small
\begin{tabular}{rrrr}
\toprule
\textbf{Predicted probability bin} & \textbf{n} & \textbf{Mean predicted} & \textbf{Actual relevant rate} \\
\midrule
0.0--0.1 & 50  & 0.070 & 0.220 \\
0.1--0.2 & 161 & 0.149 & 0.360 \\
0.2--0.3 & 230 & 0.253 & 0.539 \\
0.3--0.4 & 314 & 0.352 & 0.589 \\
0.4--0.5 & 266 & 0.446 & 0.545 \\
0.5--0.6 & 203 & 0.553 & 0.675 \\
0.6--0.7 & 212 & 0.647 & 0.708 \\
0.7--0.8 & 187 & 0.750 & 0.695 \\
0.8--0.9 & 238 & 0.856 & 0.815 \\
0.9--1.0 & 406 & 0.960 & 0.899 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.6\textwidth]{figures/fig_calibration.pdf}
\caption{Calibration reliability diagram: mean predicted probability vs.\ actual relevant rate per decile bin, against the diagonal of perfect calibration.}
\label{fig:calibration}
\end{figure}

\paragraph{Finding.} The overall Brier score is 0.220 (0 = perfect, 0.25 = no better than always predicting 0.5), a real but modest improvement over chance, consistent with the modest classifier accuracy reported elsewhere. The calibration curve is asymmetric. In the high-confidence range (0.7--1.0) the classifier is close to well-calibrated, only mildly overconfident. In the low-to-middle range (0.2--0.5), it is substantially \emph{underconfident}. Candidates the model scores around 0.25 turn out relevant 53.9\% of the time, more than double the model's own stated probability. Practically, this means the simple 0.5 cutoff used throughout this thesis to define ``predicted relevant'' is likely under-counting true positives that happen to fall in this band, offering a plausible partial explanation for part of the coverage gap documented in Section~\ref{subsec:production_divergence_result}, and a concrete direction for future work (probability recalibration, e.g. Platt scaling or isotonic regression, before applying a hard threshold).

\subsection{Synthesis of Evaluation Findings}
\label{subsec:honest_synthesis}

\paragraph{Part 1: is the silver/bronze standard better than GOI's own output?} Pulling together every check in Sections~\ref{sec:silver_labels}--\ref{subsec:calibration_check}, the answer is not a single yes or no, it depends on which specific claim is being made, and the evidence does not point the same way for each:

\begin{itemize}
\item \textbf{Is the labelling methodology more defensible than trusting production's own output as ground truth?} Yes. Production's ranking cannot validate itself. It is circular by construction (Chapter~\ref{ch:introduction}). This thesis's silver standard uses independent multi-judge LLM labelling, blind calibration (Section~\ref{subsec:pooled_gold_standard}), and human tie-breaking, none of which production's own ranking has any equivalent of.
\item \textbf{Does this thesis's methodology find relevant companies production's own system misses entirely?} Yes, demonstrated. 27.3\% of silver-verified highly relevant companies are entirely absent from production's top-1{,}000 (Section~\ref{subsec:production_divergence_result}, Check 3).
\item \textbf{Does this thesis's ranking algorithm outperform production's ranking at retrieving relevant companies, head-to-head?} Still not directly shown, a symmetric head-to-head comparison is undefined by construction for the one candidate pool production never saw (Section~\ref{subsec:production_independent_eval}), and the one direct head-to-head test that was possible (Section~\ref{subsec:goi_vs_ours_ranking_result}) found the opposite, likely attributable to residual candidate-pool bias rather than a genuine quality reversal. The strongest available substitute evidence now exists, though. On a candidate pool production had no part in constructing, the pipeline recovers 88.5\% of independently, blindly verified relevant companies at the largest tested corpus tier (397,025 companies, Section~\ref{subsec:production_independent_eval_result}), which is complementary evidence for the same underlying claim rather than the strict head-to-head test itself.
\item \textbf{Is the classifier itself highly accurate and well-calibrated?} Only moderately. Brier score 0.220, with a real, systematic calibration gap in the low-to-middle confidence range (Section~\ref{subsec:calibration_check_result}).
\end{itemize}

The defensible claim this thesis supports is therefore narrower than ``the new labels are better.'' \emph{The independent silver standard is methodologically sounder for evaluation purposes and demonstrates concrete, measurable blind spots in production's own ranking, but, as Section~\ref{subsec:goi_vs_ours_ranking_result} found, does not yet establish that this thesis's own ranking outperforms production's.} Confirming or refuting that stronger claim requires evaluating against a silver-verified relevant set drawn from candidates production had no part in surfacing. The random, production-independent sample and nested scaling corpus described next (Section~\ref{sec:scaling_corpus}) were built with exactly this in mind, and the resulting production-independent test (Section~\ref{subsec:production_independent_eval}) now substantially, if not completely, addresses it.

\paragraph{Part 2: once the silver standard existed, did tuning and fine-tuning the pipeline against it actually help?} Sections~\ref{subsec:hyperparameter_tuning}--\ref{subsec:ann_tuning} ran four further, genuinely independent experiments against that silver standard, each with its own leakage-free held-out comparison, and the results are again mixed rather than uniformly positive. Wherever a numerical gap between two methods is reported below, it is checked with a \emph{paired significance test} (the Wilcoxon signed-rank test), a widely recommended choice for exactly this kind of per-query comparison in IR evaluation \citep{smucker2007comparison}. Rather than just comparing two mean scores, it compares the two methods query-by-query on the same 19 queries and asks whether one method winning more often than the other could plausibly be due to chance alone. A small $p$-value means the gap is unlikely to be noise. A large one means the sample of 19 queries is too small to tell the two methods apart with confidence, even if their point estimates differ:

\begin{itemize}
\item \textbf{Does systematically tuning the fusion ranker's hyperparameters help?} Inconclusively. Recall@1{,}000 and NDCG@1{,}000 move up while NDCG@10 moves down, a directionally plausible broad-list-versus-top-of-list trade-off, but a paired significance test on the 19 leakage-free queries finds none of these three differences statistically distinguishable from noise ($p=0.06$--$0.18$), so this should be read as suggestive rather than established (Section~\ref{subsec:hyperparameter_tuning_result}).
\item \textbf{Does a different model class, a small neural network, do better than the tuned tree ensemble?} At broad cutoffs, yes, and this is the one statistically confirmed improvement in this thesis. The neural network's NDCG@1{,}000 gain over both the untuned and tuned gradient-boosted tree ensemble is significant under a paired test ($p=0.003$ in both cases). Its apparent top-of-list gain (NDCG@10) points the same direction but is not statistically distinguishable from noise at $n=19$ (Section~\ref{subsec:neural_fusion_ranker_result}), and the result is based on a single random architecture search rather than the fully cross-validated search used for the tree ensemble, so it is the most defensible positive result of any improvement attempted here, not an unqualified one.
\item \textbf{Does fine-tuning the pretrained reranker on this task's own labels help?} It depends entirely on how the training signal is constructed. A first attempt, trained on severely imbalanced data (2.3\% negative examples), made the reranker worse at every cutoff. A second attempt, with explicit hard negatives and a pairwise ranking loss, reversed this and genuinely beat the pretrained reranker at $k=500$ and $k=1{,}000$, while still trailing it at $k=10$ and $k=50$ (Section~\ref{subsec:reranker_finetuning_result}), the same top-of-list-versus-broad-list trade-off seen in the hyperparameter tuning result.
\item \textbf{Does approximate search remain viable once the corpus is scaled up?} Yes, clearly, and this is the one experiment in this group without a trade-off. HNSW-based ANN search matches exact search's Recall@1{,}000/NDCG@1{,}000 within 0.5--2.5\% while running 25--78$\times$ faster across every corpus tier tested. At the 400K tier this near-equivalence is statistically confirmed, not just numerically small (paired Wilcoxon $p=0.86$--$0.92$), confirming at real (if still sub-20-million) scale the same favourable trade-off first observed on a synthetic corpus earlier in this thesis (Section~\ref{subsec:ann_tuning_result}).
\end{itemize}

A pattern recurs across three of these four experiments. Whenever a model's training or search objective is reweighted toward broad ranking quality (a large Recall@$k$/NDCG@$k$ at high $k$), it tends to gain coverage at some cost to precision at the very top of the list. Neither direction is unconditionally better. The right choice depends on whether the pipeline's downstream consumer looks at a handful of results directly or takes a broad candidate pool for further processing (in this pipeline's own case, the latter, Section~\ref{sec:hybrid_fullpool}). The neural network result is the one clear exception to that pattern. The ANN result is not subject to it at all, approximate search's speed gain here costs essentially no quality anywhere in the ranking.

Taken together with Part 1, this thesis's honest position is that its methodology is demonstrably more rigorous than trusting production's own output, that several concrete, measurable improvements to the pipeline itself have been found (a neural fusion ranker, a properly-trained fine-tuned reranker, and a large, safe efficiency gain from ANN search), and that the single strongest possible claim, that the resulting system retrieves relevant companies more effectively than production overall, remains open pending the production-independent evaluation described in the next section.

\paragraph{Limitation: precision is a lower bound, not an exact figure.} Every precision@$k$ number reported in this thesis is computed against the pooled, actively-sampled silver standard, not an exhaustive judgment of every retrieved candidate. A retrieved company only counts as a true positive if it happens to have already been silver-labelled. A genuinely relevant company that was simply never pooled or judged contributes neither a true positive nor a false positive. It is silently invisible to the metric. Recall@$k$ is unaffected by this, since it is computed only over the set of candidates that do have a silver label. Precision@$k$, however, is systematically biased downward by an unknown amount, since some fraction of a method's retrieved-but-unlabelled candidates are almost certainly relevant and simply were never reviewed. This is a known consequence of evaluating against pooled rather than exhaustive judgments \citep{baryossef2008random, lu2016effect}, not a flaw specific to this thesis's pipeline, but it means precision figures throughout should be read as conservative lower bounds rather than exact values.

\paragraph{A second, distinct bias: active-learning sampling skews which relevant companies get verified, not just how many.} The previous paragraph's claim that Recall@$k$ is unaffected by incomplete pooling is correct in a narrow, technical sense, that metric is self-consistent with respect to whichever set of candidates carries a label, but it does not follow that Recall@$k$ is an unbiased estimate of recall against the true, natural population of relevant companies. Section~\ref{subsec:active_learning}'s active-learning step selects candidates for labelling by \emph{uncertainty sampling}. For every query, the ten candidates the distillation classifier is currently least confident about (predicted probability closest to 0.5) are the ones sent for judging, exactly the candidates hardest to classify, by construction.

This is a well-established and reasonable strategy for improving a classifier efficiently under a limited labelling budget \citep{settles2009active}, and was the only realistic way to extend silver-standard coverage from 5 to all 101 queries within this thesis's timeframe. The alternative, randomly or exhaustively judging enough candidates per query for full coverage without any targeting, risked approaching the \$28\,000 full-judging figure estimated in Section~\ref{sec:silver_labels} rather than the far smaller cost this active-learning pass actually incurred by judging only ten uncertain candidates per query.

But it also means the resulting silver-verified relevant set is not a random or representative sample of all relevant companies, it is disproportionately made up of boundary, ambiguous cases that were hardest to classify in the first place. If genuinely relevant companies with thin or ambiguous descriptions are, in general, also harder for any retrieval method to rank highly (a plausible mechanism, since the same weak signal that confuses the classifier would also weaken a dense embedding's similarity score or a keyword match), then Recall@$k$ measured against this actively-sampled set likely understates the recall these methods would achieve against a random, representative sample of relevant companies. 

Both Recall@$k$ and Precision@$k$ throughout this thesis should therefore be read as conservative estimates for two distinct reasons rather than one: incomplete pooling depth and active-learning sampling bias. Disentangling exactly how much each contributes would require judging a genuinely random sample of the full candidate pool at additional labelling cost, and is left as future work alongside the exhaustive-ranking extension described next.

\paragraph{A third bias, harder to quantify: the author personally breaks judge disagreements, then those decisions score the author's own system.} Throughout this thesis, whenever the two LLM judges disagree on a candidate's relevance (roughly 16\% of candidates pooled across every judging round, Figure~\ref{fig:judge_disagreement_rate}), the disagreement is resolved by manual review, and that manual review was done by the author. Those resolved labels then feed directly into the silver standard used to score every retrieval method compared in this thesis, including the ones the author designed and tuned. This is a real conflict of interest that cannot be removed retroactively, only disclosed and partially mitigated.

Two things were done to limit it, though neither fully closes the gap. First, the blind calibration check reported in Section~\ref{subsec:pooled_gold_standard_result} showed the author's own blind judgments (made without seeing the existing silver label) matched it 78.7\% of the time ($\kappa = 0.603$), evidence the author is not wildly out of step with the judges in isolation, but that check tests general labelling agreement, not tie-breaking objectivity specifically, and does not rule out the more subtle failure mode of resolving genuinely ambiguous cases in whichever direction happens to favour a result the author expects or hopes to see.

Second, and more directly, the joint external review with the annotator at Istari (Section~\ref{subsec:external_review_prep}) was intended to provide a check that does not run through the author at all. The overlap with the existing, author-influenced silver labels turned out too small to support that comparison directly, so a fresh LLM-judging round was run on the full returned sample instead (Section~\ref{subsec:external_review_result}), agreeing with the Istari annotator 92.5\% of the time (kappa 0.196) without the author appearing anywhere in that comparison. This substantially closes the gap this paragraph is about. A separate, author-involved human-human kappa was also computed (87.5\% agreement, Section~\ref{subsec:external_review_result}), but should not be mistaken for the independent check itself.

\subsection{Silver-Standard Coverage: Results}
\label{subsec:external_review_prep_result}

\paragraph{Finding.} Silver coverage is not spread evenly across the ranking, it clusters heavily toward the bottom of each query's top-1{,}000 list. For query 92, every single silver-verified candidate falls in ranks 701--1{,}000, leaving ranks 1--700 with no independent verification at all. Query 1 has thin coverage (9--12 candidates per band) scattered through ranks 1--700, with the bulk (58 of 99) again sitting in ranks 701--1{,}000. This is a direct consequence of how the silver standard was built. Active learning (Section~\ref{subsec:active_learning}) deliberately samples uncertain, boundary-case candidates for labelling, not the candidates the classifier is already most confident about, so verified labels naturally accumulate low in the ranking rather than high.

Where silver labels do exist, relevance is generally high (90--100\% for query 92, 100\% in four of five bands for query 1) but not perfect, query 1's rank 701--1{,}000 band drops to 55.2\% highly relevant. The top of the ranking, where coverage is thinnest, therefore remains unverified rather than confirmed reliable.

\subsection{What the External Review Found}
\label{subsec:external_review_result}

The curated 100-candidate export was the one returned for both queries, not the full top-1{,}000, so the results below reflect that lighter, non-production-prioritised sample rather than a full-ranking review.

\paragraph{The free consistency check had too little overlap to compute.} Only 2 of the 100 returned candidates for query 1 already carried an internal silver label (1 agreement, 1 disagreement), and none of the 100 for query 92 did, both far too few for a meaningful Cohen's kappa. This is a direct consequence of the curated format's own design. Prioritising candidates absent from production's results also means little overlap with the existing silver standard, which itself draws heavily on production as one of six pooling sources (Section~\ref{subsec:pooled_gold_standard}). Only a single reviewer at Istari was ever asked to label this sample, so there is no second, independent Istari label to compute an inter-reviewer kappa against in the first place.

\paragraph{A direct human-human kappa was computed instead.} Rather than relying on the sparse overlap with the existing silver standard, the author blind-labelled the same 200 candidates the Istari annotator returned (100 per query), with no rank or prior label visible, and compared the result against those labels. Raw agreement was 87.5\% (175/200), but Cohen's kappa is only 0.049 on the original 3-class scheme and 0.000 once collapsed to a binary relevant/not-relevant split. This gap between high raw agreement and a near-zero kappa is a known statistical artefact, the ``kappa paradox'', that arises when one label dominates the distribution, here, 91\% of the Istari annotator's labels are ``highly relevant'' (182/200) and none are ``not relevant'' at all, and should be read alongside the raw agreement figure rather than in isolation. Disagreements (25 of 200, 12.5\%) skew toward the author rating a candidate \emph{more} relevant than the Istari annotator did (16 of 25) rather than less (9 of 25). This is a genuine, if distribution-limited, human-human data point alongside the LLM-based kappa figures reported in Section~\ref{subsec:pooled_gold_standard_result} and the fresh LLM-judging comparison below.

\paragraph{A fresh LLM-judging round confirmed the same pattern from a third, author-independent angle.} Since Istari's annotator could not be asked to label further data, all 200 returned candidates were instead judged by the current LLM ensemble, Gemini 3.6 Flash and Claude Sonnet 5, the same pair and enriched prompt used in Section~\ref{subsec:production_independent_eval}, with no involvement from the author at all. The two judges agreed unanimously on 199 of 200 candidates. Against the Istari annotator's labels, raw agreement was 92.5\% (184/199) with kappa 0.196 on the 3-class scheme. The binary collapse is undefined rather than merely low, since those labels never include ``not relevant'' at all, so their binary marginal is constant and Cohen's kappa has no variance left to measure. Against the author's own blind labels, raw agreement was 94.5\% (188/199) with kappa 0.069 (binary 0.000). The LLM ensemble's own labels are just as skewed as both human sources, 197 of 199 (99\%) ``highly relevant'' and none ``not relevant'', so the same kappa-suppressing mechanism applies a third time. Three independent sources, the author, the Istari annotator, and an LLM ensemble that saw neither set of human labels, now agree with each other at 87.5--94.5\% raw agreement on this sample, with kappa uniformly suppressed by that shared skew rather than by genuine disagreement.

\paragraph{A stronger result: independent human confirmation of the production-independent coverage claim.} Because the curated export specifically prioritised candidates absent from production's own top-1{,}000, the returned labels double as a small, independent human check on the exact claim Section~\ref{subsec:production_independent_eval} makes with LLM judges. Does this pipeline surface genuinely relevant companies production misses entirely? Of the 189 returned candidates not present in production's results for their query (100 of 100 for query 1, 89 of 100 for query 92), \textbf{100\% were judged at least partially relevant, and 171 (90.5\%) highly relevant}, by a human reviewer with no visibility into the pipeline's own confidence or ranking. This sample is small and restricted to two queries and one reviewer, so it does not replace the production-independent evaluation's own, larger-scale result (88.5\% recall at the largest tested corpus tier, Section~\ref{subsec:production_independent_eval_result}), but it is independent, human, and points in the same direction. Companies this pipeline finds beyond what production surfaces are not noise.

\section{Phase IV: Learned Retrieval Revisited}
\label{sec:phase4_result}

\subsection{Hyperparameter Tuning: Results}
\label{subsec:hyperparameter_tuning_result}

\paragraph{Results.} The best combination found by the search (selected on the 82 tuning-only queries) was \texttt{max\_depth=3}, \texttt{max\_iter=50}, \texttt{learning\_rate=0.03}, \texttt{min\_samples\_leaf=10}, \texttt{l2\_regularization=0.5}, notably shallower and using far fewer boosting iterations than the original hand-picked configuration (\texttt{max\_depth=4}, \texttt{max\_iter=150}).

\begin{table}[ht]
\centering
\caption{Recall@$k$/NDCG@$k$ on the 19 leakage-free queries: untuned baseline vs.\ best combination found.}
\label{tab:hyperparameter_tuning}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{6}{c}{\textbf{Recall@$k$}} \\
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
Baseline (untuned) & 0.012 & 0.042 & 0.073 & 0.183 & 0.325 & 0.654 \\
Tuned               & 0.010 & 0.051 & 0.070 & 0.206 & 0.360 & \textbf{0.678} \\
\midrule
& \multicolumn{6}{c}{\textbf{NDCG@$k$}} \\
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
Baseline (untuned) & \textbf{0.034} & \textbf{0.039} & \textbf{0.049} & 0.101 & 0.156 & 0.286 \\
Tuned               & 0.015 & 0.033 & 0.044 & \textbf{0.108} & \textbf{0.168} & \textbf{0.295} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Tuning moves the metric the search was told to optimize in the intended direction. Recall@1{,}000 rises from 0.654 to 0.678 and NDCG@1{,}000 from 0.286 to 0.295, both leakage-free numbers since the 19 deep queries never influenced the search. NDCG@10 moves the other way, from 0.034 to 0.015, and the same pattern holds throughout the $k$-sweep. The tuned model beats the baseline at $k \in \{300, 500, 1{,}000\}$ but loses at $k \in \{10, 50, 100\}$.

A paired significance test on these same 19 queries (Wilcoxon signed-rank) tells a more cautious story than the point estimates alone. None of the three differences just described, NDCG@10 ($p=0.109$), NDCG@1{,}000 ($p=0.182$), or Recall@1{,}000 ($p=0.063$), reaches conventional significance at $n=19$, though the recall difference sits close to the threshold and its 95\% paired-bootstrap confidence interval ($[-0.014, +0.057]$) leans positive. The honest reading is therefore not ``tuning trades top-of-list precision for broad-list recall'' stated as an established fact, but that this specific 19-query sample is too small to distinguish either direction from noise with confidence, even though the direction of each change is consistent with a real, mechanistic explanation. NDCG@1{,}000 and Recall@1{,}000 reward broad coverage of the full ranked list, not sharpness at the very top, so the search favoured hyperparameters (shallower trees, fewer boosting iterations) that plausibly spread relevance signal more evenly across the ranking. Whether the tuned or untuned configuration is preferable in practice still depends on how the ranking is consumed downstream, the tuned configuration's broad-list orientation matches how this pipeline is actually deployed (a candidate pool handed to a downstream reranker, Section~\ref{sec:hybrid_fullpool}), but this thesis's own data does not let that preference rest on a statistically confirmed trade-off.

On the all-101-query subset (not leakage-free, since 82 of those queries informed the search), the same qualitative pattern holds. NDCG@1{,}000 improves (0.166~$\to$~0.173) while NDCG@10 degrades (0.007~$\to$~0.003), reported for completeness rather than as independent confirmation.

\subsection{Neural Network Fusion Ranker: Results}
\label{subsec:neural_fusion_ranker_result}

\paragraph{Results.} The best architecture found, selected purely on the 16 search-validation queries, was notably small: a single hidden layer of 16 units, dropout 0.1, learning rate $10^{-4}$, weight decay $10^{-3}$.

\begin{table}[ht]
\centering
\caption{NDCG@$k$ on the 19 held-out deep-coverage queries (leakage-free for all three models): untuned GBDT, tuned GBDT (Section~\ref{subsec:hyperparameter_tuning}), and the neural network.}
\label{tab:neural_fusion_ranker}
\begin{tabular}{lrrrrrr}
\toprule
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
GBDT baseline (untuned) & 0.034 & 0.039 & 0.049 & 0.101 & 0.156 & 0.286 \\
GBDT tuned               & 0.015 & 0.033 & 0.044 & 0.108 & 0.168 & 0.295 \\
Neural network            & \textbf{0.086} & \textbf{0.079} & \textbf{0.094} & \textbf{0.165} & \textbf{0.222} & \textbf{0.360} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Unlike every other tuning or fine-tuning experiment in this thesis, all of which traded top-of-list precision for broad-list recall or vice versa, the neural network's point estimate wins at \emph{every single} $k$ value tested, against both the untuned and the tuned GBDT, on Recall@$k$ and NDCG@$k$ alike (Table~\ref{tab:neural_fusion_ranker}, and the same pattern holds on Recall@$k$, not shown here for space). The gain is largest at the top of the list. NDCG@10 nearly triples over the untuned baseline (0.034~$\to$~0.086) and roughly sextuples over the tuned GBDT (0.015~$\to$~0.086), while NDCG@1{,}000 improves by a smaller but still substantial margin (0.286~$\to$~0.360).

A paired significance test on the same 19 queries (Wilcoxon signed-rank) confirms part of this picture more strongly than the rest. At NDCG@1{,}000, the neural network's advantage over both the untuned GBDT ($p=0.003$, 95\% paired-bootstrap CI $[+0.035, +0.114]$) and the tuned GBDT ($p=0.003$, CI $[+0.029, +0.105]$) is statistically significant, with a confidence interval that excludes zero in both cases, the broad-list improvement is real, not an artefact of query-level noise. At NDCG@10, however, where the point-estimate gain looks largest in relative terms, neither comparison reaches significance at $n=19$ (vs.\ untuned, $p=0.249$; vs.\ tuned, $p=0.080$, borderline but not significant), and the wider confidence intervals reflect how much a handful of queries can swing a mean over only 19 data points at that cutoff. The defensible claim is therefore narrower than ``wins at every $k$.'' The neural network's broad-list improvement is statistically confirmed, while its apparent top-of-list improvement, though consistent in direction and worth reporting, has not been distinguished from noise on this sample size.

The most plausible explanation lies in what the winning architecture actually is. A single hidden layer of 16 units is a small, close-to-linear model, not a deep network exploiting complex feature interactions. This thesis's ten fusion-ranker features are all continuous and already fairly informative (similarity scores, inverse ranks, the pretrained reranker's own score), the kind of smooth, well-behaved feature space where a compact network with smooth decision boundaries can plausibly outperform an ensemble of axis-aligned tree splits, particularly once combined with standardized features and class-weighted loss, both necessary for a neural network but irrelevant to a tree-based model. This result should still be read cautiously. It comes from a single random architecture search over 20 combinations and a single train/validation split rather than the full cross-validated search used for the GBDT, so the specific margin should not be over-interpreted. But the direction, a small, properly regularized neural network outperforming the tuned tree ensemble at broad cutoffs on a leakage-free held-out set, with that specific improvement statistically confirmed, is the most defensible positive result of any tuning or fine-tuning experiment attempted in this thesis.

\subsection{Fine-Tuning the Reranker: Results}
\label{subsec:reranker_finetuning_result}

\begin{table}[ht]
\centering
\caption{Recall@$k$ and NDCG@$k$ on the 19 held-out deep-coverage queries: pretrained \texttt{bge-reranker-v2-m3} vs. the fine-tuned version.}
\label{tab:reranker_finetuning}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{6}{c}{\textbf{Recall@$k$}} \\
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
Pretrained & \textbf{0.044} & \textbf{0.100} & \textbf{0.134} & 0.276 & \textbf{0.365} & \textbf{0.681} \\
Fine-tuned & 0.027 & 0.089 & 0.132 & 0.276 & 0.363 & 0.680 \\
\midrule
& \multicolumn{6}{c}{\textbf{NDCG@$k$}} \\
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
Pretrained & \textbf{0.137} & \textbf{0.110} & \textbf{0.115} & \textbf{0.187} & \textbf{0.227} & \textbf{0.350} \\
Fine-tuned & 0.078 & 0.087 & 0.098 & 0.170 & 0.209 & 0.334 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{First attempt: uniformly worse.} Fine-tuning made the reranker worse, not better, at every single $k$ value tested, on precision, recall, and NDCG alike (Table~\ref{tab:reranker_finetuning}). This is a clean negative result, not a marginal or mixed one. Unlike Section~\ref{subsec:hyperparameter_tuning}'s trade-off between top-of-list and broad-list quality, there is no $k$ at which the fine-tuned model wins.

The most plausible cause is visible directly in the training data. Of the 820 training examples, only 19 (2.3\%) were labelled \texttt{gold\_label}~$=0$ (not relevant). 594 (72.4\%) were labelled highly relevant. Training loss fell sharply within two epochs (0.646~$\to$~0.081), consistent with the $\sim$568M-parameter model rapidly fitting a training signal that is overwhelmingly ``this is relevant'', at the expense of the discriminative ranking ability the pretrained checkpoint already had. A reranker that has partly collapsed toward scoring most candidates as relevant would produce exactly this pattern: uniformly worse ranking at every cutoff, since its ability to \emph{distinguish} candidates, not just recognise relevance in isolation, is what degrades. This is consistent with, and reinforces, the general lesson from Section~\ref{subsec:hyperparameter_tuning}. What a model is trained or tuned against determines what it gets good at, and a training signal this skewed was never likely to preserve, let alone improve, ranking quality.

\begin{table}[ht]
\centering
\caption{Recall@$k$ and NDCG@$k$ on the 19 held-out deep-coverage queries: pretrained reranker vs. the first (MSE, imbalanced) and second (pairwise, hard negatives) fine-tuning attempts.}
\label{tab:reranker_finetuning_v2}
\begin{tabular}{lrrrrrr}
\toprule
& \multicolumn{6}{c}{\textbf{NDCG@$k$}} \\
\textbf{Model} & \textbf{10} & \textbf{50} & \textbf{100} & \textbf{300} & \textbf{500} & \textbf{1000} \\
\midrule
Pretrained          & \textbf{0.137} & \textbf{0.110} & \textbf{0.115} & \textbf{0.187} & 0.227 & 0.350 \\
Fine-tuned v1 (MSE) & 0.078 & 0.087 & 0.098 & 0.170 & 0.209 & 0.334 \\
Fine-tuned v2 (pairwise) & 0.070 & 0.078 & 0.102 & 0.175 & \textbf{0.240} & \textbf{0.363} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Second attempt: broad-list gains, top-of-list trade-off.} Fixing the class imbalance and switching to a pairwise loss produces a genuine improvement over the first attempt at nearly every $k$, and the fine-tuned model now actually \emph{beats} the pretrained baseline at $k=500$ (NDCG 0.240 vs.\ 0.227, Recall@500 0.435 vs.\ 0.365) and $k=1{,}000$ (NDCG 0.363 vs.\ 0.350, Recall@1{,}000 0.744 vs.\ 0.681), the first time in this thesis's fine-tuning experiments that a fine-tuned model outperforms its pretrained starting point. This is not, however, an unambiguous win. At $k=10$ and $k=50$ the fine-tuned model still trails the pretrained baseline. This is the same qualitative pattern already found in Section~\ref{subsec:hyperparameter_tuning_result}'s fusion-ranker tuning, a training signal built around broad ranking quality (here, distinguishing relevant from a large sampled pool of presumed-negative candidates) improves broad-list metrics at some cost to top-of-list precision. Taken together, both experiments point to the same conclusion. Reweighting a fusion or reranking model's training objective reliably trades top-of-list sharpness for broad-list coverage in this pipeline, and neither direction is unconditionally ``better'', the right choice depends on whether the downstream use case looks at a handful of results or consumes a broad candidate pool for further processing.

\section{Phase V: Scalability}
\label{sec:phase5_result}

\subsection{Scaling to a Larger Corpus: Results}
\label{sec:scaling_corpus_result}

\begin{table}[ht]
\centering
\caption{Recall@1{,}000 and NDCG@1{,}000 at the smallest and largest tiers, all four methods.}
\label{tab:scaling_eval}
\begin{tabular}{lrrrr}
\toprule
& \multicolumn{2}{c}{\textbf{Recall@1{,}000}} & \multicolumn{2}{c}{\textbf{NDCG@1{,}000}} \\
\textbf{Method} & \textbf{100K} & \textbf{400K} & \textbf{100K} & \textbf{400K} \\
\midrule
BM25       & 0.632 & 0.570 & 0.339 & 0.310 \\
MiniLM     & 0.678 & 0.610 & 0.364 & 0.330 \\
Linq-Embed-Mistral & \textbf{0.762} & 0.632 & 0.371 & 0.308 \\
GTE-large  & 0.750 & \textbf{0.638} & \textbf{0.410} & \textbf{0.361} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Finding.} Recall degrades gracefully for every method as the corpus grows $4\times$ (10--17\% relative decline, not a cliff), consistent with more distractor companies competing for a fixed top-1{,}000 budget rather than any retrieval failure. Linq-Embed-Mistral leads at 100K but degrades faster than GTE-large as the corpus grows, so by 400K the two are close on recall while GTE-large holds a clear NDCG lead throughout, meaning GTE-large is not just finding relevant companies but ranking them better. Latency tells a separate story. At 400K companies, BM25 is the \emph{slowest} of the four methods (647\,ms/query), slower even than the 4{,}096-dimensional Linq-Embed-Mistral embeddings (455\,ms), while GTE-large's 1{,}024-dimensional dense vectors are by far the fastest (112\,ms). GTE-large is therefore the strongest method in this nested-corpus scaling experiment on both quality and speed, while BM25, despite its simplicity, is both the weakest on quality and the slowest at this corpus size. Section~\ref{subsec:production_independent_eval_result} finds a different method ahead under a differently constructed candidate pool, so this ranking should be read as scoped to this specific test rather than as a universal ordering. Question (2), whether the original baseline-model ranking survives re-evaluation against the non-circular silver standard, remains a separate follow-up not yet started. This experiment reaches 397,025 companies, roughly 2\% of Istari's full 20-million-company database, and therefore demonstrates a favourable scalability trend up to the tested size rather than end-to-end performance at Istari's full production scale.

\subsection{ANN Index Tuning: Results}
\label{subsec:ann_tuning_result}

\begin{table}[ht]
\centering
\caption{Exact search vs.\ ANN (HNSW, \texttt{M}=16, \texttt{ef\_construction}=40, \texttt{ef\_search}=500, the cheaper-to-build configuration) at each tier, GTE-large, Recall@1{,}000/NDCG@1{,}000, and query latency.}
\label{tab:ann_tuning}
\begin{tabular}{lrrrrrrr}
\toprule
& \multicolumn{2}{c}{\textbf{Recall@1{,}000}} & \multicolumn{2}{c}{\textbf{NDCG@1{,}000}} & \multicolumn{2}{c}{\textbf{Latency (ms)}} & \\
\textbf{Tier} & \textbf{Exact} & \textbf{ANN} & \textbf{Exact} & \textbf{ANN} & \textbf{Exact} & \textbf{ANN} & \textbf{Speedup} \\
\midrule
100K & 0.750 & 0.735 & 0.410 & 0.405 & 28.3 & 1.13 & 25$\times$ \\
200K & 0.702 & 0.684 & 0.390 & 0.383 & 57.0 & 1.27 & 45$\times$ \\
300K & 0.663 & 0.656 & 0.373 & 0.371 & 85.1 & 1.36 & 62$\times$ \\
400K & 0.638 & 0.635 & 0.361 & 0.359 & 112.3 & 1.44 & 78$\times$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[ht]
\centering
\includegraphics[width=0.75\textwidth]{figures/fig_ann_tradeoff.pdf}
\caption{ANN speed-recall tradeoff at the 400K tier (GTE-large): Recall@1{,}000 vs.\ query latency across the \texttt{ef\_search} sweep, both build configurations, against the exact-search reference point.}
\label{fig:ann_tradeoff}
\end{figure}

\paragraph{Finding.} At a moderate \texttt{ef\_search} setting (500), ANN retrieval matches exact search within 0.5--2.5\% on both Recall@1{,}000 and NDCG@1{,}000 at every tier, while running 25--78$\times$ faster (Table~\ref{tab:ann_tuning}). A paired significance test at the 400K tier (Wilcoxon signed-rank over the same 19 queries) supports this reading directly rather than only by the size of the point-estimate gap. Neither the Recall@1{,}000 difference ($p=0.918$) nor the NDCG@1{,}000 difference ($p=0.860$) between exact and ANN search is remotely close to significant, with 95\% paired-bootstrap confidence intervals straddling zero in both cases ($[-0.025, +0.018]$ and $[-0.013, +0.007]$ respectively). Unlike the other comparisons in this chapter, where the absence of significance is a caution against overclaiming a difference, here it is itself the desired result. It is statistical evidence, not just a small percentage gap, that ANN's massive speed advantage does not come at a measurable quality cost at this operating point. The speedup grows with corpus size, from 25$\times$ at 100K to 78$\times$ at 400K, since exact search's cost scales with corpus size while HNSW's query cost grows only logarithmically, so ANN's advantage compounds exactly as the corpus scales further, directly supporting the proposal's premise that approximate search is what makes scalable top-$k$ retrieval practical. Higher-quality configurations can do marginally better still. \texttt{M}=32/\texttt{ef\_construction}=200 at \texttt{ef\_search}=200 reaches Recall@1{,}000$=$0.644 at the 400K tier, fractionally \emph{above} exact search's own 0.638 (an artefact of HNSW's approximate graph traversal occasionally surfacing a marginally different top-1{,}000 set, not a meaningful quality gain), at 1.40\,ms, still roughly 80$\times$ faster than exact search.

This is not a free lunch at every setting, the trade-off the proposal asks to characterize is real at the cheap end. At \texttt{ef\_search}=10, recall collapses to 0.16--0.28 depending on tier and build configuration, far below exact search, confirming that ANN's speed advantage is bought at real cost to recall once search width is set too aggressively. Build cost also differs sharply between configurations. At the 400K tier, \texttt{M}=16/\texttt{ef\_construction}=40 builds in 88.8\,s versus 556.5\,s for \texttt{M}=32/\texttt{ef\_construction}=200, roughly 6$\times$ slower, for a quality difference of at most 1--2\% once \texttt{ef\_search} is reasonably high. The cheaper build configuration is therefore the more practical choice overall: fast to build, fast to query, and recall/NDCG effectively indistinguishable from exact search once \texttt{ef\_search} is set to 500 or above.

This experiment's 25--78$\times$ speedup is substantially larger than Section~\ref{sec:ann_scaling_result}'s earlier 6.5--6.6$\times$ finding on the synthetic 2-million-vector corpus. The two are not a strictly controlled comparison, since they use different embedding models, different exact-search implementations, and different corpora, so the specific magnitudes should not be compared directly. What matters is that both experiments, one on synthetic filler data early in this thesis, one on the real, nested scaling corpus built later, agree on the same qualitative conclusion. Approximate search is a large, low-risk efficiency gain for this retrieval task, with negligible quality cost at a well-chosen operating point.

\section{Overall Experimental Findings}
\label{sec:overall_findings}
% ────────────────────────────────────────────────────────────

Table~\ref{tab:final_comparison} summarises the headline result from each experimental phase. Chapter~\ref{ch:conclusion} draws these findings together against this thesis's original research questions.

\begin{table}[!htb]
\centering
\small
\caption{Final comparison across the pipelines evaluated in this chapter. Each result is reported under the protocol it was actually evaluated against, since not every method was re-evaluated under every protocol (Table~\ref{tab:protocol_summary} defines each protocol).}
\label{tab:final_comparison}
\begin{tabularx}{\textwidth}{>{\raggedright\arraybackslash}p{2.8cm}>{\raggedright\arraybackslash}p{2.4cm}>{\raggedright\arraybackslash}p{2.4cm}X}
\toprule
\textbf{Method} & \textbf{Protocol} & \textbf{Headline metric} & \textbf{Interpretation} \\
\midrule
BM25 & Pseudo-relevance & Recall@1{,}000 $=$ 0.635 & Weakest baseline; limited by vocabulary mismatch \\
Best single dense model (MiniLM) & Pseudo-relevance & Recall@1{,}000 $=$ 0.741 & Strongest baseline despite smallest model size \\
3-way hybrid + reranker & Pseudo-relevance & Recall@1{,}000 $=$ 0.804 & Complementary signals close part of the oracle gap \\
Learned fusion ranker & Pseudo-relevance & Recall@1{,}000 $=$ 0.823 & Best result under the original protocol \\
Learned fusion ranker (retrained) & Silver standard & NDCG@10: 0.930 $\to$ 0.952 & Ranking quality holds; feature-importance interpretation reverses \\
Neural network fusion ranker & Silver standard & NDCG@1{,}000: 0.286 $\to$ 0.360 & Broad-list gain over tuned GBDT is statistically significant \\
Linq-Embed-Mistral & Production-independent & Recall@1{,}000 $=$ 0.885 & Strongest available evidence of genuine coverage gain \\
HNSW (ANN) & Silver standard, scaling tiers & Within 0.5--2.5\% of exact search & Large speed gain (25--78$\times$), no measurable quality cost \\
\bottomrule
\end{tabularx}
\end{table}


\chapter{Conclusion and Future Work}
\label{ch:conclusion}

\section{Summary of Contributions}

This thesis set out to answer a specific question (Chapter~\ref{ch:introduction}): can embedding-based semantic retrieval, combined with approximate nearest-neighbour search, generate accurate top-$k$ candidate sets from a large company dataset while keeping query latency low, and how does it compare to Istari's own production system? A learned fusion ranker reached the strongest result under the original protocol (Recall@1{,}000~$=$~0.823, Section~\ref{sec:learned_fusion_result}), but since no human relevance judgements exist for this task, that result alone would rest entirely on production's own, potentially circular, output. This thesis therefore built an independent, multi-judge silver standard (Section~\ref{subsec:pooled_gold_standard}), which showed that production's own ranking misses more than a quarter of independently verified relevant companies outright (Section~\ref{subsec:production_divergence_result}), and that one of the fusion ranker's own headline interpretive findings did not survive independent validation (Section~\ref{subsec:refit_fusion_ranker_result}). Re-tuning against that standard produced genuine, if uneven, gains, and approximate search matched exact search within a few percentage points while running tens of times faster, an advantage that grows rather than shrinks as the corpus scales (Section~\ref{subsec:ann_tuning_result}).

Read honestly and without overclaiming (Section~\ref{subsec:honest_synthesis}), a final production-independent test, evaluated exclusively on companies production had no opportunity to already know about, substantially closes the single most important claim this thesis had left open: at the full 397,025-company tested scale, the pipeline recovers 88.5\% of independently, blindly verified relevant companies, higher than this thesis's original headline Recall@1{,}000 figure. This is not, strictly, the symmetric head-to-head comparison against production's own ranking that Section~\ref{subsec:goi_vs_ours_ranking} set out to make, no such comparison is definable for a candidate pool production never ranked at all, but it is the strongest available evidence that this thesis's coverage gains hold up once the pooling bias favouring production is removed by construction.

\section{Future Work}
\label{sec:future_work}

Several concrete extensions were identified over the course of this thesis but not implemented, either for lack of time or because they depend on results not yet available. They are grouped here by what they would establish.

\paragraph{Closing the central open question.} The single most important claim this thesis had left open was whether its retrieval pipeline finds relevant companies production never surfaced, tested fairly rather than against a candidate pool partly pooled from production's own output. Section~\ref{subsec:production_independent_eval_result} now runs exactly this test, on the random, production-independent 300,000-company sample, and finds strong recall (0.885 at full scale) on a pool clean of that bias by construction, substantially, if not completely, closing the gap. One smaller piece of this remains. Re-scoring the original baseline embedding models' already-computed rankings against the current, non-circular silver standard, at no additional retrieval or encoding cost, would check whether the original baseline comparison (Section~\ref{sec:extended_baselines_result}) survives independent verification the same way the fusion ranker's own feature-importance finding was checked and found to partially reverse (Section~\ref{subsec:refit_fusion_ranker_result}).

\paragraph{Strengthening the evaluation methodology itself.} Precision figures throughout this thesis are conservative lower bounds rather than exact values, since the silver standard is pooled rather than exhaustive (Section~\ref{subsec:honest_synthesis}). A more exhaustive LLM ranking pass over an already-pooled candidate set, rather than the smaller actively-sampled subset judged here, would tighten this bound. It would need to keep drawing candidates from that same pooled, multi-source set rather than a single retrieval method's own output, otherwise the single-source circularity the silver standard was built to avoid reappears one layer downstream.

Paired significance testing (Wilcoxon signed-rank and paired bootstrap confidence intervals) has now been added for the three comparisons in this thesis where a directional claim was most load-bearing: tuned vs.\ untuned fusion ranker, neural network vs.\ tree ensemble, and ANN vs.\ exact search. The results were not uniformly confirmatory. The hyperparameter-tuning trade-off did not reach significance on the 19-query leakage-free sample, the neural network's broad-list (NDCG@1{,}000) gain was confirmed but its top-of-list (NDCG@10) gain was not, and ANN's near-equivalence to exact search was confirmed directly (Sections~\ref{subsec:hyperparameter_tuning_result}, \ref{subsec:neural_fusion_ranker_result}, \ref{subsec:ann_tuning_result}). Extending the same testing to the remaining comparisons not yet covered (fine-tuned vs.\ pretrained reranker, production vs.\ independent ranking) is a direct next step using the same per-query data and methodology.

The classifier's predicted probabilities are only moderately well calibrated, with a systematic gap in the low-to-middle confidence range (Section~\ref{subsec:calibration_check_result}). Probability recalibration, for example Platt scaling or isotonic regression, before applying the fixed 0.5 relevance threshold used throughout, is a direct, well-understood fix. Label propagation from judged candidates to their nearest, unjudged neighbours in embedding space \citep{zhu2002labelpropagation} remains a cheaper, complementary way to grow labelled data, identified during the active-learning expansion (Section~\ref{subsec:active_learning}) but not implemented.

The joint relevance-label review with the annotator at Istari (Section~\ref{subsec:external_review_prep}) has since returned, but only as the lighter, curated 100-candidate export for two queries rather than the full ranked list originally planned, so the LLM-human kappa originally intended could not be computed from the sparse pre-existing overlap. Judging all 200 returned candidates fresh with the current LLM ensemble closed this gap instead (92.5\% agreement, kappa 0.196, Section~\ref{subsec:external_review_result}), entirely without the author's involvement. A separate, author-involved human-human kappa was also computed by blind-labelling the same 200 candidates (87.5\% agreement, kappa 0.049), though its low kappa despite high raw agreement reflects the reviewer's heavily skewed label distribution rather than genuine disagreement.

A genuinely blind, non-circular human check on this thesis's own silver labels was also obtained a different way. A self-labelling exercise over a stratified 300-candidate sample, blind to the existing silver labels, reached a raw agreement of 78.7\% and a kappa of 0.603 (Section~\ref{subsec:pooled_gold_standard_result}). The remaining open item is a second, independent external reviewer at Istari, so that a genuine inter-reviewer kappa becomes possible rather than only a comparison against the author's own labels. A cleaner fix for the tie-breaking conflict of interest specifically would be blind tie-breaking, routing disagreement cases to a reviewer, ideally not the author, without revealing which system's ranking surfaced the candidate or at what position, so the resolution cannot be influenced by which result it will end up scoring.

\paragraph{Further modelling experiments.} The neural network fusion ranker (Section~\ref{subsec:neural_fusion_ranker}) used a random hyperparameter search over a single train/validation split rather than the fully cross-validated search used for the tree ensemble. Bayesian optimisation, mentioned as a candidate strategy but not implemented for lack of a suitable dependency, could search the same space more efficiently and with a proper nested cross-validated estimate of its final performance.

Fine-tuning was applied to the reranker (Section~\ref{subsec:reranker_finetuning}) but not to a bi-encoder embedding model directly, a larger and riskier undertaking given the modest size of the current silver standard, but one with real potential to improve first-stage retrieval quality rather than only reranking.

The boilerplate/platform-summary contamination identified in the underlying corpus (Section~\ref{sec:trust_filter_result}) was quantified but not corrected in any result reported here. Using the boilerplate-similarity score as an input feature to a trained model, rather than as a hard filter, would let the model learn how much to trust an imperfect signal instead of requiring it to be reliable enough for a fixed decision rule.

\paragraph{Scaling further.} Every experiment in this thesis operates on a corpus of at most 397,025 companies, roughly 2\% of Istari's full 20-million-company database. The ANN scaling results (Sections~\ref{sec:ann_scaling_result}, \ref{subsec:ann_tuning_result}) are consistent across a synthetic 2-million-vector test and a real 397,025-company test, and both point toward the same favourable trade-off, but neither directly measures behaviour at the full production scale. Testing whether retrieval quality, ANN's efficiency advantage, and the silver-standard construction pipeline itself all continue to hold at that scale would require a substantially larger data export from Istari than used in this thesis. That is the natural next step toward a production deployment of the methods developed here.

\bibliography{references}


\backmatter
\chapter{Code Availability}

This report's own \LaTeX{} source and the full analysis pipeline are available at \url{https://github.com/marmeepandya/masters-thesis}. Result outputs, trained model artefacts, and other intermediate files are available at \url{https://drive.google.com/drive/u/3/folders/1dRm6Oj8p3JlzWZsJoMJukuh19AkFJi1m}.

Company-level data from Istari's Global Organisation Index (GOI), the evaluation queries, and the production search results used as a comparison baseline are proprietary to ISTARI.AI and are not included in either location. Intermediate result files that do not expose raw company data, including relevance labels and evaluation metrics, are included where possible, so that the analysis can be reproduced conditional on independent access to the underlying GOI data.

\chapter{Ehrenwörtliche Erklärung}

Ich versichere, dass ich die beiliegende Bachelor-, Master-, Seminar-, oder Projektarbeit ohne Hilfe Dritter und ohne Benutzung anderer als der angegebenen Quellen und in der untenstehenden Tabelle angegebenen Hilfsmittel angefertigt und die den benutzten Quellen wörtlich oder inhaltlich entnommenen Stellen als solche kenntlich gemacht habe. Diese Arbeit hat in gleicher oder ähnlicher Form noch keiner Prüfungsbehörde vorgelegen. Ich bin mir bewusst, dass eine falsche Erklärung rechtliche Folgen haben wird.

% Declare below which AI tools you used in the process of writing your work,
% including text, image, code, and data generation. If you used a tool for a
% purpose not included in the list yet, add it to the list.
\begin{center}
  \textbf{Declaration of Used AI Tools} \\[.5em]
  \small
  \setlength{\tabcolsep}{5pt}
  \renewcommand{\arraystretch}{1.3}
  \begin{tabularx}{\textwidth}{@{}p{3.1cm}Xp{2.1cm}c@{}}
    \toprule
    \textbf{Tool} & \textbf{Purpose} & \textbf{Where?} & \textbf{Useful?} \\
    \midrule
    Claude Sonnet 5 (Claude Code) & Drafting, editing, and restructuring thesis text; proofreading; \LaTeX{} formatting & Throughout & Yes \\
    ChatGPT (web) & General writing and research assistance & Throughout & Yes \\
    Perplexity (web) & Web research and literature lookup & Throughout & Yes \\
    GPT-4o-mini (API) & LLM relevance judge, original judge pair & Ch.~4--5 & Yes \\
    Claude Haiku 4.5 (API) & LLM relevance judge, original judge pair & Ch.~4--5 & Yes \\
    GPT-5.4 (API) & LLM relevance judge, upgraded pair (bronze labels, active learning) & Ch.~4--5 & Yes \\
    Claude Sonnet 5 (API) & LLM relevance judge, upgraded pair (active learning, production-independent test) & Ch.~4--5 & Yes \\
    Gemini 3.6 Flash (API) & LLM relevance judge, paired with Claude Sonnet 5 (production-independent test, external review) & Ch.~4--5 & Yes \\
    \bottomrule
  \end{tabularx}
\end{center}

\vspace{2cm}
\noindent Unterschrift\\
\noindent Mannheim, den \today \hfill

\end{document}

