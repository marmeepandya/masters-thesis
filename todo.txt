Sentence-BERT (Reimers & Gurevych, 2019) - https://arxiv.org/abs/1908.10084
DPR — Dense Passage Retrieval (Karpukhin et al., 2020) - https://arxiv.org/abs/2004.04906
FAISS paper (Johnson et al., 2017) - https://arxiv.org/abs/1702.08734
BM25 — The Algorithm Behind the Search 
IR Evaluation metrics - https://nlp.stanford.edu/IR-book/html/htmledition/evaluation-in-information-retrieval-1.html
BEIR Benchmark - https://arxiv.org/abs/2104.08663

Tier 1 — Essential, read these first:

1. Robertson & Zaragoza (2009) — "The Probabilistic Relevance Framework: BM25 and Beyond" — The original BM25 paper. Short and readable. You're already using BM25, so this gives you the theoretical grounding to explain it properly in your thesis.
2. Karpukhin et al. (2020) — "Dense Passage Retrieval for Open-Domain Question Answering" (DPR) — DPR introduced the bi-encoder architecture trained with in-batch negatives, where each query is matched against relevant documents while treating other passages in the batch as hard negatives. OpenReview This is the foundational paper for dense retrieval and directly justifies your embedding approach.
3. Thakur et al. (2021) — "BEIR: A Heterogeneous Benchmark for Zero-shot Evaluation of Information Retrieval Models" — The benchmark paper everyone cites when evaluating retrieval systems across domains. BM25 achieves an average nDCG@10 of 43.4 on BEIR, improved to over 52.6 via hybrid reranking. Emergent Mind Your results fit squarely into this pattern.
4. Cormack et al. (2009) — "Reciprocal Rank Fusion outperforms Condorcet and individual Rank Learning Methods" — The RRF paper. RRF merges ranked lists from sparse and dense retrievers post-retrieval without architectural changes, making it a simple yet effective fusion baseline. arXiv This is what you'll implement next.
5. Johnson et al. (2019) — "Billion-scale similarity search with GPUs" (FAISS) — The FAISS paper. You're already using it, so read this to understand what IndexFlatL2 vs IndexIVFFlat vs HNSW actually means for your 20M scale-up.

Tier 2 — Read after you've implemented hybrid retrieval:

6. Khattab & Zaharia (2020) — "ColBERT: Efficient and Effective Passage Search via Contextualized Late Interaction over BERT" — ColBERT introduces a late interaction mechanism that computes relevance via a MaxSim operator over per-token embeddings, delivering over 170× speedup relative to existing BERT-based cross-encoders while being more effective than non-BERT baselines. Berkeley EECS Good context for your related work chapter.
7. Formal et al. (2021) — "SPLADE: Sparse Lexical and Expansion Model for First Stage Retrieval" — SPLADE leverages contextualized representations to perform lexical expansion through learned sparse representations ResearchGate, bridging the gap between BM25 and dense retrieval. Useful for the related work section.
8. Izacard et al. (2021) — "Contriever: Unsupervised Dense Information Retrieval with Contrastive Learning" — Contriever is competitive with BM25 on 11 out of 15 BEIR datasets for Recall@100, making it a strong unsupervised dense baseline comparable to your all-MiniLM-L6-v2 setup. OpenReview

Tier 3 — For when you're writing the thesis:

9. Lin et al. (2022) — "A Survey of Model Architectures in Information Retrieval" — A comprehensive overview of the whole field. Good to have read before writing your related work chapter.
10. Hybrid retrieval taxonomy paper (ResearchGate, 2025) — This covers the concept of complementarity ratio (RoC), which quantifies the complementary contributions of sparse and dense models and has been shown to directly correlate with improved retrieval performance. ResearchGate Useful framing for your motivation.

===========================================================================================================
SATURDAY / SUNDAY — goal: ask Ralph for a meeting Monday
===========================================================================================================

Repo status this was based on (checked directly, not from memory):
  - No 10_hnsw_scalability.py yet — ANN/scalability experiment not started
  - 08_llm_relevance_judge still uses llama3 on the original held-out set — GPT-4o/60-query
    expansion not done
  - No repo-root master comparison_all.csv — results still live per-pipeline under result/<name>/
  - result/summary_quality/ has 3 CSVs already computed (audit, query_contamination, sensitivity)

SATURDAY
  [ ] PRIORITY: Resolve the summary/junk contamination issue — see result/summary_quality/
        summary_quality_query_contamination.csv shows severe contamination for several queries:
          web hosting companies: 85.4% junk | cloud computing providers: 73.1% | e-commerce
          platforms: 53.1% | CMS: 33.2% | ISPs: 18.2% | web design agencies: 11.7%
        summary_quality_sensitivity.csv shows cleaning junk moves NDCG by ~2-9% (BM25/MiniLM/v2,
        varies by k) — not catastrophic, but real and consistently downward
        Steps:
          - Read summary_quality_audit.csv to confirm root cause (templated/generic
            "hosting"/"domain registrar" boilerplate summaries attached to many company profiles)
          - Decide + document: filter junk companies from the corpus before re-running
            evaluations, or keep as-is and report cleaned vs. original numbers side by side
          - Flag any existing comparison_all.csv numbers as "pre-cleaning" if a corpus filter
            ends up applied, since every current result was computed before this fix
  [ ] Qualitative error analysis: 3-5 queries where MiniLM underperforms v2
        Find companies v2 retrieves that MiniLM misses
        Write 1 paragraph of notes per query — becomes error analysis section
  [ ] Make sure every experiment has a saved CSV in result/
  [ ] Build a single repo-root comparison_all.csv pulling in every method (BM25, BGE, MiniLM,
        OpenAI, Nomic, HyDE, Hybrid RRF+Reranker, v2, all _summary variants) — none exists yet
  [ ] Draft 1-page chapter outline for August writing

SUNDAY
  [ ] Finish HNSW scalability sweep if not done Saturday (new 10_hnsw_scalability.py,
        IndexHNSWFlat on 98K corpus, sweep ef_search: [16, 32, 64, 128, 256], NDCG@10 vs latency)
  [ ] Check whether Manav replied (missing-companies list / quota fix / latency target) —
        note the answer either way, don't let it block the write-up
  [ ] Write a 1-page status summary for Ralph covering:
        - Current results table across all methods (BM25, BGE, MiniLM, OpenAI, Nomic, HyDE,
          Hybrid RRF+Reranker, Istari v2)
        - Ground-truth circularity finding: v2 scores NDCG@10 = 0.991 against production
          ground truth, suggesting the GT was generated by v2 or something close to it
        - Summary/junk contamination finding and how it was resolved (per Saturday's fix)
        - Open blockers: still no reply from Manav (emailed 2026-06-30) on missing-companies
          list / quota / latency target
        - Questions for Ralph: how to frame circular ground truth in the thesis narrative,
          whether corpus cleaning should happen before the "final" comparison numbers, and
          a timeline sanity check against the Sept 15 deadline
  [ ] Message Ralph Sunday evening or first thing Monday to request the meeting, attaching
        the 1-pager so the meeting starts from context instead of a status recap

===========================================================================================================
