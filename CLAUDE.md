For each .ipynb or .py or .sh or any other file that you generate, please try to avoid line breaks for comments. Ofc there are situations like syntax etc when you must break line otherwise the code will definitely not run, I am not referring to that. 
Always ask questions when you are confused. 
Never run a file, always ask the user to run, if it is complicated then you can guide how the user can run the file. 
You can always compile the .tex 